global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts-binance-auto-trader.yml

scrape_configs:
  - job_name: "binance-auto-trader-api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
