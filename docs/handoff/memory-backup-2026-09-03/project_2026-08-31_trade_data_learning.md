---
name: project_2026-08-31_trade_data_learning
description: 매매 실데이터 1,075건 분석 + 진입 지표 복원으로 사장님 사상을 실측 검증하고 로직에 반영 (Fix 239~245)
metadata:
  type: project
---

# 📊 2026-08-31 실데이터로 배운 것 (Fix 239~245)

## 자동 vs 수동 (30일, 종료 1,075건)

    자동  518건  기대값 -1.72/건   약 -853
    수동  557건  기대값 -41.0/건   약 -22,849   ← 손실의 96%

**자동매매는 거의 본전이다.** 그리고 자동 손실은 `AUTO_BB LONG` 하나(-875)뿐.

    AUTO_BB S    317건  손익비 2.60  기대값 +0.41  +126.54   ← 유일한 흑자
    PUMPSPLIT L   34건  손익비 1.05  +17.04
    AUTO_BB L    161건  승률 16.1%   -874.68

## 🎯 진입 지표 복원으로 사장님 사상이 실측 확인됐다

`entry_context` 가 12.1% 뿐이라 **진입 시각 캔들을 다시 받아 지표를 복원**했다
(`scripts/analyze_entry_patterns.py`, Fix 242 — `get_klines` 에 end_time 추가).

**수동 LONG 승 12 / 패 13** (= 사장님이 직접 잡으신 자리):

    지표          이긴 진입   진 진입
    CCI 15m        +110.6    +36.9
    3일 변동%       +65.5%    +35.5%
    RSI 15m          67.4     53.9
    볼밴위치 15m      0.877    0.611
    되돌림 4H         0.083    0.580
    OBV방향 4H       +0.168   +0.020

    되돌림 0.30~0.60   4건 75.0%   +684.76
    되돌림 1.00 초과   6건 33.3%  -1845.38   ← 원점 아래 = 최악

⇒ 사장님 「급등중에 조정은 다시 급등으로 간다」가 **숫자로 확인**됐다.

🚨 그런데 **자동 LONG 은 CCI -78 / RSI 39.7 / 볼밴 0.284 에서 산다** = 정반대 자리.
   그래서 승률 16.1% 이고 **어떤 지표도 승패를 못 가른다**(최대 차이 RSI4H +7.8).

**SHORT** — 수동·자동 모두 「덜 과열됐을 때」 이긴다:

    4H OBV     승 0.331/0.238   패 0.530/0.391   ← 사장님 사상 ④ 확인
    3일 변동%   승 53.1/8.9      패 98.3/19.9
    볼밴 4H     승 0.923         패 1.005 (상단 밖에서 숏 = 진다)

## 반영한 것

- **Fix 243/244** `surge_pullback` 신설 + auto_long_at_bottom **1순위**로 연결(사장님 선택 B).
  🚨 반드시 `extreme_bull`(3일 +30% skip)·패턴A skip **보다 앞**에 — 뒤면 3일 +45% 조건이
  수학적으로 미도달. **순서를 테스트로 고정**했다. 급락 경로는 그대로 살렸다.
- **Fix 245** SHORT OBV 게이트 0.6 → **0.35**. 공통 0.6 은 패자 중앙값(0.39~0.53)보다 위라
  **한 건도 못 걸렀다**. 승자(0.238~0.331)는 통과하도록 그 위에 뒀다.
- **Fix 240** entry_context 가 빈 채로 굳던 경로 수리(12.1% → 재시도).
- **Fix 236~238** 자본 권한 가드 + 자동 증거금 = 그 전략의 초기금액(사장님 선택 B).

## ⏸ 익절(TP/트레일링)은 일부러 안 건드렸다

근거는 있다 — 자동 **+1~3% 구간 72.2%가 손실 전환**, `TRAILING_TP` **19전 19승 +473**.
트레일링은 `peak >= TP1_PCT_DEFAULT(15%)` **AND 첫 TP 발동** 을 요구해 거의 안 걸린다.

**멈춘 이유 2가지**:
1. `close_reason` 이 **29.7%** 뿐 — 「어느 청산 로직이 좋았나」를 가릴 표본이 부족하다
2. 한 번에 4가지를 바꾸면 **Fix 244/245 의 효과를 귀속시킬 수 없다**

→ 배포 후 며칠 데이터를 보고 결정한다.

## 도구

- `scripts/analyze_trades.py --days N [--auto-only|--manual-only]` — 8개 섹션
- `scripts/analyze_entry_patterns.py [--manual-only|--auto-only] [--side] --top N` — 진입 지표 복원

관련: [[project_2026-08-30_sajangnim_strategy_doctrine_v3]] (⑨ 사장님 정정)
[[project_2026-08-31_doctrine_vs_code_audit]]

## 🏆 최대 발견 — 「하지 마라」를 계산해 놓고 그대로 들어가고 있었다 (Fix 247)

`entry_context` 중첩 구조를 자동 평탄화(Fix 246)하자마자 **64개 필드 중 상위가 전부 한 신호**:

    confluence.blocked          승 0.000  패 1.000   효과크기 -2.06
    sar_ichimoku.cloud_15m_ok   승 1.000  패 0.000   효과크기 +2.09
    ema_vcp.trend_ok            승 1.000  패 0.000   효과크기 +2.02
    sar_ichimoku.cloud_4h_ok    승 1.000  패 0.000   효과크기 +2.01
    confluence.score            승 50.0   패 0.0     효과크기 +1.49

⚠️ 독립 발견 6개가 아니라 **추세 정렬 하나를 6가지로 본 것**이다.

🚨 그리고 이건 **처음 나온 게 아니다** — `strategy_confluence.py:105-131` 주석에
**v139 백테스트가 이미 적혀 있었다**:

    AVOID     227건  -19,207 = 전체 손실 -22,068 의 **87%**
    CONFLICT   67건  적중률 **16.4%** / -1.86%   <- 금지보다도 나쁨
    AGREE      40건  57.5% / +2.00%

그런데 `strategy_confluence.evaluate` 호출자는 `api/v1/analysis.py`(화면) 와
`learning_sync_worker.py`(학습 저장) **둘뿐**이었다. **진입에는 한 번도 안 쓰였다.**

→ **Fix 247** `app/services/confluence_gate.py` 신설.
`_create_auto_bb_strategy`(자동 진입 5개 워커 공용 관문)에서 **생성 직전에만** 호출
(후보마다 돌리면 캔들 3회 x 40~100 심볼 = IP ban 위험, Fix 117/122 실제 사고).
fail-open. 기본 OFF + 「막았을 것」 예고 로그.
**사장님이 재시작 직후(로그 나오기 전) 바로 ON 하셨다** — 차단 비율은 사후 관측 중.

⚠️ 지켜볼 것: `AUTO_BB SHORT` 가 30일 기준 유일한 흑자(손익비 2.60)다.
게이트가 그것까지 막으면 승자를 자르는 것이다. 방향별 차단/통과 비율을 볼 것.

## 🔎 도구 자체의 버그도 하나 (Fix 246)

`entry_context` 71.2% 인데 리포트는 「비어 있습니다」였다 —
분석기가 `rsi_15m` 같은 **평탄 키를 가정**했는데 실제는 7개 하위 dict 로 중첩.
→ 키를 추측하지 말고 **전부 펼쳐 효과크기로 정렬**한다.
raw 차이로 줄세우면 RSI(0~100)가 항상 이기고 비율값(0~1)의 차이가 묻힌다.

## ✅ 배포 당일 실측 검증 (2026-08-31 저녁)

    Fix 240  데이터 수집   close_reason 29.7% -> **100%** / entry_context 22.0% -> **92.1%**
    Fix 245  SHORT OBV    AUTO_BB S 손익비 2.60 -> **8.84**, 기대값 +0.41 -> **+3.73** (16건, 표본 얇음)
    Fix 247  합의 게이트   6시간 **20건 차단** (ETHUSDT SHORT=AVOID / LIGHTUSDT LONG=CONFLICT)
    Fix 250  필수 조건     재시작 직후부터 LIGHTUSDT(3일 -18%) 가 더 이상 안 잡힘

🏆 **이중 방어가 실증됐다** — Fix 244 가 잘못 고른 LIGHTUSDT 를
   Fix 247(합의 CONFLICT)이 **이미 막고 있었고**, 그 뒤 Fix 250 이 애초에 안 고르게 고쳤다.

## 🚨 하루에 같은 함정 3번 — 전부 **실행 로그**가 잡았다

정적 분석으로는 안 보였다:

1. **Fix 249** `_create_long_strategy` 가 진입 직전에 `confirm_peak(LONG)` 를 또 부른다.
   `_turns_for_long` 은 rsi<=35 / cci<=-80 에서만 turn 을 세는데 급등 종목은 RSI 68 / CCI 248
   -> **수학적으로 0/2**. Fix 244 가 골라도 전부 죽었다 (볼밴 3차 0건과 같은 구조).
   교훈: **순서만 고정하고 뒤에 또 있는 게이트를 놓쳤다.**
2. **NameError 직전** — 호출부 2곳에 일괄로 `pattern=result.get(...)` 을 넣었는데
   알람 루프엔 `result` 가 없었다. 테스트가 잡았다.
3. **Fix 250** 「6중 4」 다수결이 **정의 조건 실패를 덮어썼다** —
   LIGHTUSDT 가 3일 **-18%** / 되돌림 0.568 / 볼밴 1.18 인데 타이밍 지표 4개로 통과.
   교훈: **정의 조건을 「N중 M」에 섞지 마라.** 필수/선택 두 축으로 나눈다.

## 다음에 볼 것

- `AUTO_BB L` 승률 16.1% / 기대값 -5.87 이 Fix 244/249/250 이후 움직이는가
  (오늘 12건 전패는 전부 **적용 이전** 진입분)
- 게이트 후보 (승 6건이라 아직 못 씀): `bb_top.levels.expected_drop_pct` 효과 **-1.97** /
  `pump_continuation.close_pos` -1.14 / `pump_dump.retrace_recovery_rate` -1.01
- 익절/트레일링 — `TRAILING_TP` 5전 5승 +92, `FORCE_SL` 21건 -217. 여전히 보류 중

## 🚨 Fix 252 — 내가 만든 회귀를 A/B 로 확인 (같은 날 저녁)

Fix 250 이 필수 조건 3개를 **전부 `blocked`** 로 세웠는데, 워커는 `blocked` 를 보면
**함수 전체를 조기 종료**한다 = **급락 경로(패턴 B)까지 통째로 사라진다**.

    Fix 251 상태   scanned=35  ROUND_TRIP_BLOCKED=35/35   <- LONG 진입 전멸
    Fix 252 적용   scanned=36  ROUND_TRIP_BLOCKED=14      B2=9 B1=8 B0=3 B3=1
    되돌린 직후    scanned=32  ROUND_TRIP_BLOCKED=31      B0=1        <- 다시 전멸

사장님이 내 지시대로 `git reset --hard` 했다가 되돌린 덕에 **우연히 A/B 대조**가 됐다.

⇒ 개념 분리: `blocked` = 사장님 사상 위반(원점 아래) **그것만** /
   `reject` = 이 경로가 아님 → 급락 경로로 흘러간다.

🚨 **교훈**: 「기동 실패면 되돌리십시오」 같은 롤백 지시는 **판정 조건을 정확히** 줘야 한다.
   스케줄러는 멀쩡했는데(로그 조회 시점 착각) 사장님이 정상 수정을 되돌리셨다.

## 🚨 entered=0 의 진짜 원인은 게이트가 아니라 **슬롯 포화**였다

    활성 29건 (LONG 20 / SHORT 9)  used=29/30   미실현 합계 **+195.12**

    이익의 출처:  ZKUSDT SHORT +85.95 / POLUSDT SHORT +79.16 / ZKCUSDT SHORT +12.04
                 = **SHORT 3건이 +177 = 전체 미실현의 90%**

손익비 8.84 / 기대값 +3.73 인 SHORT 가 **9자리**, 오늘 승률 0% 인 LONG 이 **20자리**.
그리고 11:06~14:02 사이 11건이 진입했다 = **진입은 막혀 있지 않았다**.
내가 조용한 3시간 창(08~10시)만 보고 「게이트가 과하다」로 오판했다.

⇒ 사장님 선택 **B**: 동시 보유 상한 **30 → 40**
   (`sajangnim_top_short_daily_limit` — 코드 아님, SystemSetting 하나. 재시작 불필요)
   ⚠️ B 는 LONG/SHORT 불균형을 직접 고치지 않는다. 진입의 69%가 LONG 이라
   늘어난 10자리도 대부분 LONG 이 채울 수 있다. LONG 새 로직 성적을 보고
   A(SHORT 전용 슬롯)를 재검토할 것.

## 상한 설정의 단일 진실

`sajangnim_top_short_daily_limit` **하나뿐**이다 (`services/position_limit.py`).
`sajangnim_max_concurrent_positions` 는 Fix 191 로 **퇴역** — 남아 있으면 경고만 뜬다.

## 🎯 Fix 253 — 사장님 데이터가 사장님 결정을 뒤집었다 (2026-09-01)

    Fix 49 (08-24)  사장님 verbatim "단계별 진입후 -5% 손실이면 청산"  -> SHORT 5%
    Fix 87 (08-25)  LONG 만 10% 로 상향. 근거 "-5% = 15m 알트 노이즈에 잘린다"

**실측이 그 근거를 반증했다:**
- LONG 이틀 연속 **승자 0명** (08-31 12건 / 09-01 11건, 둘 다 0.0%)
- 손절은 설정대로 정확히 작동 (설정 대비 초과 **중앙값 0.2%p**)
  = 느슨한 손절이 승률을 올린 게 아니라 **잃는 크기만 2배**로 키웠다
- 🚨 이익 중인 LONG 13건의 **최저 ROI 가 -0.1 ~ -4.3%** —
  **-5% 를 건드린 승자가 한 건도 없다**. 넘긴 건 SUPERUSDT(-6.5%) 하나인데 그것도 손실 중.

⇒ LONG 5% 로 복귀. 두 곳의 매직넘버를 `LONG_FORCE_SL_ROI` 상수 하나로 묶고
  **근거 실측을 값 옆 주석에** 남겼다 (Fix 87 이 근거 없이 뒤집힌 게 문제의 절반이었다).

⚠️ 되돌리기 전에 반드시 같은 측정을 다시 할 것 —
  「이익 중인 LONG 이 -5% 아래로 간 적이 있는가」.

## ⏳ 2026-09-01 현재: **아직 아무것도 검증 못 했다**

    수정후(08-31 09:00 이후 진입) 종료 = 전체 3건 / LONG 1건

09-01 손실 -156 중 **-143 이 수정 전 진입**이다. Fix 244~253 은 심판대에 오르지도 않았다.
**LONG 이 10건 넘을 때까지 아무것도 바꾸지 말 것.**

## 다음에 볼 것 (우선순위)

1. **수정후 LONG 10건 이상 쌓이면** 승률/기대값 비교 — 이게 전부의 출발점
2. **FLAT_CLEANUP 7/24건 (29%)** — 거래소에서 포지션이 사라졌는데 우리가 청산한 게 아님.
   동기화 문제인지 외부 요인인지 미조사
3. **볼밴 지지 붕괴 SHORT 신호** — 사상 ③ 에 있는데 코드에 없음 (BTR 의 진짜 자리)
4. **익절/트레일링** — TRAILING_TP 는 계속 100% 승률인데 거의 안 걸림
5. LONG/SHORT 슬롯 불균형 — 상한 40 으로 늘렸으나(선택 B) 진입의 69%가 LONG 이라
   불균형 자체는 미해결. A(SHORT 전용 슬롯) 재검토 대상

## 📉 Fix 254 — 사상 ③ 이 드디어 코드가 됐다 (2026-09-01, 기본 OFF)

사장님 사상 ③ **"급락한것은 이전급등에 대한 급락이라 확실한 숏"** /
**"볼밴 지지선 붕괴와 지속하락을 찾아서 분할 포지션 진입"** 이
코드에 없었고, 오히려 정반대로 돌고 있었다:

    unified_15m_entry._detect_15m_surge:  side = "SHORT" if c1h > 0 else "LONG"
    => BTR -44% 붕괴가 「급락」으로 분류돼 **LONG 진입**

`app/services/support_breakdown.py` — **5개 전부** 충족해야 방향을 뒤집는다:

    ① 선행 급등 +50%↑  ② 볼밴 위치 < 0.45  ③ 종가 < 직전 8봉 최저
    ④ 거래량 평균 x1.5  ⑤ OBV < 0  (사상 ④ — 안 꺾인 이탈은 **가짜**)

🚨 다수결 금지 — 방향을 뒤집는 판정이다 (Fix 250 의 교훈).

### 설계 중 잡은 내 오류 (기록해 둘 것)

③ 을 처음엔 「중단선 위였다가 깨졌다」로 짰는데 **BTR 에서 영원히 거짓**이었다 —
16배 급등 뒤에는 20봉 볼밴이 너무 넓어져 **횡보 구간이 계속 중단선 아래**에 머문다.
=> 「지지선 붕괴」의 문자 그대로 **직전 8봉 최저가 이탈**로 판정.
**폭등 종목에서는 볼밴 상대 위치가 직관과 다르게 움직인다** — 다음에도 주의.

설정: `support_breakdown_short_enabled` (기본 0). 사장님이 미리보기로 두셨다.

## 🔋 2026-09-01 사장님 차트 3장이 만든 새 규칙 (Fix 257~259)

사장님이 XPLUSDT / AIOUSDT / 0GUSDT 를 연달아 보여주시며 같은 말씀:

  "obv가 **지속적으로 하락**한 차트라 (롱이 아니라) 숏이야"
  "급등후 급락했을때 obv 같이 급락하면 다시 지지반등 이상을 하려면
   **obv가 강력하게 상승해야해**. 이건 **작은 반등후 하락**하는거야.
   **세력이 모두 떠난후 다시 상승하려면 세력이 다시 들어와야**"
  "저항과 동시에 obv가 같이 급락해서 **세력이 바로 익절**했는데"

### 진입 시점 캔들 복원으로 임계를 얻었다

    전략              결과        1H 회복률   4H 회복률   4H obv_dir
    #1890 SNXXUSDT   **+22.71**   **0.637**    0.187      +0.3811
    #1909 AIOUSDT     실패          0.060      0.266      +0.3088
    #1884 XPLUSDT     실패          0.106      0.069      -0.1274

🚨 **1시간봉이 판별자**다 — 4H 회복률은 승/패가 **뒤집혀** 있다.
🚨 「지금 OBV 가 오르는가」(obv_dir)와 「떨어진 만큼 돌아왔는가」(회복률)는
   **다른 질문**이다. Fix 257(4H obv_dir <= -0.10)은 AIO(+0.3088)를 못 잡는다.

### 넣은 것

- **Fix 257** LONG OBV 게이트 0.6 -> **-0.10** (Fix 245 SHORT 0.35 와 대칭).
  🚨 `direction` 문자열(±0.5 밴드) 대신 **ratio 를 직접** 본다 — 밴드가 거칠어
  「명확한 하락」을 flat 으로 놓쳤다.
- **Fix 259** `obv_recovery.py` — 회복률 = (현재-저점)/(고점-저점), 임계 **0.30**.
  급락 LONG 경로에서 Fix 256 이 받아온 **1H 분석 재사용** = 추가 API 0.
  OBV 가 떨어진 적 없거나 낙폭 미미하면 **대상 아님**(사장님: "obv 같이 급락하면").

### 배포 직후 실측 — 차단률 14/15 (93%)

차단된 20종목의 회복률 분포: **0.031 ~ 0.266** (최대 0.266).
승자 프로파일 0.637 과 **완전히 다른 영역**이다.
=> 임계가 빡빡한 게 아니라 **지금 시장에 「세력이 다시 들어온」 급락 종목이 없다**.
   임계를 0.20 으로 낮춰도 대부분 막힌다.

⚠️ 표본 3건에서 나온 임계다. 승자가 쌓이면 다시 잡을 것.
⚠️ 이 상태가 며칠 지속되면 급락 LONG 경로가 사실상 닫힌다.
   다만 그 경로는 실측 **승률 0% (30건 이상)** 이라 보호 방향이다.

## 🚨 관측 실수를 **세 번** 했다 (Fix 255 / 258)

「적중했을 때만 로그」를 남겨 **0 건이 「안 도는 것」인지 「조건 미달」인지** 구별이 안 됐다.
- Fix 254 -> Fix 255 로 카운터 추가 (unified_15m)
- 같은 실수를 auto_long_bottom 에 반복 -> Fix 258
- Fix 259 는 처음부터 카운터를 넣었다

**교훈: 새 게이트를 만들 때 「평가 수 / 적중 수 / 오류 수」를 사이클 로그에 함께 넣는다.**

## 🚨 꺼져 있는 워커에 기능을 넣었다 (Fix 256)

`unified_entry_enabled = 0` — unified_15m_entry 는 **10분간 0사이클**.
같은 10분에 auto_long_bottom 은 17사이클. Fix 254 를 죽은 워커에 넣었던 것.
**교훈: 배선 전에 그 워커가 실제로 도는지 로그로 확인한다.**

## 🖥️ VPS 직접 접속 (2026-09-01부터)

사장님 요청으로 내가 SSH 로 직접 배포·조회한다:
`ssh -o BatchMode=yes root@159.65.137.250`
(키 = 사장님 로컬 `.ssh/id_ed25519`, 메모리 [[reference_vps]] 참조).
사장님이 복붙할 필요 없다. 자금이 오가는 설정 변경은 여전히 사장님 승인 후.
