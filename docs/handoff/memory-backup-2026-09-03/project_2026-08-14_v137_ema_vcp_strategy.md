---
name: project-2026-08-14-v137-ema-vcp-strategy
description: v137 EMA/VCP + v138 SAR/일목 = 외부 매매법 2종 기계화 + 합의(confluence) 판정 + 학습 대조 검증 (미커밋/미배포)
metadata: 
  node_type: memory
  type: project
  originSessionId: c83fb51f-c7ba-48ff-9868-b11fa5654bff
  modified: 2026-08-14T00:22:00.038Z
---

**2026-08-14**: 사장님이 유튜브 「쿨라매기 이동평균선 매매전략」(차트광) Gemini 정리본을 주며
「학습한 자료와 같이 활용할 수 있게」 요청 → **v137 EMA/VCP MTF 분석 + 학습 대조** 구현.

## 사장님 사상 (헌법 v137, 이번에 새로 확립)
「**남의 매매법도 우리 데이터로 검증해서 쓴다**」
= 진입 시 셋업 등급(A/B/C/D) 박제 → 종료 후 실제 승률과 대조 → **그 다음에** 자동화 판단.
**의도적으로 자동 주문 안 붙임** (헌법 4번 검증 없는 코드 금지). 2단계(표본 검증) 건너뛰고
4단계(trigger_mode 자동 진입) 가지 않기로 명시 합의.

## v138 확장 (같은 날, 두 번째 매매법 = 파라볼릭 SAR + 일목 구름대)
사장님이 `parabolic_sar_strategy.md`를 주며 「이것도 **같이** 적용」 요청.
**핵심 = 나란히 띄우기가 아니라 서로 검증시키기**:
- 📐 EMA/VCP = **돌파형** (터지는 첫 순간) / ☁️ SAR·구름대 = **추세추종형** (이미 난 추세)
- 성격이 반대라 합의가 의미 있음 → `strategy_confluence.evaluate()`
- **한쪽만 D = CONFLICT = 점수 절반** (한쪽 근거만 보고 진입하는 것 차단!)
- **구름 내부(INSIDE)도 D** = 횡보장 진입 = 이 매매법 최대 손실 원인
- **SAR·구름 = 완료봉만** (진행 중 봉 SAR는 찍혔다 사라짐 = 리페인팅 silent bug!)
  → 테스트 `test_구름은_완료봉_기준_리페인팅_방지`가 강제

## v139 소급 백테스트 = 신 로직을 과거 데이터로 검증 (같은 날!)
프로덕션 DB(**Neon 클라우드**, VPS 로컬 postgres 아님!) 읽기 전용 조회 +
Binance 공개 캔들로 **진입 시점 재현**(lookahead 없음). 스크립트 = `backend/scripts/backtest_setup_grades.py`.
VPS 접근: `ssh root@159.65.137.250` → `docker exec binance-auto-trader-api python -c "..."`.

### 🚨 분석 중 발견한 치명적 버그 3개 (전부 수정함)
1. **`TradeLearningService`가 없는 속성 5개 참조** (`first_entry_at`/`planned_capital`/
   `unrealized_pnl_pct`/`close_reason`/`current_price`) → v134 이후 **학습 저장 0건**!
   (record 37건 전부 OPEN, entry_context/progression 전부 비어있음)
   → **이게 안 고쳐졌으면 v137/v138 entry_context도 배포 즉시 무용지물이었음!**
2. `learning_sync_worker`가 실패를 성공으로 카운트 (`closed=27` 로그인데 실제 0건)
3. `prediction_outcome_worker`가 1h/4h/24h를 전부 「현재가」로 채움 → **60% 오염**

### 📊 핵심 실측 (실매매 309건 5.5주 + 추천 761건 2일)
- 실매매 총 **-22,757 USDT / 승률 35.3%**. SHORT가 손실 3.3배.
- **물타기 깊이가 진짜 원인**: 1단계 평균 -5.6 / 4단계 -105 / **9단계 이상 8건이 -9,762 = 전체 손실의 43%**
- 진입의 **74%가 신 로직 D등급**, 그 D구간이 **손실의 87%**
- 필터 시뮬: -22,068 → **-2,897** (D 차단 시). 단 **승률은 오히려 하락** (대형 손실만 걸러냄)
- 추천: 합의 **AGREE 57.5% vs CONFLICT 16.4%** (41%p!) → 충돌이 AVOID보다도 나쁨
- **SAR A등급이 B등급보다 나쁨** (22% vs 43%) = 「전환 첫 점」이 과열 정점일 수 있음
- **양쪽 일관**: 4H/1H 필터 전부 ⭐ / **15m 트리거는 전부 상반** ⚠️
  → 헌법 v139: 「상위 TF 필터는 믿고, 하위 TF 트리거는 의심하라」

### 반영: 점수 재배분(1H 구름 25→30, 첫반등 10→15, 15m돌파 10→3), CONFLICT ×0.3 + `blocked` 플래그
### 미반영(과최적화 방지): 거래량폭발 감점, SAR A→B 강등, LONG 억제
### ⏳ 사장님 결정 대기: **물타기 최대 단계 제한** (1순위!) / D등급 자동 차단 여부

## v140 = 사장님 주력 전환: 15m 천장 예측 + 볼밴 중단 (같은 날!)
사장님: 「15분봉 최상단과 볼밴 중단 기준 / 1시간 4시간은 **보조** / macd obv rsi vol 같이 쓰면 정확도 최고」
→ **478,435개 15m 캔들 실증 연구** (`scripts/study_15m_top_bbmid.py`)

### 🎯 사장님 가설 = 맞았지만 「쓰는 법」이 10배를 갈랐음
- **단순 임계값 조합** (RSI≥70+MACD꺾임+OBV하락+Vol감소) 2개+ = 2.49% (**0.77배 = base 이하!**)
- **다이버전스 조합** (가격 신고가인데 지표 못 따라옴) 2개+ = 28.1% (**8.7배**)
- 단독: RSI div 29.7%(9.2배) / OBV div 28.5% / MACD div 25.5% / BB상단**고가**터치 13.8%
- **역효과 신호**: OBV 5봉하락 0.39배, Volume 정점후감소 0.63배
- 🚨 **RSI≥70 을 추가하면 32%→22% 하락!** %B≥1.0(종가돌파)도 하락 → **점수에서 제외**
  (고가 터치가 맞고 종가 돌파는 이미 늦음)
- 최적: 다이버전스 3개 + BB터치 = **38.5% (11.9배)**
- **1H/4H 보조 효과 거의 없음** (32.2%→33~34%) = 사장님 「보조 역할」 지시가 데이터적으로 정확!

### 🎯 볼밴 중단 = 목표지 지지선 아님 (v131 가정과 반대!)
- 천장 후 **99.7%가 8시간 내 BB중단 도달** (중앙값 2봉=30분, -1.84%) → **TP1 = BB중단**
- 도달 후: **이탈 47.2% vs 반등 18.3%** = 2.6배! → 전량청산 X, 절반만
- 상위추세별 반등률 16~20% = 1H/4H로 반등 예측 불가

### ✅ 구현 검증 (연구≠코드 방지, `scripts/validate_bb_top_analyzer.py`)
표본 24,980 → S 37.2%(11.3배) / A 34.2%(10.4배) / D 2.2%(기저 이하) = 연구값 재현!

### 파일: `services/bb_top_analyzer.py`, 테스트 23개, `docs/BB_TOP_15M_STRATEGY_SPEC.md`
### ⚠️ 천장(SHORT)만 검증됨. 바닥(LONG)은 대칭 구현했으나 **미검증**
### 부수 fix: 세 분석기 모두 `klines if klines else fetch` → `is not None` (빈 리스트가 네트워크 호출 유발하던 결함)

## v141 = 5m/15m 급등락 실시간 진입 (같은 날!)
사장님: 「5분봉과 15분봉에서 20% 정도의 급등락을 급등락 실시간 진입 전략으로」
→ 5m 캔들 신규 수집(181심볼×7,200봉) + `scripts/study_pump_dump_20pct.py`
→ **핵심 지표 = TP/SL 「선착 판정」** (MFE/MAE는 순서를 몰라서 손익 판단 불가!)

### 실측 결론
- **양의 기대값 54셀 중 37개(69%)가 「급등 → 추격 LONG」** = 방향 확정
- **급락은 역추세 6 vs 추격 5 = 동전던지기 → 진입 비권장(D등급, side=None)**
- **작은 TP는 독!** +3%/-3%는 대부분 마이너스, **+10%/-5% 비대칭만 통함**
  (승률은 떨어지는데 기대값은 오름 = 변동성 때문에 TP 전에 SL 먼저 맞음)
- 대표: 15m 20% 1시간 급등 → 추격 LONG +10/-5 = TP선착 38.1%, **EV +0.76%** (218건)
- **20%보다 15%가 표본 3~4배 + 안정적** (15m 4시간: 782건 EV +0.44% vs 483건 +0.23%)
- ⚠️ **EV는 수수료(왕복 0.08%) 차감 전! 슬리피지 넣으면 대부분 0 근처** — 반드시 함께 표시
- ⚠️ 승률 30~38% = 연속 손실 김 (10연패 정상 범위)

### 파일: `services/pump_dump_live_analyzer.py`, 테스트 28개, `docs/PUMP_DUMP_LIVE_STRATEGY_SPEC.md`
### 기존 v133c(5분 3% 무조건 추격)와 다름: 급락 추격을 **중단** 권고

## v141a = 15m은 「20% 전후」 밴드만 (사장님 추가 지시)
「임계값 이상(≥)」이 아니라 **[하한,상한) 밴드**로 다시 측정 (`scripts/study_15m_pump_bands.py`)
→ **같은 20%라도 「얼마 만에 갔느냐」가 갈랐음!**
- 15m 17.5~22.5% **1시간 창** → LONG +5/-5, EV **+0.18%** (255건) ✅
- 15m 17.5~22.5% **2시간 창** → EV **정확히 0.00%** (416건, 표본 최다!) → **신호 없음 처리**
- 15m 17.5~22.5% **4시간 창** → LONG +5/-5, EV **+0.23%** (546건) ✅
- 15m 급락 20% 전후 = 전 창 마이너스 → 진입 비권장
- **5m은 밴드 제한 없음** (기존 10%+ 유지)
- ⏳ **사장님 결정 대기**: 22.5~27.5% 1시간 창이 EV **+1.59%**(114건)로 가장 좋음 →
  상한을 27.5%로 넓힐지 여쭤봄. 지금은 지시대로 22.5%로 제한.
- 밴드 밖이면 **왜 신호가 없는지 화면에 명시**

## v142 = 급등→고점→하락→재상승 V자 패턴 연구 (사장님: 「가끔이지만 최고일것 같아」)
`scripts/study_15m_reversal_pattern.py` — **패턴은 실재하나 매매 규칙은 실패**
- 사후 관찰: 되돌림 **3~5% → 고점 회복률 80%** / 5~8% → 58.7% / 12~20% → 24.9% / 20%+ → 12%
- **사장님 「가끔」 직감 정확**: 얕은 되돌림(≤8%)은 전체의 **15.2%**, **12%+ 되돌림이 65%**
- 급락 쪽은 훨씬 약함 (저점 회복률 최대 43.8% vs 급등 80%)
- **진입 규칙 3종 전부 마이너스**: v1 첫양봉 -0.17%, v2 직전봉돌파+TP=고점 -0.91%, v3 SL고정 -0.49%
  - v2 실패 원인 = **평균 TP < 평균 SL 손익비 역전** (반전 확인하면 이미 고점 근접)
  - v3 실패 원인 = 손익비 2~3:1로 올리니 **TP율 11~25%로 폭락**
- → **진입 전략으로 안 넣음**. 대신 `retrace_state()` = **보유 판단 도구**로 전환
  (되돌림 %→실측 회복률 표시, 「진입 신호 아님」 명시)
- 부수 소득: 급등의 65%가 12%+ 되돌림 = **v141 승률 30~38%가 설명됨**
- 미시도: v140 바닥 다이버전스/거래량 감소로 「얕게 끝날 되돌림」 사전 식별
- `docs/REVERSAL_PATTERN_FINDINGS_2026-08-14.md` / 헌법 v142

## 🌟 v143 = 4H 볼밴 중단 이탈 → 반대 밴드 (**이번 세션 최고 성과!**)
사장님: 「4시간봉 볼밴 중단 깨지면 하단까지, 하단 깨는 건 아주 적다」
→ 4h **250일치 신규 수집** (`scripts/fetch_klines_cache.py`) = **215,561 캔들** = 표본 최대!

### ✅ 가설1 맞음 / ❌ 가설2 틀림
- 중단 하향 이탈 13,053건 → **하단 도달 82.8%** (중앙 5봉=20시간, 거리 2.72%)
- 상향 돌파 13,045건 → **상단 도달 86.6%** (대칭 성립!)
- ❌ **하단 도달 후 68.3%가 하단 뚫고 마감** (추가 하락 중앙 5.69%) = **밴드는 지지선 아님!**
  (v140 15m BB중단 결론과 같은 방향)

### 💰 규칙 A(추세 방향) >> 규칙 B(역방향)
- A: 중단 이탈 진입 → TP=반대밴드, **SL -5% → TP선착 64.0%, EV +0.42%** (하향) / +0.44% (상향)
- B: 밴드 도달 후 역진입 → EV +0.21%/+0.10% = **A의 절반 이하**
- ⚠️ B 측정 시 **진입 봉 자체 SL 히트 포함** 보정 (안 하면 +0.65%로 부풀려짐!)

### 왜 최고인가: 표본 10,806건(다른 전략의 20~50배) + **4H라 지정가 진입 가능 = 슬리피지 작음**
### 한계: 승률 64%지만 손익비 0.66 / 도달까지 20시간(펀딩비) / 수수료 후 +0.34%
### 파일: `services/bb_4h_band_analyzer.py`, 테스트 20개, `docs/BB_4H_BAND_STRATEGY_SPEC.md`
### **자동화 1순위 후보** (표본·기대값·슬리피지 모두 유리)

## ✅ v141b/v143a = 사장님 최종 결정 반영
1. **급등락 실시간 진입 = 15m 20% 전후(17.5~22.5%)만** → `ENABLE_5M_SIGNAL=False`
   (5m은 화면 참고 측정만, 진입 근거로 안 씀. 신호 경로 하나로 통일)
2. **자동 전략 제안 = 4H 볼밴 기준으로 전환** (`agents/strategy_suggestion_team/bb_4h_scanner.py`)
   - team_lead.run_daily_prediction 의 소스를 PumpDumpPredictor → **BB4HScanner** 로 교체
     (기존 24h 순위 제안은 v139 백테스트에서 성공률 40.5%뿐 + 근거 미실측)
   - **4트리거 롱숏 대칭**: 중단하향→SHORT(EV+0.42, conf .78) / 중단상향→LONG(+0.44, .80)
     / 하단이탈→SHORT(+0.14, .62) / 상단돌파→LONG(+0.27, .68)
   - **신규 측정**: 하단 깬 뒤 SHORT = 5,286건, +5/-5 EV **+0.14%** (추가하락 중앙 8.13%)
     상단 깬 뒤 LONG = 5,802건, +8/-5 EV **+0.27%** (추가상승 중앙 10.23%)
   - 밴드 이탈은 중단 이탈보다 EV 2~3배 낮음 → confidence 낮추고 제안 문구에 경고 명시
   - 우선순위: 밴드 밖이면 밴드이탈 / 중단이탈은 2봉(8시간) 이내만 신선
   - 스캔 = 24h 거래대금 상위 100 심볼, 매일 06:30 UTC

## 🔄 v144 = 사장님 「20%+ 급등락은 반대매매 유리」 재검증
사장님 지적: "주식과 반대로 생각해줘, 알트 시장이야, 20% 이상은 반대매매가 유리해"

### ✅ 사장님 지적이 맞았던 부분
- 제 v141 결론(급등=추격 69%)은 **셀 개수로 세어 10~15% 구간이 지배**한 것!
- **20% 이상만** 떼어내니 반대매매가 이기는 칸이 여럿 (2h창 20~25% +0.38 / 25~30% +0.48
  / 4h창 40~60% **+1.04** / 급락 1h창 20~25% +0.81)
- 강도가 커질수록 반대매매 우세 구간 증가

### ❌ 그러나 전체 합산은 여전히 6개 중 5개 추격 우세 (평균회귀형 격자 추가해도 동일)

### 🚨 내가 낸 오류 — 반드시 기억!
「고점 대비 3% 되돌림 후 반대매매」로 재니 **EV +2.4%** 나왔는데 = **미래 참조(lookahead)**!
고점을 20% 돌파 후 8봉 **전체**에서 찾았음 → 실시간엔 불가.
**running extreme** 으로 고치니 **+2.4% → +0.04~0.66%** 로 급락.
→ 헌법 v144: **「너무 좋은 결과 = 먼저 미래 참조를 의심하라」**

### 📌 유일하게 의미 있는 구간 (미래참조 제거 후)
**급등이 「2시간 창」으로 20%+ 오른 뒤, 고점 대비 3% 되돌린 시점 SHORT**
= EV **+0.66%(4h 판정) / +0.86%(12h 판정)**, 표본 373건. 1h·4h 창은 성립 안 함.
→ **반대매매의 핵심은 방향이 아니라 타이밍** (도중 진입 = 칼날 잡기)

### ⚠️ 15m 캐시가 25일치뿐 = 다중검정 우려. **15m 6개월+ 재수집 후 재검증 필요**
### `docs/COUNTER_TRADE_VERIFICATION_2026-08-14.md`

## 📊 v145 = 사장님 「분할 진입 역추세」 전략 검증 (= 우리 단계별 진입 구조!)
사장님: "20%+ 급등락하면 빠르게 분할 매수해서 평단 확보 → 추가 진입 → 안정적 수익"

### 사장님 체감이 **맞습니다** — 시뮬 익절률 **99~100%** (대부분 1봉 내 익절)
20% 움직인 직후 15m 봉은 변동폭이 커서 평단 대비 1~3%는 거의 바로 닿음.

### 🚨 그런데 그게 위험 신호 — **손익분기 실패율**로 봐야 함
`손익분기 실패율 = TP / (TP + |실패손실|)`
- 5%×5단계 TP+2%: 익절률 99.4%, **실패시 -67.2%**, 손익분기 **2.9%**, **안전여유 +2.3%p** 🚨
- 5%×3단계 TP+2%: 손익분기 2.6%, 안전여유 **+2.0%p** 🚨
- **실패율이 0.6%→2.9%로만 올라도(5배) 기대값 0!**
- 실패 사례가 360건 중 **2~4건뿐** = 꼬리 위험 추정 불가

### 🔥 이건 가정이 아님 — **v139 실거래에서 이미 발생**
9단계+ 8건(2.6%)이 **-9,762 USDT = 전체 손실의 43%**. 시뮬의 2~4건이 실계좌에선 이렇게 나타남.
→ **전략이 틀린 게 아니라 실패 케이스를 제어 못 한 것**

### 안전 조건 4개 (사장님께 제시)
①**최대 5단계 하드 제한** ②단계별 자본 늘리지 말 것(마틴게일 금지)
③평단 기준 **절대 손절선** ④**2시간 창**만 (v144: 1h·4h는 우위 없음)

### `docs/SCALED_COUNTER_STRATEGY_2026-08-14.md` / 헌법 v145
### ⚠️ 15m 25일치로는 꼬리 추정 불가 → **6개월+ 재수집 필요**

## 🔀 v146 = 사장님 진짜 전략 = 20% 급등 후 「지속 vs 전환」 롱/숏 동시 판별
사장님: "20% 급등 후 100% 가는 심볼 있어. 1분 5분 차트로 1차 진입 결정. 지속 상승은 진입해서
최대 3단계 안에 고점 잡기. **롱숏 동시** — 급등 후 하락하면 숏, 지속 상승은 **롱 짧게**"

### 실측 (5m 25일, 20%+ 급등 **282건**)
- **기저 = 거의 동전던지기**: 지속 41.1% / 전환 48.9% (오히려 전환 우세!)
- **「20%→100%」는 282건 중 1건 = 0.4%** (+30% 13.5% / +50% 5.0%)
  → 사장님 기억은 실재하나 극단적 예외. 최대상승 중앙 +10.2% / 최대하락 중앙 -13.0%
- **단독 지표 전부 무력** (최대 1.18배): 거래량/가속/연속봉/직진성/속도 모두
- **조합해야 갈라짐**:
  · 🚀 지속(LONG 짧게) = **거래량 4배+ AND 종가 상단 70%+** → n=57, 지속 52.6% / 전환 35.1%
  · 📉 전환(SHORT) = **가속<0 AND 거래량<1배** → n=41, 지속 31.7% / 전환 48.8%
  · 👀 중립 = 진입 안 함 (동전던지기 구간 회피가 로직의 절반!)
- 사장님 지시 반영: **LONG TP +5%(짧게) / SHORT TP +10% / 최대 3단계 하드**

### ⚠️ 표본 41~57건뿐 = 확정 우위 아님. **1분봉 미반영**(5분만 씀).
### **5m/1m 6개월치 재수집 후 재검증이 최우선**
### `services/pump_continuation_analyzer.py`, 테스트 17개, `docs/PUMP_CONTINUATION_SPEC.md`

## 🔬 v146b = 1분봉 미시구조 = **개선 없음 (반영 안 함)**
사장님이 「1분 5분 차트」라 하셔서 1m 14일치 수집(181심볼×20,160봉) 후 검증.
- 1m 단독: up_ratio 65%+ 1.24배 / concentration 25~40% 1.28배 (나머지 ~1.0배)
- **v146 조합에 1m 얹으면 오히려 하락**: 48.6% → 43.8%(양봉비율) / 47.4%(초단기가속)
- 표본이 16~19건으로 쪼개져 무의미
→ **헌법 v139(과최적화 금지) 따라 1분봉 미반영.** 5분봉 조합 유지.
   (`scripts/study_1m_pump_micro.py` 는 남겨둠 — 데이터 늘면 재검증 가능)

## 🎛 v147 = 사장님 UI/설정 요청 2건
1. **PnL/ROI 액션 기본 -5%** — 기존엔 분석판단(-20/-50)과 TP/SL어드바이저(-20)에 흩어져 있었음
   → `app/core/risk_constants.py` 에 **단일 상수** 신설 (헌법 6번):
     `ACTION_PNL_PCT_DEFAULT=-5.0` / `ACTION_PNL_REVIEW_PCT=-20` / `ACTION_PNL_URGENT_PCT=-50`
   → analysis.py `_judge_position` 에 **ACTION_CHECK(-5%)** 단계 신설 + advisor 도 -5% 사용
2. **15분 급등/급락 순위 팝업** (바이낸스는 24h만 제공!)
   - `symbols.py` 랭킹에 **단기 기간 4개 추가**: 15m/30m/1h/4h
     (1m·5m 캔들로 직접 계산 / TTL 30~120초 / 단기는 거래대금 pool 200→**300**)
     ⚠️ 주의: 이 API 에서 **"1m" 은 1개월**! 그래서 분 단위는 15m/30m 만 씀
   - 캐시 키 v129 → **v147** (신 기간 반영)
   - `app/static/pump-ranking.html` 신규 = `?direction=gainers|losers` 로 **급등창/하락창 각각**
     (창 이름이 달라 동시에 열림, 20%+ 는 빨간 배경 강조, 심볼 클릭 = 분석창)
   - `index.html` 급등락 카드에 **🚀 15분 급등 / 📉 15분 급락** 버튼 2개
   - `test_symbol_ranking_route_order.py` period 목록 갱신

## ⚙️ v147 전략 인스턴스 목록 기본값 2개 변경 (사장님 화면 지목!)
사장님이 스크린샷으로 「PNL / ROI 액션」 칼럼을 직접 지목 → 처음엔 내가 분석창 쪽으로 오해했음.
**실제 대상 = 전략 인스턴스 목록의 드롭다운 2개**:

1. **트레일링 회귀 기본 10% → 5%** (「PNL / ROI 액션」 칼럼 드롭다운)
   - `risk_constants.TRAILING_RETRACE_PCT` 10 → **5** + `strategies-list.js` fallback 10 → 5
   - 이력: 원래 5 → v36(6/10) 사장님이 10으로 → **v147 다시 5**
   - 💡 `test_risk_constants_centralization.py` 가 이미 5를 기대하고 있었음 (v36때 테스트 미갱신)
2. **TP1 기본 25% → 15%** ("tp1 단계 시작도 15%로")
   - **실제 동작 기본값은 `strategy_service.py` 신규 전략 생성 시 `tp1_pct_override=D("25")`** 였음
     (JS의 25는 표시용 fallback일 뿐! 두 곳이 흩어져 있었음)
   - `risk_constants.TP1_PCT_DEFAULT = 15` 신설 → service + JS 양쪽 통일 (헌법 6번)
   - ⚠️ **기존 전략은 저장된 25 유지** = 신규부터 15% 적용

3. **DEFAULT_SL_PCT_OF_CAPITAL 90% → 50%** (사장님 지시)
   - 이력: 100 → **90** (v5, 2026-06-19 SYNUSDT 청산 사고 -585 USDT: 청산이 SL보다 먼저 발동)
     → **50** (v147) = 청산 회피를 넘어 **손실 자체를 절반에서 끊는** 방향
   - v139 근거와 일치: 깊은 물타기 8건이 전체 손실 43% → 일찍 끊었으면 크게 줄었을 구간
   - `risk_service.py` 의 잘못된 주석("default 80%" ← 실제 90) 도 정정

### ⚠️ 3개 합쳐진 효과: TP1 25→15% + 트레일링 10→5% + SL 90→50%
   = **익절도 손절도 모두 짧아짐** (작게 자주 / 크게 안 물림). 사장님 확인 필요!

### ✅ stale 테스트 2건 해소 → 실패 23 → **22건**
   - `TRAILING_RETRACE_PCT`: 테스트가 5 기대 → 코드 5로 되돌려 일치
   - `TRAILING_MIN_STAGE`: 테스트 3 기대 vs 코드 1 → **코드가 맞음**(v8 BEATUSDT 완화) → 테스트 정정

## 🚨 v147a = 사장님이 화면 보고 잡아낸 것 (중요 교훈!)
사장님: "여기 내용을 변경해야 하는거 아닌가" (대시보드 스크린샷)
= **분석기/서비스만 바꾸고 대시보드 카드의 실제 경로는 안 바꿨음!**
  화면 증거: AKEUSDT → 1시간 **-7.01%** 인데 5분 +3.45% 라고 **LONG 추천**

원인: `/live-pump-dump/scan` 이 v133c 옛 로직(5분 ±1.5% / 1시간 ±3% 무조건 추격).
fix:
 · `live_pump_dump.py` → **PumpDumpLiveAnalyzer 단일 경로**로 재작성
   (15m 20% 전후 급등만 LONG / 급락은 side=None + 사유 / TP·SL·기대값·표본 함께 반환)
 · 🚨 **프런트 CRITICAL**: `a.side || 'LONG'` 이 **null 을 LONG 으로 둔갑** →
   진입 비권장 급락에도 「▶ 즉시 진입」 버튼이 떴음! → canEnter 판정 + 버튼 미렌더
 · 진입 config 하드코딩도 v147 기본과 일치 (tp1 25→15 / SL 90→50)
 · 카드 설명문/타입 라벨 + 제안 라벨 bb4h_* 4종 추가

### 🔑 교훈: **로직을 바꾸면 그 로직을 쓰는 UI 경로를 전부 추적해야 한다.**
   서비스/분석기만 갈아끼우면 화면은 옛날 것을 계속 보여준다 (= 사장님이 옛 신호로 매매!)

## 🔍 v147b = 나머지 대시보드 카드 전수 점검 (사장님 "진행해줘")
- 🚨 **silent bug**: 「심볼 분석기」 카드가 손익을 **항상 +0.0%** 로 표시하고 있었음
  `symbol-analyzer.js` 가 `s.unrealized_pnl_pct` 를 읽는데 **`/strategies` 응답에 그 필드가 없음**
  (`unrealized_pnl`(USDT)만 존재) → `Number(undefined||0)` = 0
  → fix: Binance 포지션 캐시(`window._binancePositionsCache[acct].positions[sym].roi_pct`) 사용,
    없으면 **숫자 지어내지 말고 '-'**
- ⚠️ **실측 충돌**: 「급등+BB중단 알람」(v131) 전제 "BB중단=지지선=매수기회" 가 데이터와 반대
  (4H 도달 후 68.3% 관통 / 15m 이탈 47.2% vs 반등 18.3%)
  → 삭제하지 않고 알람 payload 에 `caution` 동봉 + 워커 docstring/카드 설명문에 경고 노출
    = **관찰용 신호**임을 명시 (사장님 요청 기능이라 기능 자체는 유지)
- 🧹 `team_lead` docstring 정정 (v143a 로 BB4HScanner 로 바뀌었는데 옛 흐름 설명 남아 있었음)
- ✅ 이상 없음: auto_manual_executor(타입 비의존) / tp-sl-advisor / reentry-alerts

## 🌟 v147d = 15m 밴드 상한 22.5% → 27.5% (사장님 "27.5로 해줘")
**핵심 교훈: 밴드를 넓히라는 지시를 「상한 숫자만 바꾸기」로 구현하면 오히려 나빠졌다.**
재측정(`study_15m_pump_bands.py --bands 17.5:22.5,22.5:27.5,17.5:27.5`) 결과:
- 통합 17.5~27.5% → 1시간 +0.23% / **2시간 −0.07%(음수!)** / 4시간 +0.16%
- 분리 유지 시 → 17.5~22.5%: 1시간 +0.18%·4시간 +0.23% / **22.5~27.5%: 1시간 +1.59%**
- 이유 = **하위 밴드마다 최적 TP/SL 이 다름** (20% 전후는 +5%, 22.5%↑는 +15%).
  하나로 묶으면 절충값(+10%)이 되어 양쪽 다 안 맞음.
→ 구현: `BAND_15M=(17.5,27.5)` 로 넓히되 `PLAYBOOK_15M_BAND` 를
  **`{(lo,hi): {창: play}}` 중첩 구조**로 바꿔 하위 밴드별 플레이북 유지.
  헬퍼 `_band_15m_play(abs_chg, window)` 신설.

**과최적화 검증 (중요)**: 밴드별 「최적 1개」만 보면 9 TP/SL × 2방향 = 18개 중 최댓값을
고른 셈이라 부풀려짐. 전 조합을 펼쳐 확인 →
- 22.5~27.5% **1시간**: LONG 6/9 양수, SHORT 0/9, TP 키울수록 좋아지다 +20%에서 꺾임
  = **구조가 있는 결과** → 채택 (우측 꼬리형)
- 22.5~27.5% **2시간**: 최적만 보면 SHORT +0.14% 로 보였으나 전 조합은 LONG 1/9·SHORT 2/9
  = **노이즈** → 신호 없음. 4시간은 양방향 0/9 → 신호 없음.
- ⚠️ 채택한 1시간 play 는 **TP선착 36% = 64%가 지는 구조**. UI에 「자주 지고 가끔 크게
  이긴다 + 연패 견딜 자본에서만」 경고 강제 노출. 표본 114건/25일치 = 꼬리 추정 불안.

## 상태 (⚠️ 다음 세션 확인 필수)
- ✅ **커밋 + 푸시 완료** (2026-08-14): 브랜치 `feat/v137-v147-strategy-analyzers-learning-fix`
  commit `7b1cce9` (56 파일, +12,107 / -130). main = `2a17a26` 그대로.
  **PR 생성 + 머지 필요** (사장님 GitHub 웹 UI):
  https://github.com/herosys1-crypto/binance-auto-trader/pull/new/feat/v137-v147-strategy-analyzers-learning-fix
- ⚠️ **배포 시 `api` + `scheduler` 둘 다 restart** (학습 fix 가 scheduler, 분석/랭킹이 api)
  `cd ~/binance-auto-trader/backend && git pull && docker compose restart api scheduler`
- 신규 7개: `services/ema_vcp_analyzer.py`, `services/sar_ichimoku_analyzer.py`,
  `services/strategy_confluence.py`, 테스트 2개 (23 + 37 = **60개 통과**),
  `docs/EMA_VCP_MTF_STRATEGY_SPEC.md`, `docs/SAR_ICHIMOKU_MTF_STRATEGY_SPEC.md`
- 수정 4개: `api/v1/analysis.py` (캔들 1회 조회 공유 + 두 분석기 + 합의 + 점수 0~100 고정),
  `static/analysis.html` (합의 카드 + SAR 3단 카드 + fmtPrice),
  `workers/learning_sync_worker.py` (entry_context 3종), `api/v1/trade_learning.py` (`GET /setup-stats`)
- 배포 시 **api + scheduler 둘 다 restart** (analysis API = api / learning_sync = scheduler).

## 다음 단계
1. 표본 20건+ 쌓일 때까지 대기 → `GET /api/v1/trade-learning/setup-stats?days=90`의 `verdicts`
   **핵심 지표 = 「🤝 합의 평균 vs 충돌 평균」** (= 같이 보는 게 이득이었나?)
2. 합의 유효 확인되면 → STRONG_AGREE 텔레그램 알람 → 그 후에야 자동 진입 trigger_mode 검토

## 기존 테스트 실패 23건 (내 변경과 무관, git stash로 검증함)
`test_static_assets_integrity`(TERMINAL_STATUSES drift), `test_tp10_stages`,
`test_stream_service_dynamic_stages`, `test_v7_short_exit_partial_stage`,
`test_risk_constants_centralization`, `test_strategy_status_constants` — **별도 정리 필요**.

관련: [[project-2026-08-06-v130-obv-reentry-complete]] (같은 「차트 분석 진입」 계열),
[[project-2026-08-13-v134-v135a-learning-system]] (학습 저장 기반)
