---
name: project_2026-08-27_pyramid_bbsplit_tp20
description: Fix 178~187 — 볼밴 분할 전략 / 예약 전략 / TP1 옵션이 죽어있던 버그 / TP20 확장 / 피라미딩 개방 + 실검증으로 승률 3.7%→50% 확인
metadata: 
  node_type: memory
  type: project
  originSessionId: 9810b26c-b7e1-4349-8e91-83e3a14b072a
  modified: 2026-08-27T15:09:24.238Z
---

# 🏆 2026-08-27 Fix 178~187 + 실검증 (승률 3.7% → 50%)

main `e749115` (PR #374). **전부 배포·실동작 확인 완료.**

## 📊 실검증 결과 (24h)

| | 아침 (2일 누적) | 저녁 (24h) |
|---|---|---|
| 승률 | **3.7%** (11승 288패) | **50%** (34승 34패) |
| 실현 | **-997.66** | **+402.19 USDT** |

진단이 옳았다 — **로직이 나빠서가 아니라 실행되지 않아서**였다.
활성 13건 미실현 +56 / 조용한 실패 0건 / KS 해제 / stage_trigger 13건 검사 오류 0.

## 신설 전략 2개

**① 볼밴 분할 (`pump_split_entry_worker`, 15분)** — Fix 179/179b/180/181
사장님: "볼밴 하단 -3% 이탈 100 / -5% 200 / -7% 300, 손절 -10%, tp1 15%"
- 기준선 = LONG 하단(일반)·중단(긴추세) / SHORT 상단·중단. 「긴추세」= 4H 중단선 위 24h 유지
- 1차는 **「이탈 즉시」가 아니라 기준선 -3% 도달** (가짜 이탈 방지)
- 손절 ROI -10% → 3차까지 물렸을 때 볼밴 -10.41% ≈ 사장님 지정
- **실동작 확인**: `#1579 ONTUSDT STAGE2_OPEN` = 2차 분할 실체결 / `#1581 BTRUSDT` +20.10 익절
- 설정 UI = ⚙세팅 카드 「📊 볼밴 분할 전략」 (`pump_split_enabled`/`_capitals`/`_max_concurrent`)
- **전용 상한은 전역과 독립** → 계정 전체 = 전역 + 볼밴 상한의 합
- 🚨 **죽은 단계 검산**(`check_no_dead_stage`)을 저장·실행 양쪽에서 돌린다.
  중간에 검토한 -1/-3/-5% 안은 **3차가 영원히 진입 불가**였다 (손절가 -4.78% < 트리거 -5%).
  음성 대조군으로 검사가 실제로 잡는지도 고정했다 (레버 10x·SL 4%·간격과다 → 차단).

**② 예약 전략 (`scheduled_entry_worker`, 5분)** — Fix 182/182b
- `POST /strategies` 는 원래 생성만 하고 진입 안 함(`WAITING`) → **절반은 이미 있었다.**
  구별자 = `capital_management_mode = "scheduled"`, 모달 「📅 예약으로 저장」 버튼
- WAITING 은 ACTIVE_LIKE 에 **없다** → 슬롯 안 먹고 중복가드도 안 걸림 → **진입 직전 재확인** 필수
- 기본 7일 만료 (잊힌 예약이 갑자기 들어가는 게 가장 위험)
- 목록 배지 📅 예정 / 📅 예약진입

## 🚨 TP1 옵션이 **완전히 죽어 있었다** (Fix 183/184)

실측: `strategy=1577 TP1_override=20.00 → tp_levels=[('TP20', 20.00), ..., ('TP1', 10.0)]`
→ 사장님이 20% 로 설정했는데 **ROI 10% 에서 익절**되고 있었다.

원인 2겹:
1. `tp_levels` 는 **TP20→TP1 내림차순**인데 `idx == 0` 에 override 를 넣었다 = **TP20 을 덮어씀**
2. `TP_LABEL_TO_IDX` 가 TP1~TP10 만 → TP20 은 -1 → `-1 > -1` 거짓 → **애초에 도달 불가**

계보: v105 `max()` 전체적용 → TP1~4 동시발동 → #838 사고 → v131 이 고치려다 엉뚱한 칸 지목
= **사고를 덮으면서 기능을 껐다.** 로그는 정상처럼 보였다.

해법: label 로 TP1 지정 + **사다리 전체를 같은 폭만큼 shift** (10/15/20/25 + 설정20 → 20/25/30/35).
TP1 만 바꾸면 TP2(15) 가 먼저 발동해 TP1 이 영원히 건너뛰어진다 (실측 3개 연쇄).
평가도 **임계 오름차순 → TP 번호 순 + 순차**로 변경. 정상 사다리 28조합은 결과 동일(회귀 없음).

## TP10 → TP20 확장 (Fix 186/187)

**사장님이 맞았다 — 기반은 이미 있었다**: alembic 0021 이 tp11~20 컬럼을, v118 이 청산 경로를
이미 확장해 뒀고 **판정 경로만 TP10 에서 막고 있었다.** 마이그레이션 불필요.

가장 위험했던 곳 = `tp_sl_orchestrator:399` `if 1 <= n <= 10: ... else: status="COMPLETED"`
→ TP11 체결 시 **잔량이 남았는데 COMPLETED + total_capital=0** → SL·트레일링 정지 → 고아 → KS.

하드코딩 9곳을 전부 `TOTAL_TP_LEVELS` 참조로. 덤으로 **이미 깨져 있던 2건**도 정리:
`tp_miss_detector` 가 TP1~5 만 감시(TP6+ 무감시) / 테스트가 v121 이후 한 달째 빨간불.

**Fix 187** = 각 TP 가 잔량의 25%씩 복리로 줄어 최소주문(5 USDT) 미만이 되면 주문이 거절되고
**그 TP 가 영원히 완료 안 돼 사다리가 조용히 멈춘다**. 미달 시 전량 청산으로 해소.
미달 시작점: 투입 100U→TP10 / 600U→TP16 / 2000U→TP20 / 12000U→없음.

## 수익 피라미딩 개방 (Fix 185)

사장님: "모든 전략 — 수동/모달 전략도 수익 나면 추가 진입" / "수익구간에서 추가하는건 좋은 전략같아"

막고 있던 2겹:
1. `AUTO_ENTRY_TYPES_PYRAMID` **화이트리스트** — 모달·볼밴 전략은 전부 탈락 (`not_auto_entry_type`)
2. **동시보유 상한 게이트 = 내 판단 착오.** Fix 112b 는 "새 전략을 만든다" 전제였는데
   Fix 156 에서 `add_position_now`(기존 포지션 추가)로 바꿔놓고 게이트를 안 걷어냈다.
   → 10/10 이면 워커가 통째로 return. #1581 이 ROI +22% 까지 갔는데 피라미딩 0회.

**실동작 확인**: 재시작 직후 카운터 6→10건 (증가 6 = 사이클예산 3 × 2사이클).
사유 집계가 `roi_below_trigger=26 / max_pyramid_count=3 / cooldown=1` 로 바뀌고
`not_auto_entry_type`·`slot_full` 이 **0** = 30건 전부 후보로 올라옴.

## 🔑 사장님 결정

1. 볼밴: -3/-5/-7%, 손절 -10%, TP1 15%부터 25%씩, 회귀 -3%, 자본 100/200/300, 전용상한 3
2. 예약 전략 = 「모든 전략」 대상 / 2배율 자동채움은 **불필요** (직접 입력)
3. 피라미딩 = **모든 전략** (수동/모달 포함), 1회 300 고정
4. TP **20단계까지** 사용
5. 기존 방식(수동 물타기)은 **「청산 후 재진입」 해제 + 목록에서 「강제:끔」** 이 필요

## ⚠️ 운영 교훈 (실제로 겪음)

**`git pull` 이 "Already up to date" 여도 재시작은 별개다.**
머지 직후 pull 했다면 그때 이미 최신이라, 나중에 pull 만 반복해서는 **절대 반영되지 않는다.**
Fix 185 가 배포됐는데도 로그에 옛 `Fix112b` 가 계속 나온 원인이 이것이었다.

## 헌법 128~142

**128** 자본 모델(청산후대체 vs 분할매수)이 다르면 전략을 분리 — 같은 -5% 가 다른 걸 뜻하면 사고
**129** 새로 돈을 넣는 기능은 기본 OFF 로 배포
**130** 분할 진입은 각 차수 트리거가 손절가보다 먼저 오는지 검산 — 어긋나면 조용히 죽는다
**131** 사용자가 바꿀 수 있는 수치는 **바꿨을 때 무엇이 깨지는지** 함께 검산
**132** 검사를 만들면 그 검사가 실제로 잡는지 **음성 대조군**으로 증명
**133** 리스트를 인덱스 위치로 지목하지 말 것 — **의미(label)로** 찾을 것
**134** 사고를 덮는 수정이 **기능 자체를 끄지 않았는지** 확인 (v131 = TP1 옵션 사망)
**135** 사다리형 임계값은 한 칸만 바꾸면 안 된다 — **통째로 이동**
**136** 상수가 정한 범위 밖의 항목을 만들지 말 것 — 안 쓰이면 "되는 줄" 착각하게 된다
**137** 게이트를 걸 때 그 워커가 **무엇을 만드는지** 확인 (Fix112b → 156 → 185)
**138** 화이트리스트는 **나중에 생긴 것이 자동으로 빠진다** — 블랙리스트를 쓸 것
**139** 확장은 상수 하나만 올려서 끝나지 않는다 — 전수 grep 후 **한 커밋**으로
**140** py_compile 통과는 import 존재를 보장하지 않는다 — **실제 import 해서 접근**해 볼 것
**141** 회귀는 「몇 개 실패」가 아니라 **before/after 실패 목록 차집합**으로 (26→22, 신규 0)
**142** 비율 청산은 **복리로 작아진다** — 최소 주문 금액에 부딪히면 쪼개지 말고 정리

## ⚠️ 미해결

- **chart_profile.py** (MTF 수치 프로파일) — 사장님 "천천히 만들어 두고", 아직 미작업.
  감사 결과: `mtf_snapshot` 의 `snap["tf"]` 를 **읽는 코드가 0건**, `exit_context` 영구 `{}`,
  `StrategyInstance.entry_snapshot` **컬럼 자체가 없어** failure_pattern_analyzer 가 항상 None.
- **15분 게이트 두 겹** (confirm_peak RSI≥65 꺾임 vs Fix55 RSI 1.0↓, 3단계 3/3 STRICT) —
  사장님이 「그대로」 선택. BTRUSDT 처럼 OBV 안 꺾이는 종목은 3단계가 실질 차단.
- **TP11~20 임계** = 기본 +5% 사다리라 TP20 = ROI 105%. 큰 포지션에서만 의미.
- 모달 TP 입력칸이 TP1~TP10 까지만 (TP11~20 은 auto-extend 로만).
- `saveV219Settings` **함수 2중 정의** (strategy-suggestions.js:1120 죽은 코드 / :1466 실동작).

관련: [[project_2026-08-27_obv_stage_entry_model]] [[project_2026-08-26_killswitch_gaps]]
