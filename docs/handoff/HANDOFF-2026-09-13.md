# 인계 메모 — 2026-09-13

새 대화는 이 문서 하나만 읽고 시작하면 된다. 사장님이 "대기열 진행" 이라고 하면 §4 순서대로 묻지 말고 진행한다.

## 1. 지금 배포 상태

| 항목 | 값 |
|---|---|
| main 최신 | `8289bc1` (문서) · 코드 최신 `d0194b0` |
| VPS | `root@159.65.137.250` · `~/binance-auto-trader/backend` · 서비스 `api` / `scheduler` |
| alembic head | `0039_si_entry_profile` |
| 작업 워크트리 | `.claude/worktrees/infallible-euler-6dc297` (브랜치 `claude/infallible-euler-6dc297`, origin 과 동기) |
| 배포 검사기 | `docker compose exec -T scheduler python scripts/verify_fix364_deploy.py` → ①~⑥절 PASS (9/12) |

## 2. 최근에 끝난 일 (9/9 ~ 9/12)

- **Fix 364~366** OBV 자동 단계 손절 25/15/25/25 · 심볼 관리 재진입(프로브) · 가상매매 엔진 v2. 문서 `docs/spec/OBV_AUTO_LADDER_SPEC_2026-09-09.md`, `MANAGED_SYMBOL_REENTRY_SPEC_2026-09-09.md`, `PAPER_TRADING_LEARNING_2026-09-08.md`.
- **Fix 367 / 367b·c·d** 「➕ 새 전략 (기존 방식)」 = 처음 방식. 새로 만드는 기존 방식 전략은 TP1 +25%(25% 익절) · 강제손절 없음 · 단계 정리(잔량 10) 제외. 표식 컬럼 `strategy_instances.entry_profile='legacy_manual'` 이 있는 인스턴스만 해당, 배포 전 인스턴스는 옛 동작. 문서 `docs/spec/LEGACY_LADDER_RESTORE_2026-09-11.md`.
- **Fix 368** 외부 전략 2종(후지모토 RSI·MACD·일목 3역 호전 / 마하세븐 MA30·200 속임수 돌파). 워커 `external_strategies`(60초) **shadow 모드** = 신호만 Redis 에 기록, 주문 없음. 가상매매 규칙 8개 등록. 문서 `docs/spec/EXTERNAL_STRATEGIES_FUJIMOTO_MACH7_2026-09-12.md`.
- **사장님 결정 1 적용**: VPS 설정 `pyramid_sides=LONG` (9/12 00:50 UTC). 자동 피라미딩은 LONG 에만.

## 3. 운영 상태 스냅샷 (9/12 09:00 KST)

- 살아 있는 전략 9건: OBV 자동 프로브 5 (TRUTH·FF·VVV·XAN·BR LONG), 기존 방식 3 (SOPH·RAYSOL·哈基米 LONG, 표식 있음), v219 사다리 1 (DASH SHORT 2단계).
- 7일 실현손익 **−1,162.66**. 손실 대부분이 「추가」에서 났다: 수동 💉 추가 건 약 −1,000, 자동 피라미딩 0/7. 추가 없는 OBV 프로브는 −5.8.
- 캡: `sajangnim_top_short_daily_limit=1`(v219 사다리 전용 범위) · `pump_split_max_concurrent=1`. v219 사다리 건당 −3.8 이라 유지.
- 시장 국면 `market_breadth_last` = MKT_UP (0.695, 9/11 23:46 UTC).
- 가상매매 채택 규칙: SHORT `confirm_peak_111`·`s2_hist_turn_down`, LONG `bottom_331`·`multiday_rebound_352`(UP24)·`surge_start_346`(DOWN24). 탈락: `pullback_331`·`l1_hist_turn_up`·`toprev_331`·`s1_breakdown`.

## 4. 다음 할 일 (사장님 9/12 승인, 순서대로)

단일 진실 = `docs/spec/PENDING_DEV_QUEUE_2026-09-12.md`. 요약:

1. **2① 수동 💉 추가 뒤 손절** — 설정 `manual_add_after_sl_enabled`(기본 0). `api/v1/strategies/lifecycle.py` add-position 성공 뒤 `success_pyramiding_worker._apply_after_add_sl` 호출.
2. **2② LONG 자동 추가는 MKT_UP 일 때만** — 설정 `pyramid_require_breadth_up`(기본 1), 국면 값 없으면 허용.
3. **3A** SHORT `s2_hist_turn_down` 진입 가족 (shadow).
4. **3C** `long_bottom_detector` 다일 필터 24h≤−8% → 설정 `multiday_context=UP24`.
5. **3B** LONG `bottom_331` 진입 가족 (shadow, 4h 80봉 필요).
6. **3D** LONG `surge_start_346` DOWN24 직접 진입 (shadow).
7. **4** 후지모토·마하세븐 실주문: 가상 보고서에서 규칙 Δ>0·CV 4/4·n≥100 확인 뒤 사장님이 `fujimoto_mode=on` / `mach7_mode=on`.
8. **5** 캡 1·1 재검토: 3번 배포 1주 뒤.

한도 권고(9/13): Opus 5 남은 한도로는 1~4 까지만, 5~8 은 Fable 재설정(9/16 14:00) 뒤.

## 5. 작업 규칙 (꼭 지킬 것)

- **실자금**: Claude 는 주문을 내지 않고 API 키를 다루지 않는다. 새 전략 가족은 shadow 기본, 실주문은 사장님이 설정으로 켠다.
- **공개 저장소**: 커밋 전 staged diff 에서 `npg_`, `sk-`(20자↑), `ghp_`, `ep-*neon`, `postgresql://…:…@` 스캔. `api_secret=decrypt_text(...)` 는 오탐.
- **숫자**: 사장님 숫자는 그대로, Claude 가 고른 숫자는 설정 키 + 문서에 「Claude가 정함」.
- **가족별 적용**: OBV 자동 / v219 사다리 / 볼밴 분할 / 기존 방식 / 재진입 / 외부 전략 — 공용 경로에 넣지 않는다.
- **줄끝**: CRLF 파일(strategy_service, stage_trim, crud, models/strategy_instance, schemas/strategy, execution_service, capital_calculator, cm-open-modal.js, multi-symbol.js, template-save.js 등)은 패치 스크립트로 줄끝 보존. 패치 스크립트는 Write 도구로 스크래치패드에 쓴다(bash heredoc 에 한글·따옴표 넣으면 깨진다).
- **검증 규모**: 반박 검증 워크플로는 주문·손절·자본 로직 변경에만 1회(렌즈 3~4). 화면·문서·후속 수정은 테스트 + 검사기로 끝낸다. (사장님 9/11 「이거 하나에 이렇게 오래 걸려야 하나」)
- **git**: `git stash` 금지. 푸시 전 `git fetch && git merge --no-edit origin/main`, 푸시는 `git push origin HEAD:main` + `git push origin HEAD`. 커밋 전 `git clean -n` 으로 리뷰 에이전트가 만든 빈 잡파일(`backend/app/0`, `tuple[bool` 등) 확인.
- **배포**: VPS `git pull origin main` → (마이그레이션 있으면) `docker compose exec -T api alembic upgrade head` **성공 확인** → `docker compose restart api scheduler` → 검사기 PASS + 첫 사이클 로그. alembic 리비전 id 는 32자 이내(9/11 사고).
- **DB 쓰기**: VPS DB UPDATE/INSERT 는 사장님 명시 결정이 있을 때만. 읽기 전용 조회는 `docker compose exec -T api python -` 로 자유롭게.

## 6. 참고 위치

- 제 메모리 색인: `C:\Users\user\.claude\projects\C--Users-user------binance-auto-trader\memory\MEMORY.md` (맨 위 = 개발 대기열, 두 번째 = 차트 학습 일지에 Fix 353~368 교훈).
- 가상매매 보고서: API `/api/v1/paper-trading/report.md` (VPS 에서 `_build_report(db, 60)` 로 직접 뽑으면 약 4분 걸린다).
- 외부 전략 그림자 신호: Redis `ext:shadow:{fujimoto|mach7}:*`, 사이클 요약 `ext:last_cycle`.
- 관리 재진입 명부: 테이블 `managed_symbols`, 사이클 요약 로그 `[Fix365] 완료`.
