# 2026-09-03 사장님 요청 전체 감사

> 감사 방법: 12건 각각을 **감사관 1명 + 반증관 1명**이 독립 조사했다.
> 반증관은 원 판정을 뒤집는 것을 목표로 삼았고, 뒤집힌 항목은 본문에 명시했다.
>
> 🚨 **이 문서의 유일한 규칙: 「코드에 그 문자열이 있다」는 증거가 아니다.**
> 오늘 하루에만 Fix 318 이 엉뚱한 함수에 붙어 정적 검사 13건을 통과했고, Fix 311 이 사장님 사양을
> 통째로 막은 채 테스트 57건을 통과했으며, Fix 315 는 마커를 저장하는 코드가 0곳이었다.
> 그래서 아래 판정은 전부 **① 운영 로그 ② DB 행 ③ 운영 컨테이너에서 직접 함수 호출 ④ AST 전수**
> 중 하나 이상의 실행 증거에만 근거한다. 증거가 없으면 **「미검증」**이라고 썼다.
>
> **감사 시각**: 2026-09-03 12:45 UTC (21:45 KST)
> **배포 상태**: VPS `~/binance-auto-trader` HEAD = **`32a7696` (Fix 331)** — 로컬 워크트리와 동일
> **컨테이너**: api 11:17:52 UTC 기동 / scheduler **12:34:48 UTC 기동**
> **VPS 는 읽기 조회만 했다.** 배포·재시작·설정변경·DB 쓰기·주문은 일절 하지 않았다.

---

## 🚨 감사 도중 상황이 바뀌었다 — 반드시 먼저 읽어주십시오

과업 브리핑의 기준 커밋 `63d7b08`(Fix 328)은 이미 낡았다. 감사가 진행되는 동안 **Fix 329 → 330 → 331 이
연속 배포**됐고, 스케줄러가 **12:34:48 UTC 에 재기동**되면서 **12:35:24 UTC 에 오늘 첫 「완전체」 진입이
발생**했다. 이 한 건(`ZECUSDT #2098`)이 감사 판정 **세 개를 동시에 뒤집었다**.

```
12:30:50  [Fix111] ZECUSDT 정점확인 OK (반복상승 3회, 꺾임 2/2)
12:30:50  [Fix270/4H] ✅ 4H 참고 — 반전 전략이라 막지 않음 (Fix 330)
12:30:50  [Fix247] 🚫 진입 차단 — 합의 판정 AVOID (score=0)      ← Fix 331 이전
12:30:50  ❌ ZECUSDT 진입 실패 = 알람 유지 (재시도!)
          … 30초마다 동일 패턴 반복 (12:31 / 12:32 / 12:33 / 12:34) …
──────────────  12:34:48 UTC  scheduler 재기동 (Fix 331 적재)  ──────────────
12:35:25  [Fix315] ZECUSDT SHORT 단계 사다리 [10.0, 300.0, 600.0] (간격 1.50%) — 총 910 USDT
12:35:25  [Fix299/적응TP] |24h| 5.5% < 15% = 안정 → TP1 3%  | 사다리 [3.0, 6.0, 9.0, 12.0]
12:35:26  [Fix52] SL 강제 -5%: strategy=2098
12:35:26  [auto_bb_breakdown] 🎯 실 진입 성공: ZECUSDT SHORT strategy=2098 (MARKET!)
12:35:26  [sajangnim_top_v219] ✅ 자동 SHORT: ZECUSDT cap=10.00 conf=0.85 (id=2098)
```

**동일 심볼·동일 조건에서 배포 전후가 갈린 자연 실험이다.** DB 재조회 결과:

| 컬럼 | 값 | 의미 |
|---|---|---|
| `capital_management_mode` | **`stage_ladder`** | 전 기간 **단 1행** — Fix 315 사다리 최초 실행 |
| `total_capital` | **910.00** | = 10 + 300 + 600 (사장님 사다리) |
| `stages_config` | `{'capitals':[10,300,600],'stages_count':3,'trigger_percents':[None,1.5,1.5]}` | 3단계 실제 생성 |
| `tp1_pct_override` | **3.00** | 전 테이블 **단 1행** — 적응 TP 「안정 3%」 최초 실행 |
| 템플릿 TP 사다리 | **3 / 6 / 9 / 12** | 비율 유지(가산 아님) 확인 |
| `force_sl_roi_override` | 5.00 | -5% 짧은 손절 |
| 워커 | `sajangnim_top_v219` | **v219 정점 SHORT 7일 0건 → 진입 성공** |

---

## ⚡ 한 장 요약

| # | 요청 | 관련 Fix | 상태 | 근거 |
|---|---|---|---|---|
| ① | OBV 자동 로직이 실제로 작동하는가 | Fix 173/177, 130 | 🚨**고장** | 자동 신규 진입 **30일 0건**. 살아있는 OBV_REVERSE **0건**. `auto_obv_enabled='1'` 인데 그 값을 읽는 유일한 워커의 호출처가 스케줄러에서 **주석 2줄**. 템플릿 87건 **전부 수동 `_quick_`** |
| ② | 추가 진입 트리거 ROI +5% → **+2%** | Fix 300 | ✅**작동확인** | 운영 컨테이너에서 `_trigger_roi(db)` 직접 호출 → **2.0**. 로그 `entered=0 skipped=5 … 트리거 ROI>=2.0%` 와 DB 후보 5건 산술 완전 일치. 설정 후 발동 14건 중 **10건이 ROI 5% 미만**(min 2.045), 전건 FILLED 주문 매칭 |
| ③ | 부분 손절 — 모든 단계에서 10 USDT 남기기 | Fix 318/319/324/326 | ⚠️**배포됐으나미검증** | 부분 손절 자체는 오늘 **7건 실행**(잔량 명목 20.00 = 증거금 10 × 레버 2). 그러나 **7/7 전부 다음 사이클이 전량 청산**해 되돌림. 그걸 고치는 **Fix 326 은 12일 전체 로그에 0회** |
| ④ | BTC/ETH 계열 11종목 제외 | Fix 303 | ✅**작동확인** | api·scheduler 두 컨테이너에서 `_assert_symbol_allowed` 직접 실행 → 11종 전부 BLOCK, 소문자·공백·빈값도 fail-SAFE. 오늘 신규 31건 중 제외심볼 **0건**, 마지막 진입은 08-25(Fix 303 이전) |
| ⑤ | v219 사다리 — 1단계 10은 손절없이, 2단계부터 부분손절 | Fix 315/317/322/326 | ⚠️**배포됐으나미검증**<br>(부분 승격) | **✅ 3단계 사다리 구성은 12:35:24 첫 실행 확인**(`stage_ladder` 1행, 910 USDT). **✅ 1단계 손절없이**도 로그 2건 확인. **⚠️ 그러나 2단계 도달 0건** — 부분손절→다음단계 완주는 아직 0회 |
| ⑥ | 기본 방식과 OBV 자동을 완전히 구분 | Fix 322/323/326 | ⚠️**배포됐으나미검증** | Fix 322 절이 실제 면제 사유가 된 건수 **0/25**, Fix 323 신규 절은 `trim_on=True` 가 단락시켜 **현 설정에서 도달 불가**. 살아있는 25건 중 OBV **0건** |
| ⑦ | 다단계 전략의 손절 잠김 해제 | Fix 321/322 | ✅**작동확인** | 현재 배포 코드(01:52:26 기동) 이후 게이트 **건너뜀 15건**, `FORCE_STOP_LOSS_TRIGGERED` **19건**과 초 단위 일치. 비종료 전략 **25건 전수 `exempt=True`, 잠김 0건** |
| ⑧ | 상승 50 + 하락 50 = 100개 모니터링 | Fix 325 | ✅**작동확인** | 24h 로그 51만 줄에서 해당 판정 **504건 전부** 100개. 컨테이너 실시세 경계 실측: 상승 50위 통과 / 51위 차단, 하락 50위 통과 / 51위 차단 |
| ⑨ | 「가능하면 10% 상승하락」 우선 진입 | Fix 328 | ✅**작동확인** | 게이트 자체 운영 차단 로그 **76줄**(고아행 #2086/#2087 과 교차확인), `게이트 오류` 0건. 우선 분기는 `#2097 EGLDUSDT` 실 진입으로 통과 확인. 빠듯 분기(70%)는 러닝 프로세스 경계 실행으로 확인 |
| ⑩ | 지지선 7점 판정 | Fix 327 | ⏸**설정꺼짐** | `support_score_gate_enabled` **DB 행 없음** → `gate_enabled=False`. 배선·순서(`start_stage1` 발주 **앞**)는 정상. 🚨 **켤 수 있는 UI·API·시드가 저장소에 0곳** |
| ⑪ | 손절 후 재진입 — 실제로 도는가 | Fix 274/270/315 | 🚨**고장** | 24시간 **608회 시도, 성공 0건**. 차단 귀속: LONG 304건 → Fix 274 가 297건, SHORT 297건 → Fix 270 이 294건. 재진입 면제 분기가 **구조적으로 없음**(게이트가 `strategy_type_suffix` 사용 지점보다 앞) |
| ⑫ | 적응 TP — 기본·OBV 경로에도 적용됐는가 | Fix 299 | ❌**미구현**<br>(auto_bb 는 ✅) | `adaptive_tp` 호출처는 배포 이미지에서 **`auto_bb_breakdown_worker.py` 단 1개**. `execution_service` / `strategy_service` / `bb_mid_line_worker` / `pump_split_entry_worker` 전부 **0곳**. **OBV_REVERSE 76건 전부 TP1 = 15.00** |

**집계: ✅작동확인 5 / ⚠️배포됐으나미검증 3 / ⏸설정꺼짐 1 / ❌미구현 1 / 🚨고장 2**

> 🚨 **오해 방지**: ✅ 5건은 「그 기능이 도는 것」을 확인한 것이지 「돈을 벌고 있다」가 아니다.
> 그리고 12건 중 **사이클을 끝까지 완주한 것은 0건**이다 — 오늘 2단계 이상 도달 전략이 하나도 없다.

---

## 🚨 지금 당장 손봐야 할 것

우선순위는 **실자금 손실 위험**이 큰 순서다.

### 1순위 🩸 명목 30 USDT 미만 포지션이 강제손절에서 **영구 면제**된다 — 지금 1건 살아있음

**무엇이**: `compute_trim` 이 SKIP 을 반환하고, Fix 326 이 SKIP 이면 **아무것도 안 하고 `return`** 한다.
결과: 손절 주문이 **아예 나가지 않는다.**

**왜**: `stage_min_trim_ratio = 2` 이므로 「청산분 ≥ 잔량 × 2」를 못 채우면 SKIP 이다.
목표 잔량이 명목 20(= keep_notional 10 × 레버 2)이므로, **보유 명목이 60 미만이면 어떤 손실 구간에서도 SKIP**이다.
실서버 심볼 필터로 -5% / -10% / -20% / -50% / -80% / -100% / -200% / -400% ROI 를 전부 재현했고 **전 구간 SKIP**이었다.
사유만 순서대로 바뀐다: 「잔량 목표 20.00 >= 보유 명목」 → 「청산분 명목 < MIN_NOTIONAL 5」 → 「청산분 < 잔량 × 2」.

**결과**: 의도한 -5%(≈ -0.5 USDT) 손절 대신 **증거금 10 USDT 전액**을 거래소 강제청산까지 끌고 간다.
Fix 326 **이전**에는 SKIP → 전량 청산이었으므로 **이 동작 변화는 Fix 326 이 만든 것**이고, 아직 한 번도 실측되지 않았다.

**어느 파일 어느 함수**:
- `backend/app/services/stage_trim.py` → `compute_trim()` (SKIP 판정)
- `backend/app/services/tp_sl_orchestrator.py` → `_execute_force_stop_loss()` (SKIP 이면 `return`)

**현재 노출**(감사 시각 기준 `STAGE1_OPEN` 5건 중):

| id | 심볼 | 자본 | 레버 | 명목 | 사다리 | 상태 |
|---|---|---|---|---|---|---|
| **#1988** | TQQQUSDT | **10.00** | 2 | **20** | 단계 1개 | 🩸 **영구 면제 + 갈 2단계 없음** (09-02 00:30 생성, 36시간째) |
| #2088 | CRDOUSDT | 100 | 2 | 200 | 1개 | 정상 |
| #2090 | MAGMAUSDT | 1500 | 2 | — | 1개 | 정상 |
| #2096 | ARCUSDT | 100 | 2 | 200 | 1개 | 정상 |
| #2098 | ZECUSDT | 910 | 2 | — | **3개** | 정상(신규) |

> 앞선 감사 시점의 #2005 BMNRUSDT / #2009 MSTRUSDT 는 그 사이 종료됐다. **현재 남은 것은 #1988 하나.**

**파급 (더 중요)**: 부분 손절은 **정의상** 명목 20(증거금 10)을 남기므로, **성공한 부분 손절의 잔량은
다음 사이클에 반드시 이 SKIP 구간에 들어간다.** Fix 326 이전에는 다음 사이클이 지워버려서 문제가 안 보였다.
Fix 326 이 실행되기 시작하면 **잔량이 영원히 안 닫힌다.** 오늘 6시간에 7건이 발생했다.
게다가 `STAGE1_OPEN` ∈ `ACTIVE_WITH_POSITION` ⊂ `ACTIVE_LIKE` 이고 `position_limit.count_active_positions`
가 이 집합을 세므로 **잔량 하나가 동시보유 슬롯 하나를 영구 점유**한다.
단계계획이 여럿인 사다리 전략은 다음 단계로 흘러 해소되지만, **1단계 단발 전략과 수동 `_quick_` 전략은 해소 경로가 0개**다
(`force_close_after_hours` / `time_based_exit` / `max_holding_hours` / `time_reverse_exit` 전부 미설정,
`reconcile` 은 거래소 잔량이 0일 때만 정리).
최근 3일 생성 148건 중 **단계 1개짜리가 90건(61%)** 이다.

🔁 교훈: **「부분 청산을 만들면, 남긴 것이 다음 사이클에 어떻게 취급되는지 끝까지 따라가라.」**
Fix 326 자신의 교훈이 **한 칸 더 남아 있었다.**

---

### 2순위 🚨 손절 후 재진입 — 24시간 608회 시도, 성공 0건 (항목 ⑪)

**무엇이**: 재진입 워커는 30초 주기로 **살아서 돈다**(watchlist·대기사유 기록 정상). 그런데 완주가 0이다.

**왜**: `_create_auto_bb_strategy` 의 **신규진입 게이트 2종이 재진입을 면제해주지 않는다.**
24시간 「None 반환」 608건을 선행 게이트 로그와 동일 심볼·동일 방향·2~6ms 간격으로 귀속한 결과:

| 방향 | 시도 | 주 차단자 | 건수 |
|---|---|---|---|
| LONG | 304 | **Fix 274** (24h 급등 15% 미만) | 297 |
| SHORT | 297 | **Fix 270** (4H MACD hist) | 294 |

```
11:11:07,329 [Fix274/LONG급등] ⛔ SUIUSDT 차단 — 24h +7.6% < 15%
11:11:07,333 [RT_REENTRY] 🚨 skip: SUIUSDT LONG _create_auto_bb_strategy None 반환!
```

AST 로 확인: `_create_auto_bb_strategy(L1404~2165)` 에서 `strategy_type_suffix` 가 로직에 **처음 쓰이는 곳은 L1672**인데,
`return None` 게이트 6개(L1440·1446·1539·1571·1630·1647)가 **전부 그 앞**에 있다.
→ 재진입은 어떤 게이트도 면제받을 수 없다. **구조적**이다.

**두 번째 벽**: 도착할 2단계가 없었다. 활성/재진입 인스턴스 전건에 `_count_total_stages_from_template` 를
실행한 결과 `auto_bb_break*` 는 예외 없이 `total_stages=1`. 템플릿 570여건 전수에서도 단일 자본
(`[10.0]` / `[300.0]` / `[450.0]` / `[600.0]` / `[675.0]`)뿐이었다.

**어느 파일 어느 함수**:
- `backend/app/workers/auto_bb_breakdown_worker.py` L1496~1542 (Fix 274) 및 L1545~1571 (Fix 270)
  → `strategy_type_suffix.startswith("_reentry")` 면제가 필요
- 같은 파일 사다리 생성부 → 재진입 템플릿도 다단계로 만들어야 함

**⚠️ 부분 완화**: **Fix 330 이 12:21 UTC 에, Fix 331 이 12:34 UTC 에 배포되면서 Fix 270 과 Fix 247 이
「거부권」에서 「참고」로 바뀌었다.** SHORT 재진입을 막던 294건분은 해소됐을 **가능성**이 있다.
다만 **아직 재진입 성공은 0건**이고, LONG 을 막는 Fix 274(통과율 5/2,700 = **0.19%**)는 그대로다.
→ 재측정이 필요하다. 관찰할 문구: `[RT_REENTRY] … reentered=1 … success=1`.

---

### 3순위 🚨 OBV 자동 진입이 구조적으로 도달 불가 (항목 ①)

**무엇이**: `auto_obv_enabled='1'` 로 켜져 있는데 **자동 신규 진입이 30일 0건**이다.

**왜 — 3겹**:
1. 그 설정을 읽는 실판독처는 `auto_bb_breakdown_worker.py:341` **단 하나**인데,
   `run_auto_bb_breakdown` 의 호출처가 `scheduler_runner.py:313-314` **주석 두 줄뿐**이다. 살아있는 호출 0.
   (스케줄러는 `MemoryJobStore` 라 옛 job 이 부활할 가능성도 0.)
2. 대체 워커 `unified_15m_entry` 는 30초 주기로 **실제 등록돼 있으나**(`scheduler_runner.py:502-508`)
   `unified_entry_enabled='0'` 이라 즉시 return 한다. **그리고 켜도 소용없다** — 그 파일의
   `reentry_alerts` 참조 0, `trigger_mode` 대입 0, `OBV_REVERSE` 생성 0.
   (docstring 10행은 "OBV_REVERSE 포함"이라 적었지만 **거짓**이다.)
3. 알람 → 자동진입 구간은 **의도적 미구현**이다. `static/js/reentry-alerts.js:191-244` 의 버튼은
   "⏳ 자동 실행 준비중", 모달은 사장님 2026-08-09 「A 안전」 결정으로 안전장치 4개 미완성이라 명시,
   `saveReentrySettings` 는 아예 제거됨. **이것이 템플릿 87건이 전부 `_quick_`(수동)인 이유다.**

**작동하는 부분(정정)**: 「단계 진입 **판정**」은 진짜로 돈다.
`stage_trigger_worker.py:822-840` 이 `check_stage_entry_signal` 을 실호출하고
`[stage-trigger Fix173 OBV] 🎯 진입 신호!` 를 찍는다. 그리고 실제로
`09-03 07:58~08:00` 에 `[stage-trigger Fix173 OBV] ⏳ 대기: strategy=2091 stage=2 … 진입창 차단: 볼밴 위치 1.185 > 1.00`
가 남아 있다. → **판정은 살아있고, 자동 진입 생성만 죽었다.**

**런타임 태그 전수**(scheduler tail 4000): `[reentry_alert v130]` 24회, `[ladder_restart]` 18회,
`[stage-trigger]` 52회 — 그런데 **`[auto_bb_breakdown]` 0회, `[unified_15m_entry]` 0회**.
Redis `ZCARD reentry_alerts:v1 = 8`, 알람은 계속 **생산**되고 있다. **소비자가 없다.**

**어느 파일 어느 함수**:
- `backend/app/workers/scheduler_runner.py:313-314` — 주석 처리된 `run_auto_bb_breakdown` 등록
- `backend/app/workers/unified_15m_entry_worker.py` — OBV_REVERSE 생성 로직 자체가 없음
- `backend/app/static/js/reentry-alerts.js:191-244` — 자동 실행 버튼 미구현

**추가**: `ladder_restart` 는 매 사이클 「검사=9 재시작=0 사유=`{'ladder_not_exhausted':8,'dup_in_cycle':1}`」로
`check_stage_entry_signal` 에 **영원히 도달하지 못한다**(교착 확인).

---

### 4순위 ❌ 적응 TP 가 기본 방식·OBV 자동 경로에 없다 (항목 ⑫)

**무엇이**: Fix 299 의 「변동성 연동 TP1」(급등락 15% / 안정 3%)이 **`auto_bb_breakdown_worker` 한 경로에만** 붙어 있다.

**왜**: 배포된 이미지 안에서 직접 grep 한 결과

```
docker compose exec api grep -rn "adaptive_tp|pick_tp1|tp_ladder_from_tp1" /app/app
  → auto_bb_breakdown_worker.py 만 (1822 / 1827-1829 / 1854 / 1968)
docker compose exec api grep -c adaptive /app/app/services/execution_service.py    → 0
                                        /app/app/services/strategy_service.py      → 0
                                        /app/app/workers/bb_mid_line_worker.py     → 0
                                        /app/app/workers/pump_split_entry_worker.py→ 0
```

`strategy_service.py:576` 은 배포본에서도 `tp1_pct_override=TP1_PCT_DEFAULT` 그대로다.

**DB 결정타**: `tp1_pct_override` 전체 분포 —

| 값 | 건수 | 출처 |
|---|---|---|
| **3.00** | **1** | 🆕 오늘 12:35 `#2098 ZECUSDT` (auto_bb 경로) |
| 5.00 | 89 | `bb_mid_line` / `pump_split` 하드코딩 |
| **15.00** | **811** | 기본값 — **`OBV_REVERSE` 76건 전부 여기** |
| 20~300 | 428 | `control.py:1012` 수동 API |
| NULL | 152 | — |

즉 **적응 TP 가 만들 수 있는 값은 15 아니면 3 뿐인데, 3 이 오늘 12:35 이전까지 0건이었다.**

**결함의 진짜 크기 (감사관이 놓친 것)**: 기본 방식·OBV 자동이 **지금 실제로 도는 사다리는 15/20/25/30** 이다.
템플릿 5/10/15/20 에 override 15 → `shift = +10` → 효과 사다리 **15/20/25/30**, TP5 부터 `risk_service.py:608-613`
이 **+5 씩 자동 연장**한다. 그런데 `adaptive_tp.py` 독스트링이 직접 이렇게 적어 놨다 —
「기존 사다리(15/20/25/30)는 간격이 5%p 로 좁아 TP1 만 닿고 나머지는 거의 못 닿았다」.
**Fix 299 가 제거 대상으로 지목한 바로 그 사다리 형태 그대로 두 주력 경로가 돌고 있다.**

**어느 파일 어느 함수**:
- `backend/app/services/strategy_service.py:576` — `tp1_pct_override=TP1_PCT_DEFAULT` (적응 TP 미연결)
- `backend/app/workers/stage_trigger_worker.py` OBV 경로 — 호출 0곳
- `backend/app/services/adaptive_tp.py` — 모듈 자체는 정상 (`adaptive_tp_enabled='1'`)

**🚨 부수 위험**: `auto_bb_breakdown_worker.py:1834-1845` 는 전략 생성마다 **`BinanceClient` 를 새로 만들고
자격증명을 복호화해 `get_24hr_ticker(symbol)` 를 호출**한다. 그 뒤 Fix 310/328 게이트가 막으면 전략이 통째로 삭제된다.
**IP ban(418) 무한 연장 전력이 있는 저장소**에서 핫패스에 버려질 REST 호출을 얹은 것이다.
→ 적응 TP 블록을 게이트 **뒤로** 옮기면 실효 회복 + API 절약이 동시에 된다.

**🔧 정정 (앞선 개선안이 틀렸다)**: 「risk_service 는 TP1 만 override 해서 TP1 3% / TP2 15% 역전이 생긴다」는
**v131 시절 동작**이다. 현재 `risk_service.py:701-710`(**Fix 184**)는 사다리 전체를 이동시킨다:
```python
_shift = _override - Decimal(str(_tp1_base))
tp_levels = [(label, val + _shift) for label, val in tp_levels]   # 사다리 전체 이동
```
역전은 발생하지 않는다. **진짜 문제는 이동이 「가산」이지 「비례」가 아니라는 것**이다.
템플릿 5/10/15/20 에 override 3 → `shift = -2` → **3/8/13/18** 이 되지, 의도한 **3/6/9/12** 가 되지 않는다.
(auto_bb 경로는 override 가 아니라 **템플릿을 직접 3/6/9/12 로 세팅**해서 비율이 유지된다 — `#2098` 로 확인.)

---

### 5순위 ⚠️ 「부분 손절 → 다음 단계 진입」이 오늘 0/7 로 되돌려졌다 (항목 ③·⑥)

**무엇이**: 부분 손절은 **성공**했는데, **다음 사이클이 잔량을 죽였다.**

오늘 실행된 부분 손절 7건 (scheduler 원본 로그 12일치 전수):

| 시각(UTC) | 심볼 | id | 청산 / 잔여 | 잔여 명목 | 다음 사이클 |
|---|---|---|---|---|---|
| 02:10:11 | MUBARAKUSDT | #2047 | 50009 / 353 | 10.01 | ❌ 전량 청산 |
| 02:36:41 | HYPEUSDT | #2045 | 7.39 / 0.13 | 10.70 | ❌ 전량 청산 |
| 05:44:26 | QQQUSDT | #1853 | 0.84 / 0.02 | 14.18 | ❌ 전량 청산 |
| 07:44:00 | UAIUSDT | #2089 | 2449 / 52 | **20.01** | ❌ 전량 청산 |
| 07:46:46 | ROBOUSDT | #2064 | 16462 / 1732 | **20.00** | ❌ 전량 청산 |
| 07:55:00 | MARSCOINUSDT | #2091 | 12960 / 184 | **20.04** | ❌ 08:00:22 전량 청산 |
| 08:01:52 | HEMIUSDT | #2095 | 9472 / 1173 | **20.01** | ❌ 08:02:09 전량 청산 |

각 건마다 `[Fix319] 잔량 N 유지 — 전략 종료하지 않음 (다음 단계 트리거 대기)` 가 동반됐다.
**DB 확인: 7건 전부 `STOPPED`, `current_position_qty = 0`.**

> 💡 **Fix 324 실행 확인 부수 소득**: 07:44 이전 로그는 `목표 10.00`, 07:44부터 `목표 20.00`(= 10 × 레버 2).
> 사장님 「10 usdt」= **증거금** 해석이 실제로 반영됐다.

**왜**: `tp_sl_orchestrator.py:578-581` 이 그 버그를 스스로 주석에 적어 놓았다.
Fix 326 이 정확히 이것을 고쳤는데 — **`Fix326` 문자열은 scheduler 전체 로그(1.4GB, 08-22~현재)에 `grep -c` = 0.**
`FORCE_STOP_LOSS_TRIGGERED` 도 08:02:07 이 마지막이고 Fix 326 배포(08:25) 이후 **0건**이다.

**어느 파일 어느 함수**: `backend/app/services/tp_sl_orchestrator.py` → `_execute_force_stop_loss()`

---

### 6순위 🚨 「1단계는 손절없이」가 마지막 실행에서 **정반대로 작동**했다 (항목 ⑤)

앞선 감사관이 완전히 놓친 것이다. 판정 사유와 실제 행동이 갈렸다.

```
01:11:20 [Fix319] XAGUSDT #1989 전량 손절 (skip): 청산분 0.159 < 잔량 0.152 x 2
         → 1단계급 소액이라 정리하지 않는다 (사장님 「손절없이 그냥」)
01:17:20 [Fix319] XLMUSDT #2016 전량 손절 (skip): 청산분 58 < 잔량 57 x 2
         → 1단계급 소액이라 정리하지 않는다 (사장님 「손절없이 그냥」)
```

**DB**: `#1989` total_capital **10.00** → `STOPPED` 01:12:50 realized **-0.52**
       `#2016` total_capital **10.00** → `STOPPED` 01:18:50 realized **-0.54**

판정 문구는 「손절하지 않는다」인데 **행동은 전량 손절**이었다 = Fix 326 이전 `else` 분기.
🚨 **즉 이 사양이 마지막으로 실제로 돌았을 때 사장님 지시와 정반대로 작동했고, 그것을 고친 Fix 326 은 미실행이다.**
「증거 없음」보다 나쁜 상태다.

**어느 파일 어느 함수**: `backend/app/services/tp_sl_orchestrator.py` → SKIP 분기의 `else` 처리
(현재 코드는 고쳐져 있음 — **실행 확인만 남았다**)

---

### 7순위 ⚠️ 실행되지 않는 죽은 코드 3건 — 「있는데 아무도 안 부른다」

이 저장소가 반복해서 당한 유형이 **감사 대상 모듈 안에** 그대로 남아 있다.

| 함수 | 파일 | 운영 호출처 | 실측 비용 |
|---|---|---|---|
| `drop_excluded()` | `backend/app/services/symbol_exclusion.py:98` | **0곳** (tests 5곳뿐) | BTCUSDT 가 오늘 하루 **1,009줄**, 그중 `진입 실패 = 알람 유지 (재시도!)` 가 **317회** = 30초마다 성공 가능성 0인 재시도. LINKUSDT 838 / ETHUSDT 752 / LTCUSDT 178 / BCHUSDT 28 |
| Fix 322 「손절 명시」 절 | `backend/app/services/risk_service.py` | 실행 0회 | 판정 순서상 4번 `trim_enabled` 가 전역 ON 이라 항상 먼저 True → 3·5번이 **가려짐** |
| Fix 323 「재진입 예외」 절 | `backend/app/services/risk_service.py` | 실행 0회 | `trim_on=True` 가 단락. retry=True 전략 #2093 에 옛 조건/신 조건 둘 다 실행 → **바뀜 0/1** |

**`drop_excluded` 원인**: `auto_short_at_top_worker.py` 알람 루프는 **진입 성공 시에만** `r.delete(key_str)` 와
`remaining -= 1` 을 한다. 실패하면 Redis 알람이 남아 **영구 재처리**된다.
완화 요인 2개(정확히 기록): ① `remaining` 은 성공 시에만 감소하므로 제외 심볼이 **정상 후보의 슬롯을 굶기지는 않는다**
② 현재 Redis 알람에 제외 심볼 **0건**(TTL 로 자연 소멸).

**Fix 322/323 파급**: 잠금 해제 **전체가 `stage_trim_before_next_enabled=1` 스위치 하나에 매달려 있다.**
그 스위치를 끄면 어떻게 되는지 실측했다(단계계획 보유 1,488건에 `trim_enabled=False` 몽키패치):

| | 단계정리 | 손절명시 | 재진입 | 분할매수 | **잠김** |
|---|---|---|---|---|---|
| 현재(ON) | 1,368 | — | 51 | 69 | **0** |
| 반사실(OFF) | — | 1,069 | 51 | 69 | **299 (20%)** |

잠김 299건은 전부 `force_sl_roi_override IS NULL` 인 행이다. Fix 322 독스트링의 목표
(「스위치를 끄는 순간 손절이 다시 잠긴다」)가 **완전히 해소되지 않았다.**
그중 비종료는 8건(#465 #513 #561 #577 #591 #593 #595 #613)이고 전부 `REENTRY_READY` + `unrealized_pnl=0`
(실포지션 없음, 7~8월 갱신) 이라 **당장 자금 위험은 없다.**
다만 **손절 ROI 를 명시하지 않고 새로 만드는 전략은 이 함정에 그대로 빠진다.**

**추가 관측 구멍**: `risk_service.py:251` 은 **로그 없이 `return False`** 한다.
그 자리에서 잠겨도 로그로는 **영원히 안 보인다.** (`[SL v130]` 은 전체 로그에서 0건.)

---

## ⏸ 꺼져 있어서 안 도는 것

> 🚨 **아래 명령은 전부 실자금 시스템의 DB 를 직접 바꾼다. 나는 실행하지 않았다.**
> 사장님이 직접 판단하고 실행하실 것.

### (1) 지지선 7점 게이트 — Fix 327 (항목 ⑩)

**현재**: `support_score_gate_enabled` **행 없음** → `gate_enabled=False`.
운영 세션 직접 호출 결과 `gate_enabled: False / min_long: 6 / max_short: 1`
(대조군 `chg24 gate_enabled: True` — 조회 자체는 정상).

**코드는 정상이다**(직접 읽어 확인): `execution_service.py` `start_stage1` 본문
**line 254 `_sup_eval(...)` 호출 → line 272 `_place_stage_entry_order`** — 차단이 발주 **앞**에 있다.
진입 경로 8개 전수 확인 결과 **우회 경로 없다**(`pump_split`/`scheduled_entry`/`auto_reentry`/`ladder_restart`/
`surge_ladder`/`auto_bb_breakdown`/`control.py`/`auto_long_at_bottom` 전부 `start_stage1` 로 수렴).
컨테이너 파일 mtime 08:51Z < api 기동 11:17:52Z → **코드는 적재됐다.** 테스트 `22 passed`.

```bash
# ⚠️ 실자금 DB 쓰기 — 사장님 판단 후 실행
ssh root@159.65.137.250 'cd ~/binance-auto-trader/backend && docker compose exec -T -e PYTHONPATH=/app api python -c "
from app.core.database import SessionLocal
from app.models.system_setting import SystemSetting
db = SessionLocal()
row = db.query(SystemSetting).filter(SystemSetting.key==\"support_score_gate_enabled\").first()
if row: row.value = \"1\"
else:   db.add(SystemSetting(key=\"support_score_gate_enabled\", value=\"1\"))
db.commit(); print(\"ON\")
"'
```

**🚨 켰을 때의 위험 — 무해한 장식이 아니다.**
이 시스템이 **실제로 매매한 심볼**(최근 5일 DB 기준 12종: AKE/SKR/PROM/USELESS/ONG/UNI/CYS/GIGGLE/BTR/FARTCOIN 등)로
**3,536봉을 리플레이**한 결과:

- 지지선 접촉률 **8.99%** (318건) — 대형주 표본(5.44%)보다 높다. 알트·밈 위주이기 때문.
- 접촉 시 점수 분포: `{0:39, 1:79, 2:62, 3:30, 4:27, 5:29, 6:20, 7:32}`
- **접촉 조건부 차단율: LONG 83.6% (266/318) / SHORT 62.9% (200/318)**
- 전체 진입시도 대비 실효 차단율: **LONG 7.52% / SHORT 5.66%**

→ **진입 시도 13~18건 중 1건을 실제로 끊는다.** 특히 **LONG 쪽이 훨씬 세게 맞는다**
(사장님 주력인 「급등 중 조정 LONG」이 지지선 접촉 자리에 몰리기 때문).
지금은 이미 새 진입이 극도로 적으므로, **진입을 회복시킨 뒤에 켜시는 순서를 권한다.**

**🚨 더 큰 문제 — 켤 방법이 아예 없다.**
`support_score_gate_enabled` 를 **쓰는 코드가 저장소에 0곳**이다. alembic 시드 없음, 워커 없음,
`PUT /settings` 의 `fields` 화이트리스트에도 없음(`sajangnim_*`, `pump_split_*` 등 고정 목록).
UI(`perp-terminal.html`)는 **상태만 읽어 「지지선 게이트: 켬/꺼짐/모름」 칩으로 표시**할 뿐 토글이 없다.
→ **위 DB 직접 쓰기가 유일한 수단**이다. 사장님 요구 「실행가능하게 개발해야해」 기준으로는 미완성이다.

**미구현 잔여**: 스펙 JSON 의 `cooldown_bars=6` 이 코드에 없다.

**부작용 위험은 낮다**(독립 확인): 최근 24h `current_stage=0` 로 차단된 19건이 **전부 STOPPED**, 비종료 좀비 0건.
같은 raise 차단 방식이 운영에서 이미 정리되고 있다 → 켜도 찌꺼기는 남지 않는다.

---

### (2) OBV 자동 진입 — 켜도 소용없다 ⚠️

`unified_entry_enabled='0'` 이 꺼져 있지만, **켜지 말 것을 권한다.**
그 워커에는 `reentry_alerts` 참조 0, `trigger_mode` 대입 0, `OBV_REVERSE` 생성 0 이므로
**켜도 OBV 자동 진입은 생기지 않는다.** 설정 문제가 아니라 **구조 문제**다(1순위 항목 ①·3순위 참조).

```bash
# ❌ 권하지 않음 — 켜도 OBV 진입이 생기지 않고, 다른 15m 진입만 열린다
# ssh root@159.65.137.250 '... unified_entry_enabled = 1 ...'
```

---

### (3) 죽은 설정 키 정리 (오독 위험)

| 키 | 현재 값 | 실제 의미 |
|---|---|---|
| `success_pyramiding_enabled` | `'0'` | 🚨 **죽은 키**. 엔진이 읽는 스위치는 `sajangnim_pyramid_enabled`(행 없음 = **ON**). 설정 테이블만 보면 피라미딩이 꺼진 것으로 **오독하기 딱 좋다** |
| `auto_bb_break_daily_limit` | `0` | **무해**. `position_limit.LIMIT_KEYS` 순서상 `sajangnim_top_short_daily_limit = 50` 이 먼저 잡혀 `0` 에 도달하지 않는다 |
| `auto_obv_enabled` | `'1'` | 🚨 **ON 으로 보이는데 아무 일도 안 한다**(실판독처 워커의 호출처가 주석) |

---

### (4) 화면에 없는 설정 — 사장님이 확인·변경할 수 없는 것들

| 키 | 값 | UI | API | 파급 |
|---|---|---|---|---|
| `sajangnim_pyramid_trigger_roi` | `2.0` | ❌ | ❌ | 🚨 **행이 삭제/공백이 되면 경고 없이 `5.0` 으로 복귀**한다(범위 밖일 때만 WARNING). 사장님 지시가 **DB 행 하나**에만 걸린 단일점 취약 |
| `entry_priority_abs_pct` | 10.0(기본) | ❌ | ❌ | Fix 328 우선 조건 |
| `entry_slot_tight_pct` | 70.0(기본) | ❌ | ❌ | Fix 328 빠듯 판정 |
| `entry_rank_top_n` | 50(기본) | ❌ | ❌ | 「상승 50 + 하락 50」 |
| `entry_chg24_gate_mode` | rank(기본) | ❌ | ❌ | 절대값/순위 전환 |
| `sajangnim_stage_gap_pct` | 1.5(하드코딩) | ❌ | ❌ | 사다리 단계 간격 |
| `stage_keep_notional_usdt` | 10(하드코딩) | ❌ | ❌ | 🩸 **1순위 문제의 핵심 파라미터** |
| `stage_min_trim_ratio` | 2(하드코딩) | ❌ | ❌ | 🩸 **1순위 문제의 핵심 파라미터** |
| `support_score_gate_enabled` | 행 없음 | 표시만 | ❌ | 위 (1) |

`chg24_entry_gate.py` 모듈 독스트링의 「전부 설정으로 뺀다 — 사장님이 언제든 끄고 값을 바꿀 수 있어야 한다」는
**지켜지지 않았다.** 범용 설정 PUT(`strategy_suggestions.py:1017`)은 `suggestion_*` 4키 고정이다.

---

## ⚠️ 아직 실행 증거가 없는 것

각 항목에 **무엇을 관찰하면 검증되는지** 구체적으로 적는다.

### (A) Fix 326 — 부분 손절 잔량이 살아남는가 (최우선 관찰 대상)

- **현재**: `Fix326` 문자열이 scheduler **전체 로그(1.4GB, 08-22~현재)에 0회**.
  `FORCE_STOP_LOSS_TRIGGERED` 마지막이 08:02:07 UTC, Fix 326 배포(08:25) 이후 **0건**.
  `stopped_at >= 08:26` 인 전략도 **0건**.
- **검증 조건**: 부분 손절이 발생한 전략이 **다음 15초 사이클을 살아남아야** 한다.
- **관찰할 문구**:
  ```
  [Fix319] … 부분 손절: N 청산 / M 잔여 (명목 20.0x)
  [Fix319] 잔량 M 유지 — 전략 종료하지 않음 (다음 단계 트리거 대기)
  ← 그 다음 사이클에 「전량 손절 (skip)」이 나오지 않아야 성공
  ```
- **DB 확인**: 그 전략의 `status` 가 `STOPPED` 가 **아니고** `current_position_qty ≠ 0` 이어야 한다.
- **필요 조건**: 손절 트리거가 걸린 포지션 + **보유 명목 60 이상**(1순위 SKIP 구간을 넘어야 함).
- **주의**: 이게 성공하는 순간 **1순위 「잔량 영구 잔존」 문제가 현실화**된다. 두 개를 같이 봐야 한다.

### (B) 단계-진입 쪽 정리 — Fix 304 / Fix 316

- **현재**: `Fix316` 0회, 「진입 전 정리」/「정리 없이 진입」 0회 (scheduler·api 원본 로그 전 기간).
  `stage_trim_before_next_enabled` 가 켜진 시각은 **09-02 22:33:05**, 그 이후 **단계 2+ 진입 0건**.
  마지막 단계 2+ 진입은 09-02 21:44 (`#2032 AKEUSDT`).
- **검증 조건**: 사다리 전략이 **2단계 트리거에 도달**해야 한다.
- **관찰할 문구**: `[Fix316] … 다음 단계 진입 전 정리` 또는 `정리 없이 진입`
- **현재 후보**: `#2098 ZECUSDT` (3단계 사다리, 간격 1.5%) — **SHORT 이므로 가격이 +1.5% 오르면 2단계**.

### (C) Fix 322 / Fix 323 신규 절 — 현 설정에서 **도달 불가**

- **현재**: 「단계 사다리(stage_ladder)」 0건, 「손절 명시(ROI」 0건, fail-safe 경고 0건 (전 기간).
- **왜**: 판정 순서상 4번 `trim_enabled` 가 전역 ON 이라 **항상 먼저 True 를 반환**해 3·5번이 가려진다.
- **검증 조건**: `stage_trim_before_next_enabled=0` 으로 **끈 상태**에서 손절이 걸려야 한다.
  (⚠️ 끄면 299건이 잠기므로 **실운영에서 시도하지 말 것**. 스테이징 또는 단위 테스트로만.)
- **`stage_ladder` 마커 면제**: `#2098` 이 `capital_management_mode='stage_ladder'` 이므로
  **이 전략이 손절에 걸리면 Fix 315 마커 면제가 처음 발동할 수 있다.**
  관찰할 문구: `단계 사다리(stage_ladder)` — 지금까지 0회.

### (D) OBV 자동 2단계 진입 — 표본 자체가 없다

- **현재**: 살아있는 `OBV_REVERSE` **0건**(살아있는 25건은 전부 `PRICE_DOWN_PCT`).
  OBV_REVERSE 이력 81건 중 `current_stage` 분포 = 0:3 / 1:75 / 2:3, 누적 **-3,112.41**(건당 -38.42).
- **검증 조건**: OBV_REVERSE 전략이 새로 생성돼야 하는데, **3순위 문제 때문에 자동 생성 경로가 없다.**
  → 사장님이 수동(`_quick_`)으로 OBV 전략을 만들지 않는 한 **자동으로는 영원히 검증되지 않는다.**
- **관찰할 문구**: `[stage-trigger Fix173 OBV] 🎯 진입 신호!`
  (참고: `⏳ 대기 … 진입창 차단: 볼밴 위치 1.185 > 1.00` 는 07:58~08:00 에 실제로 찍혔다.
   **진짜 병목은 retry 분기가 아니라 OBV 진입창 게이트**다.)

### (E) Fix 328 빠듯 분기 — 운영 발동 0회

- **현재**: 슬롯 **9/50 = 18%** 여유모드라 70% 조건에 안 걸린다. 오늘 Fix 328 은 **사유 문자열만 바꾸고
  진입 대상은 바꾸지 않았다.**
- **도달 가능성은 확인됨**: 14일 시간별 동시보유 최대 **47**(= 94% ≥ 70%).
  단 이 47 은 `stopped_at` 기록된 진입행만 센 것이고 `stopped_at NULL` 종료행 126건이 빠져 있어 **하한값**이다.
- **검증 조건**: 동시보유가 35건(70%)을 넘어야 한다.
- **관찰할 문구**: `슬롯 N/50 (70% >= 70%) 빠듯 → |24h| >= 10% 만 받는다 (사장님 「가능하면 10%」)`
- **경계는 실측 완료**(러닝 프로세스 + 실시세 761심볼, 주문 없음):
  ```
  34/50 = 68% → (True,  '상승 21위 (24h +9.80%) — 모니터링 50+50개 안 (슬롯 34/50 여유)')
  35/50 = 70% → (False, '… 슬롯 35/50 (70% >= 70%) 빠듯 → |24h| >= 10% 만 받는다')
  40/50 + MARSCOINUSDT(+65.17%) → (True, '상승 1위 — 🌟 우선 (|24h| >= 10%)')
  BTCUSDT(+1.86%) → (False, '상승/하락 각 50위 밖')
  '_quick_'       → (True,  '수동 진입 (게이트 미적용)')
  ```

### (F) Fix 303 제외 게이트 — 12일간 **한 번도 발화한 적 없다**

- **현재**: scheduler 406만줄 + api 711만줄(08-22~09-03)에서 `Fix303` = **0**, `제외 심볼` = **0**.
- **왜**: **Fix 270 이 먼저 막는다.** 재현 확인:
  ```
  08:00:09,789 정점확인 OK
  08:00:09,820 [Fix270/4H] ⛔ BTCUSDT SHORT 차단
  08:00:09,822 ❌ 진입 실패
  ```
- **판정은 그래도 ✅**: 게이트 자체는 **두 컨테이너에서 직접 실행해 11종 전부 BLOCK** 확인했고,
  DB 상 오늘 신규 31건 중 제외 심볼 0건이다. **「막을 일이 없었을 뿐」이다.**
- 🚨 **단, Fix 330 이 Fix 270 을 「참고」로 바꿨으므로 이제 Fix 303 이 최전선이 된다.**
  BTCUSDT SHORT 알람이 살아 있으면 **Fix 303 로그가 곧 나타날 것**이다. 관찰 권장.

### (G) Fix 331 의 효과 — 30분 표본밖에 없다

- **실행은 확인됨**: `[Fix247] ✅ CRVUSDT SHORT 합의 참고: AVOID (score=0) — 반전 전략이라 막지 않음 (Fix 331)`
  최근 20분 **11회**. Fix 330 은 **41회**.
- **첫 성과도 확인됨**: `#2098 ZECUSDT` 진입 (위 자연 실험).
- **미검증**: 이게 **수익으로 이어지는지**. 표본 1건이다. 성과 판단은 **금지**한다.

### (H) ② 추가 진입 2% 의 성과 — 반나절 표본

- 5% 미만 발동 **10건**은 반나절 표본이라 **아직 아무 결론도 낼 수 없다.**
- 병목이 **트리거에서 지표게이트로 옮겨갔을 뿐일 수 있다**: 로그 사유 집계에서
  `indicator_not_rising`(Fix 273)이 계속 잡고 있다(MAGMA 2.07% 가 2% 관문은 통과했는데 여기서 막혔다).
  → 2% 인하의 실효는 겉보기보다 작을 수 있다. **설계된 안전장치이므로 결함은 아니다.**

---

## 새 진입이 안 되는 이유

### 결론: **(c) 과도한 게이트 — 고장이 아니라 「직렬 곱셈」 구조.** 그리고 **감사 중에 해소되기 시작했다.**

### 게이트별 실측 (scheduler 로그 24시간)

| 게이트 | 차단 | 통과 | 통과율 | 설정 | 위치 |
|---|---|---|---|---|---|
| Fix 111b 정점확인 | 10,885 | 316 | 2.8% | ON | `pump_top_detector` |
| Fix 259 OBV 회복률 | 69,312 | 미측정 | — | ON | `auto_long_at_bottom` |
| **Fix 274 LONG 급등** | **2,695** | **5** | **0.19%** | `=1` ON | 공용 관문 1번 |
| Fix 270 4H 추세 | 6,149 | 788 | 11.4% (고유 심볼 **29%**) | `=1` ON | 공용 관문 2번 |
| Fix 247 합의 | 743 | 45 | 5.7% | `=true` ON | 공용 관문 4번 |
| Fix 310/325 chg24 | 76 | — | — | `=1` ON | `start_stage1` |
| **Fix 327 지지선** | **0** | — | — | **행 없음 = OFF** | `start_stage1` |
| 슬롯 상한 | 0 | — | — | **9/50 = 여유 41** | **병목 아님** |
| Fix 273 피라미딩 지표 | 1,685 | 17 | 1.0% | ON | 피라미딩 |

> 📌 **정정 1**: `trend_4h_gate.py` 문서의 「21%」는 틀리지 않았다. 3.3%/11.4% 는 **로그 줄 수** 기준이고
> 매 사이클 같은 심볼을 반복 평가한 것이 분모에 들어갔다. **고유 심볼 기준 = 28/96 = 29%** 로 문서와 일치한다.
>
> 📌 **정정 2**: Fix 270 은 「모든 신규 진입」의 관문이 **아니다**. `check_trend_4h` 호출처는
> `auto_bb_breakdown_worker._create_auto_bb_strategy` **단 한 곳**이다.
>
> 📌 **정정 3**: `auto_bb_break_daily_limit = 0` 은 **무해**하다(위 「죽은 설정 키」 참조).

### 직렬 곱셈 — 공용 관문 `_create_auto_bb_strategy`

거의 모든 자동 경로(`auto_bb_breakdown` / `auto_long_at_bottom` / `auto_short_at_top` /
`bb_upper_breakout_short` / `pending_hc_fast` / `realtime_reentry` / v219)가 **이 함수 하나**를 지난다.

```
Fix 274(LONG) → Fix 270(4H) → Fix 251(되돌림) → Fix 247(합의)
  → start_stage1( Fix 303 제외 → Fix 310/325/328 chg24 → Fix 327 지지선 )
```

24시간 누적: SHORT 약 **6,872건 → 788 → 45 → 자동 진입 9건**. 곱하면 **약 0.1%**.

**LONG 은 사실상 사망**: Fix 274 가 2,700건 중 **5건**만 통과시켜, 24시간 동안 Fix 270 에 도달한 LONG 이
**1건**뿐이었다(차단 6,084건이 전부 SHORT).

### 왜 `bb_mid_line` 만 들어왔나 (오늘 31건)

**공용 관문을 지나지 않기 때문이다.** `bb_mid_line_worker` 는 `surge_ladder_entry.create_surge_position` 으로
진입하고, `trend_4h_gate` 는 `check_hist_rising` 으로 **참고 기록만** 한다(`ref[tf]`) — 막지 않는다.
`surge_ladder_entry.py` 는 도입부에 「왜 공용 관문을 쓰지 않는가」를 명시하고 있다.
→ **게이트 5중을 통째로 우회한 유일한 경로**라서 혼자 살아남았다.

### 🆕 Fix 330 / Fix 331 이 배포됐고, **작동한다**

**Fix 330** (4H 를 「거부권」→「참고」로, 반전 전략 예외) — 12:21 UTC 배포. 재시작 후 50분 실측:

| 게이트 | 차단 | 통과 | 통과율 |
|---|---|---|---|
| Fix 270 | 138 | 61 | **30.7%** (11.4% → 개선) |
| Fix 274 | 74 | 0 | 0% |
| Fix 247 | 61 | 0 | **0%** ← 병목 이동 |

Fix 270 통과 61건 중 **34건**이 `반전 전략이라 막지 않음 — Fix 330` 으로 찍혔다.
그런데 Fix 330 이 풀어준 반전 심볼 3개(`TSLAUSDT`/`UNIUSDT`/`ZECUSDT`)가 **전부 Fix 247 에서 다시 막혔다**(3/3).
Fix 247 차단 사유는 71건 전부 `합의 판정 AVOID (score=0)`, 24시간 점수 분포도 `score=0` 이 **754/856건**이다
— 추세추종 지표 2개의 합의이므로 **반전 진입에는 구조적으로 0 이 나온다.**

**Fix 331** (합의 게이트도 「참고」로) — **12:34:48 UTC 배포·기동, 20분간 11회 실행 확인.**
그리고 **12:35:24 에 `#2098 ZECUSDT` 진입 성공** — 같은 심볼이 4분 전까지 30초마다 Fix 247 에 막히던 것이 풀렸다.
**v219 정점 SHORT 최근 7일 진입 0건 → 첫 진입.** 사장님 사상 ①의 출발점이 드디어 열렸다.

### 🆕 병목이 다시 이동했다 — 이제 Fix 310/325 (순위 게이트)

배포 직후 관찰 (12:37~12:41, 4분):

```
12:37:55 [Fix310] CRVUSDT #2104 1단계 진입 차단 — 24h +1.47% — 상승/하락 각 50위 밖
12:38:26 [Fix310] CRVUSDT #2105 1단계 진입 차단 — 24h +1.52% — 상승/하락 각 50위 밖
12:38:56 [Fix310] CRVUSDT #2106 …  (30초마다 반복, #2104 ~ #2111)
```

**해석**: Fix 330/331 이 앞단을 열자 트래픽이 `start_stage1` 까지 도달하기 시작했고,
거기서 **순위 게이트가 새 최전선**이 됐다. CRVUSDT(+1.47%)는 상승/하락 각 50위 밖이라 정당한 차단이다.

**부작용**: 차단 시 전략 행이 **이미 커밋된 뒤** 예외가 나므로, 30초마다 전략 행이 생성→삭제된다.
다행히 **좀비 정리는 작동한다** — 오늘 `current_stage=0` 고아행은 **18건**이고 전부 02:07~05:07 UTC 의 옛것이며,
#2104~#2111 은 DB 에 남지 않았다. 다만 매 사이클 `[템플릿+전략 생성/커밋 → ValueError → rollback →
Fix 162 좀비확인 거래소 2회 조회 → 행 삭제]` 가 반복된다. **자금은 안전하지만 DB·API 낭비가 실재한다.**

### 🚨 오늘 「하락 50위」가 -0.23% 다 (항목 ⑧ 관련)

5M 거래대금 통과 풀 248개 중 `|24h| >= 10%` 는 **31개뿐**인데, 슬롯이 18%(9/50)라 Fix 328 우선 조건
(70% 이상일 때만 발동)이 걸리지 않아 **-0.2% 보합 종목에도 신규 진입이 허용된다**(248개 중 100개 = 40% 통과).
저장소 메모리의 실측 기록은 **±5% 에서 구분력이 완전히 사라지고 10% 가 진짜 경계**라고 말한다.
사장님 문자(「상승 50 + 하락 50」)에는 맞지만, **사상 ⑧(급락 = 이전 급등의 반작용)의 뜻과는 내용상 어긋난다.**

### 🚨 「100개」는 사실 「거래대금 5M 이상 중 100개」다 (사장님께 보고된 적 없음)

`market_movers.py` Fix220 주석의 「사장님이 잡고 싶어 하는 급등 종목을 걸러내지 않는다」가 **실측으로 반증**됐다.
5M 하한이 제외하는 것:
- **진짜 상승 50위 중 14개** — TAG +15.66%(2.3M), AIO +12.86%(4.2M), COAI +11.75%, NIGHT +11.38%(4.8M) 등
- **진짜 하락 50위 중 22개** — RECALL -15.74%(진짜 하락 6위) 등

→ **상위 50위의 28%가 감시망 밖**이다.

### 게이트를 막는 것이 이득인가

문서의 실측(게이트 없음 158건 **-3,599.87** = 건당 **-22.78** / 채택안 33건 **+183.32** = 건당 **+5.56**,
SHORT **-30.16 → +12.74**)은 **그대로 유효**하고 과적합 검사도 양쪽 절반 양수다.
🚨 **무작정 Fix 270 을 끄면 건당 -22.78 로 돌아간다. 권하지 않는다.**

다만 그 측정은 **다른 게이트가 없던 시절** 표본이고, 지금은 Fix 111b(2.8%)·Fix 259·Fix 310/325·Fix 247 이
앞뒤로 겹쳐 있다. 그리고 **정점 SHORT 는 정의상 4H 가 아직 안 꺾인 자리를 잡으므로 4H 통과 조건과 논리적으로 모순**이다.
→ 「전면 개방」이 아니라 **반전 계열만 예외**로 두는 Fix 330/331 방식이 옳은 절충이다. 그리고 그것이 방금 배포됐다.

### 선택지와 예상 효과

| 안 | 내용 | 근거 | 예상 효과 | 되돌리기 |
|---|---|---|---|---|
| **A** ✅ **이미 실행됨** | Fix 330 + Fix 331 배포 | 반전 3심볼이 Fix 247 에 100% 막혔고 그것이 그대로 풀림 | **실측**: v219 정점 SHORT 7일 0건 → 12:35 첫 진입. 재시작 50분간 Fix 270 통과율 11.4%→30.7% | `confluence_reversal_exempt = 0` / `trend_4h_reversal_exempt = 0` 한 줄 |
| **B** | Fix 327 지지선 게이트 ON | 오늘 개발 요청분, 코드 정상 | 🚨 **진입을 더 줄인다** (LONG 접촉시 83.6% 차단). **A 로 진입을 회복한 뒤에** 켤 것 | 설정 `0` |
| **C** | Fix 274 재검토 | 통과율 **0.19%(5/2,700)** 로 LONG 사실상 전면 차단 | 근거가 얇다(채택 표본 20건). **A 배포 후 재측정 권장** | — |
| **D** | 아무것도 안 함 | — | 손실은 억제되지만 v219 정점 SHORT 계속 0건, 자동 진입이 `bb_mid_line` 한 경로에만 의존 | — |

**현 시점 권고: A 는 이미 됐다. 24~48시간 관찰 후 C 를 재측정하고, 그 다음에 B 를 켠다.**

### 미검증으로 남긴 것 (정직하게)

- **Fix 259 의 통과 건수**를 못 셌다(차단 69,312건만 확인). 통과 로그가 별도 태그로 안 남는다.
- **Fix 303/325/328 은 24시간 로그 0줄**이다. 실패·차단 시에만 로그를 남기는 구조라
  **「정상 통과」와 「미실행」이 로그상 구분되지 않는다.** Fix 310 이 76건 차단을 남긴 것으로
  `chg24_entry_gate` 모듈 자체의 실행은 확인했으나, **Fix 325 순위 모드가 실제로 도는지는 별도 확인이 필요했다**
  → 12:37~12:41 의 `상승/하락 각 50위 밖` 로그로 **이제 확인됨**(순위 모드 실동작).
- **Fix 330/331 의 수익 효과**는 표본 1건이라 **판단 불가**.

---

## 각 요청별 상세

### ① OBV 자동 로직이 실제로 작동하는가 — 🚨 고장

**반증 결과**: 반증 실패. 원 판정 「고장」 **유지**. 독립 재현한 수치가 하나도 틀리지 않았고,
새로 판 두 각도(설정꺼짐 가설, 알람→진입 가설)는 오히려 판정을 **강화**했다.

**실측**:
- OBV_REVERSE 인스턴스 **81건** (STOPPED 73 / COMPLETED 8), `current_stage` 0:3 / 1:75 / 2:3
- 누적 **-3,112.41** (건당 -38.42)
- `stage_no >= 2 AND is_triggered = true` = **3건**
- **살아있는 OBV_REVERSE = 0건** (살아있는 것은 전부 `PRICE_DOWN_PCT`)
- 템플릿 **87건 전부 `_quick_`**(수동)
- 설정: `auto_obv_enabled='1'` / `auto_bb_breakdown_enabled='0'` / `unified_entry_enabled='0'`

**「주석을 호출로 오인」 함정 직접 확인 — 원 판정이 옳다**:
`stage_trigger_worker.py:822-840` 이 `check_stage_entry_signal` 을 **실호출** → `should_fire=_sig_ok`
→ `if _sig_ok:` 안에서 `[stage-trigger Fix173 OBV] 🎯 진입 신호!` 를 찍는다.
성공 로그는 진짜 그 판정 함수가 낸 것이다 → **(A) 「단계 진입 판정 작동」은 진실.**

반대로 `reentry_alert_watcher.py:194` 의 `auto_obv_enabled` 는 **순수 주석**이다.
실판독처는 `auto_bb_breakdown_worker.py:341` 단 하나(+ API 설정 화면 878/900).

**「설정꺼짐」 가설 이중 반증** — 위 3순위 항목 참조.

**반증관의 새 발견**: 알람→자동진입은 **의도적 미구현**이다(3순위 참조).
즉 원 감사관이 「전부 fail-silent 버그」로 서술한 것 중 알람→진입 구간은 **절반이 설계상 수동**이고
화면은 그 사실을 정직하게 표시하고 있다. 다만 `auto_obv_enabled` 는 여전히 **ON 으로 읽히면서 아무 일도 안 하는
죽은 설정**이므로 「고장」 라벨 자체는 유지된다.

**판정 라벨 근거**: 작동확인 아님(자동 신규 진입 30일 0건) / 미검증 아님(「작동 안 함」이 검증됨) /
설정꺼짐 아님(이중 반증) / 미구현 아님(판정 로직은 실증됐고 자동진입 코드도 `auto_bb_breakdown_worker.py:341-423` 에 존재)
→ **구현·배포됐고 스위치는 ON 으로 보이는데 구조적으로 도달 불가 = 고장.**

**정정**: 원 감사관의 "현 배포본에서 OBV 경로는 한 번도 안 돌았다"는 **과한 표현**이다.
`check_stage_entry_signal` 은 `stage_trigger_worker.py:1050`(Fix218) 볼밴 2·3차 경로에서
`PRICE_DOWN_PCT` 전략에도 쓰이므로 게이트 코드 자체는 오늘도 실행될 수 있다. 결론은 바뀌지 않는다.

---

### ② 추가 진입 트리거 ROI +5% → +2% — ✅ 작동확인

**반증 결과**: 반증 실패. 원 판정 **유지**. 오히려 앞선 감사관보다 **강한 증거**를 확보했다.

**반증 4건 전부 실패**:
1. **「그 로그를 찍는 코드가 정말 그 함수인가」** → `grep -rn "트리거 ROI"` 전 저장소 결과
   `app/workers/success_pyramiding_worker.py:868` **단 한 곳**(나머지 2건은 같은 파일 주석/변수, 1건은 테스트).
2. **「배포된 파일이 내가 읽은 파일과 다른가」** → VPS 컨테이너 `/app/app/workers/success_pyramiding_worker.py`
   md5 `ee65eeb5fe6381b5541c5166b07d8480` == 로컬 HEAD 정규화 md5 **완전 동일**.
   scheduler 기동 > 파일 mtime 09-02 18:19:17Z → 현재 프로세스가 Fix 300 코드를 들고 있다.
3. **「주석 인용을 호출로 오인 / 죽은 분기인가」** → 실제 게이트는 line 548 `if roi_pct < _trig` 하나뿐,
   `_trig` 는 line 369 에서 사이클마다 `_trigger_roi(db)` 로 새로 읽는다.
   후보 쿼리(line 404~440)에 **ROI 사전필터가 없다** → 게이트 우회 경로 없음.
   하드코딩 `5.0` 은 `MIN_UNREALIZED_ROI_PCT`(fallback 기본값) 하나.
4. **운영 컨테이너에서 그 함수를 직접 호출**:
   ```
   === _trigger_roi() called with the LIVE app session ===
     -> 2.0
   ```
   = 「설정이 있다」가 아니라 **엔진이 쓰는 그 함수가 지금 2.0 을 반환한다** (증거유형 ④의 최상급).

**산술 대사(對査)까지 맞는다**: 운영 DB 에서 워커와 **똑같은 쿼리+제외로직**으로 후보 재현 → 도달가능 5건
```
#2005 BMNR roi=-1.55 / #2009 MSTR -2.82 / #1988 TQQQ -0.73
#2093 BRUSDT roi=2.60 used=2/2 / #2090 MAGMA roi=2.07 used=0/2
```
같은 시각 운영 로그(11:24~11:26):
```
완료: entered=0 skipped=5 | roi_below_trigger=3 max_pyramid_count=1
      chg24_extreme_allowed=1 indicator_not_rising=1 (트리거 ROI>=2.0% …)
```
→ 음수 3건 = `roi_below_trigger` 3, BRUSDT 상한 = `max_pyramid_count` 1,
**MAGMA 2.07% 는 2% 관문을 통과해 하류 지표게이트까지 갔다**(`indicator_not_rising` 1). **5 = 3+1+1 완전 일치.**
5.0 이었다면 MAGMA 는 `roi_below_trigger` 가 됐어야 한다 = **값이 실제로 판정을 바꾼 것이 한 건 단위로 확인된다.**

**JSON 필드로 재검, 주문 매칭까지**: `strategy_config->>'roi_pct_at_entry'`(텍스트 파싱 아님)로 재집계 →
설정 전 **159건** min 5.006 / **5% 미만 0건**, 설정 후 **14건** min **2.045** / **5% 미만 10건**.
설정 후 14건 **전부** ±30초 내 `status='FILLED'` 주문 1건씩 매칭(빈 기록 아님).

**단위 테스트**: `pytest tests/test_pyramid_trigger_roi.py` → **10 passed**.

**그가 놓친 문제**:

**A. 🚨 2% 가 닿는 범위는 라이브 9건 중 5건뿐이다.** 4건이 정적 제외:
`split_entry` 1건(EGLD, Fix 213), `single_entry` 3건(CRDO/PYTH/ARC = `bb_mid_line`, Fix 282).
그런데 **Fix 282 의 코드 주석이 밝힌 제외 사유가 바로 그 5%/5% 충돌**이다 —
「발동선이 ROI +5% 인데, 이 전략들의 TP1 도 +5% 다. 익절해야 할 바로 그 지점에서 300 USDT 추가 매수가 나간다」.
**트리거가 2% 가 된 지금 그 사유는 소멸**했는데, 제외는 `_trig` 를 한 번도 참조하지 않는 `frozenset` **하드코딩**이다.
`bb_mid_line` 만으로 라이브의 1/3(3/9). 사장님 「+2% 부터 진행」이 이 3분의 1에는 닿지 않는다.
(열지 말지는 사장님 판단 — 「1회 진입 전략은 남이 손대지 않는다」는 별개 근거는 아직 유효)

**B. 🚨 화면·API 는 아직 5.0 이라고 말한다.** `app/api/v1/admin/monitoring.py:1602` 가
`MIN_UNREALIZED_ROI_PCT as _TRIG` 로 **기본 상수를 import** 해 `trigger_pct: 5.0`,
`reason: "ROI 1.33% < 트리거 5.0%"` 를 낸다. `app/static/` 전체 grep 결과 이 엔드포인트를 **소비하는 화면이 없다**
→ 지금은 모니터링 API 한정 거짓말이지만, **이걸 붙이는 화면이 생기면 그대로 사장님께 옛 값을 보여준다.**
(Fix 188~194 「모름이 꺼짐으로 표시된 fail-OFF」와 같은 계열)

**C·D**: 위 「⏸ 꺼져 있어서 안 도는 것 (3)·(4)」 참조.

**E. 병목이 트리거에서 지표게이트로 옮겨갔을 뿐일 수 있다** — 위 「⚠️ (H)」 참조.

**F. 성과 판단 금지.** 5% 미만 발동 10건은 반나절 표본이다.

---

### ③ 부분 손절 — 모든 단계에서 10 USDT 남기기 — ⚠️ 배포됐으나미검증

**반증 결과**: 반증 실패. 원 판정 **유지**. 다만 **더 강한 증거로 굳어졌고, 놓친 문제 4건을 새로 찾았다.**

**배포는 확정** (md5 로 재검): scheduler 컨테이너 안
`/app/app/services/tp_sl_orchestrator.py` = `d54b622d2b4a115924d0feb4bd58310a`,
`/app/app/services/stage_trim.py` = `d63cbba58de96fa1a88698477c953ad5` — 로컬 파일의 LF 정규화 md5 와
**바이트 단위로 완전 일치**.

**로그를 「24h」가 아니라 「전 기간」으로 다시 뒤졌다** (docker json 원본, scheduler 1.4GB / api 1.28GB):

| 문자열 | scheduler | api |
|---|---|---|
| `Fix326` | **0** | **0** |
| `Fix316` | **0** | **0** |
| `진입 전 정리` / `정리 없이 진입` / `Fix306` | **0** | **0** |
| `Fix319`/`Fix318` | 24줄 (마지막 08:02:09) | — |

→ **Fix 326 은 물론 단계-진입 쪽 정리(Fix 304/316)도 배포 이래 한 번도 실행된 적이 없다.**

**DB 다른 각도 — 역시 없다**:
- `risk_events` `FORCE_STOP_LOSS_TRIGGERED` 오늘 19건, 마지막 08:00:22 UTC. **08:26(Fix326 기동) 이후 0건.**
- `stopped_at >= 08:26` 인 전략 **0건**. 약 3시간 무발동.
- 단계 2+ 진입 주문 마지막 = 09-02 21:44 (`#2032 AKEUSDT`).
  `stage_trim_before_next_enabled` ON 시각 = **09-02 22:33:05** → 설정 ON 이후 단계2+ 진입 **0건**.
  **「다음 단계 진입 전 10 USDT 남기고 정리」는 표본 자체가 존재하지 않는다.**
- 설정 재확인: `trim_enabled(db)=True`, `keep_notional=10`, `min_trim_ratio=2`
  (`stage_keep_notional_usdt` / `stage_min_trim_ratio` 행 없음 = 코드 기본값).
  전역 force_sl LONG/SHORT = (True, 80), 개별 override 3.64~10.
- 테스트: 94건 + `tests/test_stage_flow_execution.py` = **105 passed**.
  실행 테스트(가짜 client 로 `emergency_close_position` 수량 가로채기)라 Fix 318→319 급 사고는 잡힌다.
  다만 거래소·실시세·실 DB 는 안 탄다.

**새로 찾은 문제 4건**:

**(A) 🚨 손절 지연이 아니라 「손절 영구 무효」다** → **1순위** 항목 참조 (현재 노출 #1988).

**(B) 🚨 「다음 단계 대기」의 전제가 절반만 성립한다.**
오늘 손절된 10건 중 **5건이 단계 1개짜리**(#1853 #1989 #2016 #2045 #2064).
최근 3일 생성 148건 중 **단계 1개가 90건(61%)**. 단계가 하나면 기다릴 다음 단계가 없다.
게다가 **지금 계정에 미체결 주문이 0건**이라, Fix 319 가 부분손절 때 `cancel_all_orders` 를 건너뛰는 근거로 든
「다음 단계 트리거 LIMIT」은 **실제로는 존재하지 않는다**(단계 진입은 `stage_trigger_worker` 가 그때 발주한다).

**(C) 🚨 SKIP 으로 return 할 때 RiskEvent 는 이미 기록된 뒤다.**
`risk_service.evaluate_force_stop_loss` 는 판정이 True 면
`RiskEvent(FORCE_STOP_LOSS_TRIGGERED, CRITICAL, 본문에 「→ 전량 강제 청산 + 전략 종료」)` 를 `db.add + flush` 한 뒤
True 를 돌려주고, `_execute_force_stop_loss` 는 SKIP 이면 **commit 전에 return** 한다.
tp_sl 주기는 **15초**이고 `run_workers` 가 세션 하나를 모든 전략에 공유한다. 따라서 잔량 상태로 멈춘 포지션은
① 같은 사이클의 다른 전략이 commit 할 때마다 **「전량 청산했다」고 적힌 CRITICAL 이벤트가 쌓이거나**
(최대 하루 5,760건, 내용은 사실과 다름) ② 아무도 commit 안 하면 **조용히 롤백되어 DB 흔적이 전혀 남지 않는다.**
매 사이클 기록되는 것은 실측된다 — `#2089` 는 07:44:00 과 07:44:15 로 **15초 간격 연속 2건**이 남아 있다.

**(D) ⚠️ 부분 손절 체결마다 `EXIT_FULL_CLOSE_MISMATCH`(WARN)가 뜬다.**
오늘 부분 손절 3건(#2089 #2064 #2091) **전부**에서 체결 직후 `stream_service` 가 「DB 는 잔량 0 으로 판단」했다가
거래소 재조회로 정정했다. 정정되니 사고는 아니지만, **그 재조회가 실패하면 `EXIT_FULL_CLOSE_UNVERIFIED` 로 빠져
판정이 보류된다 = 잔량 보존이 거래소 API 응답에 의존**한다.

**(E) 참고**: api/scheduler 는 07:56 / 08:25 / 08:51 / 10:41 / 11:18 / 12:21 / 12:34 UTC 로
**30~90분마다 재기동**되고 있다(api 「Started server process」 누적 170회).
배포 신선도는 보장되지만, **재기동마다 실행 증거를 다시 모아야 한다.**

**결론**:
- 「부분 손절 주문이 20.00 USDT(= 증거금 10 × 레버 2) 남기고 나간다」 = 오늘 **7건 실행, 작동확인**(Fix 319/324)
- 「그 잔량이 살아남아 다음 단계로 이어진다」 = 오늘 실적 **0/7**(전부 STOPPED, qty 0),
  이를 고치는 **Fix 326 은 배포·설정 ON·105 테스트 통과이나 운영 실행 0회**
- 「모든 단계에서」의 단계-진입 쪽 절반(Fix 304/316)은 설정 ON 이후 **실행 표본 0건**

→ 항목 전체로는 **배포됐으나미검증**. 단 (A)·(B)는 검증 대기가 아니라 **지금 열려 있는 설계 결함**이다.

---

### ④ BTC/ETH 계열 11종목 제외 — ✅ 작동확인

**반증 결과**: 반증 실패. 원 판정 **유지**. 오히려 강한 증거를 얻었다.

**1. scheduler 컨테이너에서도 검증** — 원 감사관은 api 만 봤다. **실제 진입 주문은 scheduler 가 낸다.**
두 컨테이너 모두에서 직접 실행:
- `excluded_symbols(db)` = **11종 동일**, `SystemSetting('excluded_symbols')` row = **None**(코드 기본값 유효)
- `_assert_symbol_allowed`: 사장님 9종 + 분기선물 2종 **전부 BLOCK**, SOLUSDT/DOGEUSDT PASS
- 추가 케이스: `'btcusdt'`(소문자) BLOCK, `' BTCUSDT '`(공백) BLOCK, `''`/`None` BLOCK(**fail-SAFE**)

**2. 「디스크 파일」이 아니라 「실행 중 클래스 객체」를 파싱했다.**
Fix 318 함정을 배제하려고 `inspect.getsource(ExecutionService)` 로 **런타임에 로드된 클래스**를 AST 파싱
→ 게이트를 부르는 메서드 = `_place_stage_entry_order` / `_place_market_entry` / `_place_limit_entry` **정확히 3개**.
셋 다 **본문 첫 문장**이 게이트(도달 전 우회 불가). 중복 정의(Fix 202 함정) **0건**.

**3. 앱 전체 우회 경로 AST 전수 — 실제 주문 발사 13곳.**
entry 4곳은 전부 게이트 3함수 안. 나머지는 `_emergency_close_position_locked`(508/517) /
`_verify_emergency_close_applied`(786) / `_emergency_close_limit_fallback`(1782) = **전부 청산**.
`add_position_now`(수동 💉 포지션 추가)는 1477→`_place_market_entry`, 1491→`_place_limit_entry` 로 위임 = **커버 확인**.

**4. DB 행위 증거** — 오늘(09-03 UTC) 전체 신규 전략 **31건**(00:33~10:56) = 시스템은 활발히 진입 중.
그중 제외 11심볼 = **0건**. 비종료 상태 0건, 주문 0건. 제외 심볼 마지막 진입은 **2026-08-25**(Fix 303 이전).

**5. 로그 12일 전수** — `Fix303` = **0**, `제외 심볼` = **0**. 원인은 Fix 270 이 먼저 막았기 때문(위 ⚠️(F) 참조).

**6. 로컬 테스트** `pytest tests/test_symbol_exclusion.py -q` → **17 passed**.

**🚨 원 감사관이 놓친 진짜 결함 — `drop_excluded()` 가 죽은 코드다** → **7순위** 항목 참조.

**부차 위험 — 게이트가 DB 커밋 「뒤」에 있다.**
`_create_auto_bb_strategy` 는 `db.add(tpl)` → `flush` → 전략 생성 → `db.commit()` **여러 번** 수행 후에야
`start_stage1`(2029행)을 부른다. 즉 게이트는 **행이 이미 커밋된 뒤** 예외를 던진다.
Fix 270 은 주석에 스스로 「fail-open — 안전장치가 아니다」라고 적혀 있으므로, 4H MACD 부호가 뒤집히면
30초마다 [템플릿+전략 생성/커밋 → ValueError → rollback → Fix 162 좀비확인 거래소 2회 조회 → 행 삭제] 가 반복된다.
**자금은 안전하지만 DB·API 낭비가 실재한다.** (12:37~12:41 에 CRVUSDT 로 실제 관측됨)

**범위 초과 1건**: 사장님이 나열한 9종 외에 `BTCUSDT_261225`, `BTCUSDT_260925`(분기선물)를 개발자가
같은 사유(stepSize 0.001 → 최소 잔량 ≈78 USDT)로 추가했다. 근거는 docstring 에 있으나 **지시 범위 밖**이다
(메모리 「사장님 요구 이외 기능 추가 금지」).

---

### ⑤ v219 사다리 — 1단계 10은 손절없이, 2단계부터 부분손절 — ⚠️ 배포됐으나미검증 (부분 승격)

**반증 결과**: 반증 실패, 원 판정 **유지**. 다만 반증관이 **원 감사관의 「Fix319 로그 0회」를 뒤집었고**,
그 뒤 **감사 도중 `#2098` 이 3단계 사다리를 처음 실행**해 절반이 승격됐다.

**🆕 승격된 부분 — 3단계 사다리 구성은 실행 확인됐다 (12:35:24 UTC)**

```
[Fix315] ZECUSDT SHORT 단계 사다리 [10.0, 300.0, 600.0] (간격 1.50%) — 총 910 USDT
```
DB: `capital_management_mode='stage_ladder'` — **전 기간 분포에서 단 1행**
(`fixed` 1413 / `split_entry` 69 / `auto_deduct` 5 / **`stage_ladder` 1** / `scheduled` 1).
템플릿 3305: `stages_config = {'capitals':[10.0,300.0,600.0], 'stages_count':3, 'trigger_percents':[None,1.5,1.5]}`,
`stage1_capital=10 / stage2_capital=300 / stage3_capital=600`, `total_capital=910`.
→ **사장님 사다리 숫자가 그대로 실행됐다.**

**원 감사관의 「Fix319 로그 0회 = 실행 증거 없음」은 사실이 아니었다.**
그는 `docker compose logs --since 6h`(6만 줄)만 봤다. 컨테이너 원본 로그 파일(1.44GB, **12일치**)을 직접 grep 하면:
`Fix319 = 24줄` / `Fix304 = 13줄` / `Fix315 = 0` / `Fix316 = 0` / `Fix326 = 0` / `stage_ladder = 0`
→ 「2단계부터 부분손절」의 실행 메커니즘은 **운영에서 실제로 7번 돌았다**(위 5순위 표 참조).
**그런데 7/7 전부 다음 사이클이 전량 청산해 되돌렸다.**

**「1단계 10은 손절없이」도 실행 증거가 있는데 정반대로 실행됐다** → **6순위** 항목 참조.

**라이브 컨테이너에서 실제 함수를 호출해 확인(코드 낭독 아님)**:
```
ladder=['10','300','600'] / stages_on=True / gap=1.5 / max_stage=3 / keep=10 / ratio=2
Fix315 분기 재현 → {'capitals':[10.0,300.0,600.0],'trigger_percents':[None,1.5,1.5],'stages_count':3} 총 910
compute_trim 실호출(XLMUSDT, 라이브 필터):
  1단계 명목 20   → act=skip, close=0     「남길 것이 없다」  ✅ 손절없이
  2단계 명목 620  → act=trim, close=1999 / keep=67(명목 20.10) ✅
  3단계 명목 1820 → act=trim, close=5999 / keep=67(명목 20.10) ✅
```
→ **현재 이미지의 로직은 사장님 사양대로 맞다.** 고장이 아니라 「돌 기회가 없었다」.

**남은 미검증**: **2단계 도달 0건.** 오늘 2단계 이상 도달 전략이 하나도 없고,
마지막 stage-trigger 발동은 09-02 21:44 이다. `#2098` 이 SHORT 이므로 **가격이 +1.5% 오르면 2단계**가 붙는다.
그때 `[Fix316] … 다음 단계 진입 전 정리` 가 찍히면 완주 검증이다.

**반증관이 새로 찾은 문제**:

**(A) 설정 3개가 DB 에 아예 없다** → 위 「⏸ (4)」 참조.
`system_settings` 실조회 결과 존재하는 키는 `sajangnim_capital_ladder` / `sajangnim_ladder_stages_enabled` /
`sajangnim_max_stage` / `stage_trim_before_next_enabled` **4개뿐**.

**(B) 🚨 증거금 30 USDT 미만 포지션이 강제손절에서 영구 면제된다** → **1순위** 항목.

**(C) 🚨 (B)의 파급 — 부분손절 잔량도 같은 구간이라 Fix 326 이후 전부 영구 잔존한다** → **1순위** 항목.

**(D) `stage_ladder` 마커 경로는 여전히 죽어 있다** (단, `#2098` 이 첫 후보가 됐다).
단계 게이트 면제가 실제로 도는 것은 Fix 304/317(`trim_enabled`) 경로다 —
`[force_sl Fix177/178] #2046/#1989/#2016/#2047/#2045/#1853/#2089/#2064 단계 정리(Fix304) = 단계 게이트 건너뜀`
**13회 실행 확인**. 즉 「사다리 마커로 게이트를 면제한다」는 Fix 315 설계 문장은 **아직 한 번도 근거가 된 적이 없다.**

---

### ⑥ 기본 방식과 OBV 자동을 완전히 구분 — ⚠️ 배포됐으나미검증

**반증 결과**: 반증 실패, 원 판정 **유지**. 다만 반증관이 **원 감사관의 핵심 근거 하나를 정반대로 읽었음을 밝혀냈다.**

**설정**: `stage_trim_before_next_enabled=1`(실조회). `trim_enabled()` 는 이 전역 스위치가 1이면
`split_entry` 를 뺀 **모든 전략**에 True 를 돌려준다.

- **Fix 322**: 살아있는 25건에 `_stage_gate_exempt()` 실호출 → 면제 사유 =
  **단계정리(Fix304) 23 / 청산후재진입 1 / 분할매수 1**. **Fix 322 절이 사유가 된 건수 = 0/25.**
- **Fix 323** (원 감사관이 놓쳤다): `git show fcd8462` 로 확인 —
  `if not _trim_on and status != 'LIQUIDATED_WAITING_RETRY'` 는 **Fix 313 시절부터 이미 있던 코드**이고
  Fix 323 은 `and not _sl_explicit` 만 끼워 넣었다. retry=True 전략(`#2093`)에 옛 조건/신 조건을 둘 다 실행 →
  **옛skip=False, 신skip=False, 바뀜=0/1.** `trim_on=True` 가 이미 단락시키므로
  **Fix 323 신규 절도 현 설정에서 도달 불가**다. 원 감사관의 "#2093 → skip=False 확인"은 Fix 323 을 **전혀 검증하지 못한다.**

**🚨 원 감사관이 근거를 정반대로 읽었다 (가장 큰 발견)**
그는 `#2091`(OBV)·`#2095`(기본)의 08:00:22 / 08:02:07 force_sl 로그를
「두 방식 모두 손절 교착이 풀렸다 = 확인됨」의 증거로 제시했다.
그런데 **그 정확한 타임스탬프가 저장소 안에 「버그」로 기록되어 있다** — `tp_sl_orchestrator.py:578-581`:
```
07:55:00 #2091 부분 손절 → 184 잔여 ✅ / 08:00:22 「남길 것이 없다」 → 전량 청산 ❌
08:01:52 #2095 부분 손절 → 1173 잔여 ✅ / 08:02:09 「남길 만큼 크지 않다」 → 전량 청산 ❌
```
실서버 로그 원문으로 재확인했고, 오늘 이 패턴이 **7건** 발생했다.
게이트는 열렸지만 결과는 전건 **전량 청산 + 전략 종료**로, 사장님 사양의 정반대다.
DB 도 일치 — #2091·#2095 둘 다 `status=STOPPED`, `current_stage=1`.

**나머지 발견**:
1. **Fix 323 의 전제가 실측으로 반증됨.** OBV 2단계 도달 3건(#1728·#1731·#1843)은 **전부 retry=False**
   — retry 분기가 적용된 적조차 없다. OBV_REVERSE 81건 중 retry 분포는 False 50 / True 31 이라
   「Fix 177 이 자동으로 켜서 막았다」는 서사가 성립하지 않는다.
2. 원 감사관은 "`stage_entry_signal` 로그 최근 3시간 창에 0건"이라 했지만, 로그 파일을 직접 뒤지니
   **Fix 323 배포 이후인 09-03 07:58~08:00 에**
   `[stage-trigger Fix173 OBV] ⏳ 대기: strategy=2091 stage=2 … 진입창 차단: 볼밴 위치 1.185 > 1.00` 가 **다수 존재**한다.
   즉 OBV 2단계 평가는 실제로 돌았고, **진짜 병목은 retry 분기가 아니라 OBV 진입창 게이트**다.
3. **Fix 326 은 런타임 증거 0.** 스케줄러 로그 전체에서 `Fix326` **0회**.
4. **사이클 전체가 한 번도 완결된 적 없다.** 오늘 2단계 이상 도달 **0건**, 현재 STAGE2+ 살아있음 **0건**.
   살아있는 것은 전부 `PRICE_DOWN_PCT` 이고 **OBV 라이브는 0건**이라 OBV 경로는 앞으로도 자동 검증되지 않는다.
5. **🚨 `force_sl_roi_override` 가 기계값으로 덮어써진다.** `execution_service.py:1671`(Fix 269)이
   포지션 추가 때마다 재계산해 `#2091`/`#2093` = **3.64**, `#2044`/`#2045`/`#1853` = **0.16**
   (레버 2x 에서 ROI -0.2% 에 손절)이 됐다.
   **Fix 322 의 면제 조건이 「사장님이 정한 값」이 아니라 기계가 쓴 값에도 걸린다.**
6. 원 감사관이 인용한 `test_OBV자동도_같은_정리를_거친다` 는 **사실상 동어반복**이다.
   `_trim_before_stage` 는 `trigger_mode` 로 분기하지 않으므로 가짜 템플릿에 OBV_REVERSE 를 넣어도
   **OBV 고유 경로를 검증하지 않는다.**

---

### ⑦ 다단계 전략의 손절 잠김 해제 — ✅ 작동확인

**반증 결과**: 반증 4건 시도, **전부 실패**. 원 판정 **유지**. 오히려 더 강한 증거가 나왔다.

**반증 ① 「로그 문구를 다른 코드가 찍은 것 아닌가」** → 실패.
`단계 게이트 건너뜀` / `force_sl v130` / `SL v130` 을 전 소스 grep: 각각
`risk_service.py:440` / `:481` / `:335` **한 곳뿐**. 주석 인용 없음. **로그 귀속 정확.**

**반증 ② 「배포 시각이 안 맞는다」** → 실패했고 **결정적 증거로 뒤집혔다.**
컨테이너 TZ 는 **UTC**다. 커밋(KST→UTC): Fix 315/317=`00:52:49`, Fix 321=`01:45:53`,
Fix 322=`01:51:39`(파일 mtime `01:51:52` 와 일치).
즉 원 감사관이 든 「전환 순간 00:53:39」는 **Fix 317 코드**이지 현재 코드가 아니다.
그래서 `Adding job tentatively` 로 scheduler **재시작 시각을 전수 추출**했다:
`00:53:23`(Fix317) / `01:46:54`(Fix321) / **`01:52:26`(Fix322)** / 06:56 / 07:57 / 08:25 / 08:52 / 10:41 / 11:18.
→ 마지막 차단 `00:52:51` 은 재시작 **32초 전**, 첫 건너뜀 `00:53:39` 은 재시작 **16초 후** = **전환이 정확히 프로세스 경계.**
→ 그리고 **`01:52:26` 이후(= 현재 Fix 322 코드) 건너뜀 15건**
(02:10·02:36·05:44·07:44·07:46·07:47·07:49·07:55·08:00·08:01·08:02).
**현재 배포 코드가 실제로 게이트를 열었다.**

**반증 ③ 「Fix 322 의 `_fs_roi > 0` 이 부호 때문에 죽은 조건 아닌가」** → 반증됨.
`force_sl_roi_override` 는 DB 에 **양수 저장**(1,189행, 0.16~80.00 / NULL 299). `> 0` 판정 정상.

**반증 ④ 「배포 코드가 로컬과 다르다」** → 실패.
`git diff 63d7b08 HEAD -- risk_service.py` **빈 결과**. 코드는 `.:/app` 바인드 마운트.

**DB 와 로그 1:1 대조**: 09-03 `FORCE_STOP_LOSS_TRIGGERED` **19건**의 시각이 건너뜀 로그와 **초 단위로 전부 일치**
(2046/1989/2016/2047/2045/1853/2089/2064/2092/2091/2095).
`#2046` STOPPED **-45.99**, `#2047` STOPPED **-31.37** 실현.

**현재 잠김 = 0** (원 감사관보다 넓은 표본): 그는 8건만 봤으나 비종료 전략은 실제 **25건**이다.
25건 전수에 실제 `_stage_gate_exempt` 를 호출 → **전부 exempt=True**(단계정리 23 / 재진입 1 / 분할매수 1). **잠김 0건.**

**놓친 문제 (판정 유지, 잠재 위험은 실측)**:

**(A) Fix 322 는 자기가 막으려던 구멍을 80%만 막는다 — 299건이 남는다** → **7순위** 항목 참조.

**(B) 오늘 추가한 두 분기(Fix 321·322)는 운영에서 한 번도 실행된 적이 없다** → **7순위** 항목 참조.

**(C) `evaluate_stop_loss` 쪽 게이트(:251, :331)는 운영 증거가 0이고, :251 은 아예 침묵한다.**
`[SL v130]` 은 전체 로그에서 **0건**. Fix 321 의 핵심 주장(「면제가 force SL 한 곳에만 있어 일반 SL 이 잠겼다」)은
코드·테스트로만 확인되고 실행 흔적이 없다. 일반 SL 은 ROI -80% 라 -3~-15% force SL 이 먼저 발동하므로
0건 자체는 자연스럽다. 그러나 **`:251` 은 로그 없이 `return False`** 라
**그 자리에서 잠겨도 로그로는 영원히 안 보인다 = 관측 구멍.**

**(D) 참고**: 오늘 force SL 이 대부분 **15초 간격 2회씩** 발화했다(#2047 #2045 #1853 #2089 #2064 #2091 #2095).
Fix 318/319 의 「부분 손절」설계상 정상일 수 있으나 항목 ⑦ 밖이라 단정하지 않는다 — 별도 확인 권장.
(위 ③ (C) 항목이 이 현상의 유력한 설명이다.)

---

### ⑧ 상승 50 + 하락 50 = 100개 모니터링 — ✅ 작동확인

**반증 결과**: 반증 5건 전부 실패. 원 판정 **유지**.

**반증 실패 내역**:
1. **배포 불일치 의심** — `chg24_entry_gate.py` / `execution_service.py` 의 md5 가 로컬과 달랐으나 **CRLF/LF 차이**였고
   git blob 해시(`980f7185…`, `ece4f1ae…`)는 양쪽 동일.
2. **DB 차단 메시지가 옛 Fix310 abs 문구**(「당일 10% 이상 상승/하락만」)여서 Fix325 미배포를 의심했으나,
   그 문구는 현재 코드에 **0곳**이고 차단 38건이 전부 02:07~05:07 UTC = rank 배포(10:40 UTC) **이전**이라 시간순 정합.
3. **차단 전략이 좀비로 슬롯을 먹는다** — 차단 시 STOPPED(`ACTIVE_LIKE` 아님), 실측 슬롯 9/50. **피드백 루프 없음.**
4. **fail-open 무력화** — 컨테이너 로그 원본 grep 결과 `게이트 오류` **0건**.
   fail-open 13건은 전부 다른 워커(`auto_bb_breakdown`/`auto_long_at_bottom`/`success_pyramiding`).
   **게이트는 한 번도 fail-open 하지 않았다.**
5. **우회 경로** — AST 상 게이트는 `start_stage1` 1곳뿐이나, `add_position_now` 3곳은 전부 기존 포지션 추가,
   `enter_stage_at_market` 1곳은 수동 API. **신규 1단계 진입 7개 호출처는 전부 `start_stage1` 로 수렴.**

**추가 증거**:
- 로그 문구는 하드코딩이 아니라 `len(_ranked)` **실계산값**이고, 24h 로그 51만 줄에서 해당 줄 **504건 전부** 100개.
- 운영 컨테이너에서 `passes()` 경계 실측:
  ```
  상승 49위 DELL    +5.31% True
  상승 50위 PUMP    +5.28% True
  상승 51위 COLLECT +5.21% False   ← 정확히 100개에서 끊긴다
  하락 50위 BOME    -0.20% True
  하락 51위 SAMSUNG -0.18% False
  EGLDUSDT +14.64% → 「상승 15위 — 우선(|24h|>=10%)」 = Fix328 분기
  _quick_          → 「수동 진입(게이트 미적용)」 = 예외 분기 실동작
  ```
- **fail-open 0건**이므로 `#2097 EGLDUSDT`(10:56 UTC 진입, 체결 1건) 통과가 추론이 아니라 **진짜 rank 판정**임이 확정.

**놓친 문제 (판정 유지, 보고 필요)**:
- **문제1 (중요)**: 오늘 「하락 50위」가 **-0.23%** 다 → 위 「새 진입이 안 되는 이유」 절 참조.
- **문제2 (중요)**: `market_movers.py` Fix220 주석이 실측으로 반증 — **5M 거래대금 하한** → 위 절 참조.
- **문제3**: rank 차단 경로의 운영 실적은 감사 초기엔 0회였으나, **12:37~12:41 에 실제 발동 확인**
  (`상승/하락 각 50위 밖`, CRVUSDT). **이제 실증됨.**
- **문제4**: 게이트가 `start_stage1` 마다 **전량 티커 조회(weight 40)를 캐시 없이** 수행.
  09-02 23:12 로그에 실제 `429 weight budget exceeded` 가 있어 **IP ban(418) 전력 저장소에 압력.**
- **문제5**: 주력 생산자 `pump_split_entry_worker` 는 `rank_map` 을 쓰지 않고 자체적으로
  `|24h|>=15%` 상위 40개(거래대금 하한 없음)를 뽑는다. 「100개」가 그 경로에서는
  **감시 단계가 아니라 진입 직전 게이트에서만 강제**되어, 오늘 38건이 전략 행만 만들고 차단됐다. 안전하지만 낭비.

---

### ⑨ 「가능하면 10% 상승하락」 우선 진입 — ✅ 작동확인

**반증 결과**: 반증 실패. 원 판정 **유지**. 다만 원 감사관의 근거 3건은 **틀렸거나 낡았고**, 놓친 결함 3건이 있다.

**반증 ① 「배포 증거가 낡았다」** → 결론은 살아남음.
VPS 는 이미 재배포됐다(감사 시점 `c6eb905`, 현재 `32a7696`). 그가 말한 "63d7b08 = 로컬 main 과 동일"도 사실이 아니다
(로컬 `main` 은 `2586555` Fix 292c 로 훨씬 옛것이고, 배포된 것은 **워크트리 브랜치**다).
그러나 결론은 유지된다:
- `git diff --stat 63d7b08 HEAD -- chg24_entry_gate.py execution_service.py position_limit.py market_movers.py` = **빈 출력**
- VPS md5 `7179bbc5cf6f32e2263d6298b521c324` == `git show 63d7b08:…/chg24_entry_gate.py | md5sum` **정확히 일치**
- 게이트 파일 mtime **10:40:59 UTC** < api/scheduler 기동 **11:17:52 / 11:18:21**
  → **러닝 프로세스가 Fix 328 코드를 적재**했다(바인드 마운트 `/root/binance-auto-trader/backend -> /app`)

**반증 ② 「로그 0줄 = 실행 안 됨」** → **그의 부정 증거가 틀렸다 (유리한 방향으로).**
scheduler 로그는 **1.4GB** 라 `docker compose logs --since` 가 신뢰할 수 없다. 원본 json 로그를 직접 grep:
```
Fix310 : 76 / 게이트 오류 : 0 / Fix325 : 0 / Fix328 : 0
```
실제 줄:
```
05:07:30 [Fix310] GPSUSDT #2087 1단계 진입 차단 — 24h +7.63% …
04:37:33 [Fix310] ESPORTSUSDT #2086 1단계 진입 차단 — 24h -4.22% …
```
→ 이 게이트는 `execution_service.start_stage1` 에 **실제로 배선돼 운영 진입을 막은 로그 76줄**이 있고,
DB 고아행 `#2086 ESPORTSUSDT` / `#2087 GPSUSDT` 와 정확히 교차 확인된다(증거 ①+②).
`게이트 오류 0` = **fail-open 으로 조용히 죽은 적 없음.**

**반증 ③ 「4-튜플 언패킹이 터진다」** → 정상.
러닝 컨테이너 실 DB 호출: `check_position_slot(db, tag=…)` → `(True, '슬롯 여유 9/50 (src=sajangnim_top_short_daily_limit)', 9, 50)`
len=4. `_slot_usage_pct` → `(18.0, '9/50')`. 소스 4개 return 경로(130/134/140/147행) 전부 4-튜플 확인.

**반증 ④ 경계 실측** → 위 「⚠️ (E)」 참조. 설정 실측:
`entry_chg24_gate_enabled='1'`, 나머지 5키 DB 행 없음 → 기본값 `mode=rank / top_n=50 / priority=10.0 / tight=70.0`,
`cap=50 (sajangnim_top_short_daily_limit)`.

**반증 ⑤ 「빠듯 분기는 사문화 코드다」** → 반증됨.
14일 시간별 동시보유 최대 **47** = 94% ≥ 70% (원 감사관의 44보다 강하다). **도달 가능 영역이다.**

**놓친 결함**:

**(1) 사장님이 이 기능을 끄거나 값을 바꿀 수 없다** → 위 「⏸ (4)」 참조.

**(2) 🚨 조용한 무력화 취약점.** `_slot_usage_pct` 의 bare `except` + `logger.debug`.
3-튜플 회귀를 시뮬하니 `(True, '… 슬롯 조회 실패: not enough values to unpack (expected 4, got 3) 여유')` 로
**빠듯 분기가 로그 한 줄 없이 사라진다.** 매매를 막지 않는 안전한 방향이지만,
**이 저장소가 반복해 당한 「기능이 죽은 채 사는」 유형**이다 → `logger.warning` 으로 올릴 것.

**(3) 차단이 고아행을 남긴다.** 오늘 절대값 모드 차단으로 `current_stage=0` 행 **18건**(#2061~#2087).
`surge_ladder_entry` 가 STOPPED 로 「좀비 정리」는 하지만 `is_archived=False` 로 남아 목록·통계에 섞인다.
`ACTIVE_LIKE` 가 아니라 **슬롯 사용률로 되먹임되지는 않음**(피드백 루프 없음 — 확인함).
다만 `auto_short_at_top_worker` 는 실패 시 「알람 유지 = 재시도」라, **빠듯 분기가 켜지면 매 사이클 새 고아행이 쌓인다.**

**남는 유일한 미검증**: **빠듯 차단이 운영에서 실제로 발동한 적은 없다**(현재 9/50 = 18% 여유모드).
다만 (a) 우선 분기는 `#2097 EGLDUSDT STAGE1_OPEN 10:56:35 UTC`(템플릿 `PUMPSPLIT_…` = 수동 예외 아님)로
실제 통과 판정을 냈고, (b) 빠듯 분기는 러닝 프로세스 + 실시세로 70% 경계까지 실행 확인했으며,
(c) 게이트 자체는 운영 로그 76줄 + 12:37~12:41 rank 차단으로 실 진입 차단이 증명됐다.
증거 ①②③④가 모두 충족되므로 **작동확인 유지.**

---

### ⑩ 지지선 7점 판정 — ⏸ 설정꺼짐

**반증 결과**: 원 판정 **유지**. 다만 **원 감사관이 든 핵심 「실행 증거」는 무효**이고, 놓친 문제가 3건 있다.

**반증 시도 전부 실패 (판정은 맞다)** — 위 「⏸ (1)」 절에 상세.
추가로:
- **다른 이름의 설정으로 돌고 있지 않은가** → 운영 DB `system_settings` **63행 전수 조회**.
  `support`/`score`/`gate` 포함 키는 `confluence_gate_enabled=true`, `entry_chg24_gate_enabled=1`,
  `long_surge_gate_enabled=1`, `support_breakdown_short_enabled=0`(다른 옛 기능), `trend_4h_gate_enabled=1` 뿐.
  **`support_score_gate_enabled` 행 없음.**
- **하드코딩·env 우회** → `gate_enabled()` 는 `SystemSetting` 만 읽는다. env fallback 없음.
  저장소 전체에서 `support_score` 를 참조하는 파일은 **4개뿐**.
- **배선이 엉뚱한 곳에 붙지 않았는가 (Fix 318 재발 검사)** → `start_stage1` 본문
  **line 254 `_sup_eval(…)` → line 272 `_place_stage_entry_order`**. **차단이 발주 앞에 있다.**
  `trigger_next_stage`/`enter_stage_at_market` 에는 의도대로 없음.
- **우회 경로** → 8개 진입 경로 전수 확인, **없음**.
- **배포** → `support_score.py` mtime 08:51Z, api 11:17:52Z / scheduler 11:18:21Z → **코드 적재됨.** 테스트 `22 passed`.

**🚨 원 감사관의 「실행 증거」는 무효다.**
그는 *"`start_stage1` 이 9번 실행됐는데 `[Fix327]` 로그가 0줄 = 게이트가 돌고 즉시 통과했다는 실행 증거"* 라고 했다. **틀렸다.**
- 오늘 1단계 진입 9건(#2090~#2097)의 생성 시각은 **07:43~10:56 UTC**. scheduler 재시작은 **11:18:21 UTC**.
  즉 **9건 전부 재시작 이전**이다. 그 중 8건(#2090~#2096, 07:43~08:12)은
  `support_score.py` 가 디스크에 쓰이기(08:51Z)**도 전**에 발생했다.
- 게다가 게이트가 OFF면 `evaluate()` 가 `_whys=""` 로 반환하고 `if _whys:` 가 거짓이라 **로그가 원리적으로 안 남는다.**
  즉 「로그 0줄」은 *게이트가 돌고 통과함* 과 *코드가 아예 없음* 을 **구분할 수 없다.** 실행 증거가 될 수 없다.

→ 판정은 **DB 직접 조회(행 없음 → `gate_enabled=False`)만으로 확정**된다. 결론은 같지만 근거는 이것이다.

**놓친 문제 ①②③④** → 위 「⏸ (1)」 절에 전부 수록
(켤 방법이 없다 / 배선 테스트가 순서를 안 본다 / 켰을 때 영향이 크다 / `cooldown_bars=6` 미구현).

**②의 보충**: `test_start_stage1이_실제로_부른다` 는 **함수 소스 문자열 검사**다.
`_sup_eval(` 검사는 유효하지만, **이 블록을 `_place_stage_entry_order` 뒤로 옮겨도 22건 전부 통과**한다
— **Fix 318 과 정확히 같은 실패 모드.** `assert "raise ValueError" in src` 도 Fix310 의 raise 로 이미 충족돼 무의미하다.
Fix 320 처럼 게이트 ON 상태로 `start_stage1` 을 **실제 구동하는 통합 테스트가 없다.**
(순서 자체는 반증관이 직접 읽어 정상 확인했다.)

---

### ⑪ 손절 후 재진입 — 실제로 도는가 — 🚨 고장

**반증 결과**: 반증 실패, 원 판정 **유지**. 오히려 **더 강한 증거로 확증**됐고,
**원 감사관이 놓친 더 큰 차단자(Fix 270)** 를 찾았다.

**반증 1 「로그 인접을 인과로 오인했는가」** → 아니다.
11:50 사이클에서 `[Fix274] ⛔ AAPLUSDT` 바로 뒤에 `[RT_REENTRY] SUIUSDT` 가 오는 것을 발견해 오인을 의심했으나,
24h 전수 조사로 진짜 인과가 확정됐다 — **동일 심볼·동일 방향·2~6ms 간격**으로 반복.
24h 「None 반환」 **608건**은 전부 `realtime_reentry_worker` 발생이며, **591건**이 선행 게이트 로그와 동일 심볼로 귀속된다.

**반증 2 「재진입 면제가 있는가」** → 구조적으로 없다 (AST) → **2순위** 항목 참조.

**반증 3 「2단계가 정말 없는가」** → VPS 직접 실행으로 확정 → **2순위** 항목 참조.
원 감사관은 "최근 인스턴스가 없어 미검증"이라 했으나 **실제로는 전 이력 0건**
— Fix 315 는 다단계 템플릿을 한 번도 만든 적이 없었다(**더 나쁨**).
> 🆕 단, 12:35:24 의 `#2098` 이 `stages_count=3` 으로 **처음 만들어졌다.** 재진입 템플릿도 그렇게 되는지는 미확인.

**원 감사관이 놓친 것 — SHORT 를 막는 진짜 범인 Fix 270** → **2순위** 항목 표 참조.
원 감사관은 Fix 274 만 지목했고 **SHORT 재진입이 왜 0건인지 설명하지 못했다.**
실제로는 Fix 270 이 **동등 규모의 공동 차단자**이며 SHORT 전량을 막는다
(TRIAUSDT SHORT 163건, NBISUSDT SHORT 120건).
둘 다 `trend_4h_gate_enabled=1` / `long_surge_gate_enabled=1` 로 켜져 있고 **재진입 면제가 없다.**

**원 감사관의 오진 1건**: 「Fix 310 도 재진입을 막는다」는 근거 없다.
608건 전부 `_create_auto_bb_strategy` 안에서 차단되어 **`start_stage1` 에 도달조차 못 한다.**
게다가 `bb_mid_line` 은 오늘 20건 넘게 Fix 310 을 통과해 실제 진입했다(02:07~08:12 UTC)
→ **Fix 310 은 전면 동결 요인이 아니다.**

**추가 확인**:
- 24h `reentered=1` 은 1회뿐인데 그마저 `fail=1 success=0` → **실성공 0건**
- `FORCE_STOP_LOSS_TRIGGERED` 최신 08:02:07, Fix 326 커밋(08:25) 이후 0건 → **Fix 326 미실행 확정**
- `LIQUIDATED_WAITING_RETRY` **전 이력 0건**
- `start_stage1` 에 Fix 327 지지선 게이트 신설됨. 기본 OFF 이나, **켜면 재진입 세 번째 차단자가 된다**(잠재 위험)

**고칠 곳 (수정 안 함)**: `backend/app/workers/auto_bb_breakdown_worker.py`
L1496~1542(Fix 274) 및 L1545~1571(Fix 270) 에 `strategy_type_suffix.startswith("_reentry")` 면제 추가.
**다만 그것만으로는 부족** — `capitals` 가 단일 원소라 2단계가 없으므로 사장님 「부분 손절 후 다음 단계」는
여전히 성립하지 않는다. **두 곳을 함께 고쳐야 한다.**

> 🆕 **Fix 330/331 이 Fix 270·Fix 247 을 「참고」로 바꿨으므로 SHORT 재진입 차단 294건분은
> 해소됐을 가능성이 있다. 재측정 필요.** LONG 을 막는 Fix 274 는 그대로다.

---

### ⑫ 적응 TP — 기본·OBV 경로에도 적용됐는가 — ❌ 미구현

**반증 결과**: 반증 실패. 원 판정 **유지**. 코드·배포 이미지·DB·로그·테스트 **다섯 각도에서 전부 일치**했고,
유일하게 유망했던 반증은 실패했다.

**배포된 이미지에서 직접 grep** → 위 **4순위** 항목 참조.

**🚨 DB 결정타** — `tp1_pct_override = 3.00` 전체 분포는 위 4순위 표 참조.
감사 초기엔 **전 테이블 0행**이었고, **12:35:24 의 `#2098` 이 유일한 1행**이 됐다.

**오늘(09-03 UTC) 기본 방식·OBV 자동 실제 행 — 전부 15.00**:

| id | 심볼 | 타입 | trigger_mode | tp1_override | 템플릿 사다리 |
|---|---|---|---|---|---|
| 2089 | UAIUSDT | DYNAMIC_LONG | PRICE_DOWN_PCT | **15.00** | 5/10/15/20 |
| 2090 | MAGMAUSDT | DYNAMIC_LONG | PRICE_DOWN_PCT | **15.00** | 5/10/15/20 |
| **2091** | **MARSCOINUSDT** | DYNAMIC_SHORT | **OBV_REVERSE** | **15.00** | 5/10/15/20 |
| 2092 | EDGEUSDT | DYNAMIC_SHORT | PRICE_DOWN_PCT | **15.00** | 5/10/15/20 |
| 2093 | BRUSDT | DYNAMIC_LONG | PRICE_DOWN_PCT | **15.00** | 5/10/15/20 |
| 2094 | BULLAUSDT | DYNAMIC_LONG | PRICE_DOWN_PCT | **15.00** | 5/10/15/20 |
| 2095 | HEMIUSDT | DYNAMIC_LONG | PRICE_DOWN_PCT | **15.00** | 5/10/15/20 |

14일 분포도 같다: **`OBV_REVERSE` 76건 전부 15.00** / `PRICE_DOWN_PCT` 623건 15.00 + 89건 5.00
(`bb_mid_line`·`pump_split` 하드코딩). 나머지 20/25/30/35/50 은 `control.py:1012` 수동 API 값이다.

**다른 이름의 적응 TP 수색** → 없다.
`priceChangePercent|chg_24h|chg24` × TP/익절 교차 grep → `adaptive_tp.py` 자기 자신만.
「변동성 연동」 문자열 보유 파일 = `adaptive_tp.py` + `auto_bb_breakdown_worker.py` **둘뿐**.

**테스트 재실행** — `16 passed`. 단 `test_진입경로에_연결돼_있다` 는
`auto_bb_breakdown_worker.py` **한 파일의 문자열 3개**만 assert 한다. **기본·OBV 누락을 구조적으로 못 잡는다.**

**Fix299 로그** 최근 3시간 = 0줄 (감사 초기). **12:35:25 에 처음 찍혔다.**

**배포 이미지 UI/API 노출 = 0건.** 사장님이 화면에서 못 켜고 못 끈다.

**🔬 유망했던 반증 시도 — 실패**:
`#2047 MUBARAKUSDT` 가 **DYNAMIC_SHORT / PRICE_DOWN_PCT 인데 사다리가 15/30/45/60** =
적응 TP 「급등락」 사다리와 똑같은 모양이라 「기본 경로에도 적용됐다」의 증거로 보였다.
템플릿을 추적하니 **`_quick_20260903003312`** — **사장님 수동 빠른진입 UI 경로**였다
(`#2046 AKEUSDT` = `_quick_20260902232217` 도 동일). 사장님이 그 숫자를 직접 입력하신 것이고 코드가 넣은 게 아니다. **반증 실패.**

**🔧 판별자 정정 (감사 후 발견)**: `#2098` 은 `trigger_mode = PRICE_DOWN_PCT` 인데 적응 TP 가 적용됐다.
즉 **「기본 방식 = PRICE_DOWN_PCT」라는 판별자 자체가 부정확**하다.
실제 구분은 **워커 경로**다 — `auto_bb_breakdown_worker` 를 타면 적응 TP 가 붙고,
`bb_mid_line_worker` / `pump_split_entry_worker` / `surge_ladder_entry` / `_quick_` 수동 경로를 타면 **안 붙는다.**
**`OBV_REVERSE` 는 여전히 76건 전부 15.00 = 0건 적용.** 판정 「미구현」은 유지된다.

**그가 틀린 것 2건 · 놓친 것 3건** → 위 **4순위** 항목에 전부 수록.

**최종 정리**:

| 대상 | 판정 |
|---|---|
| `adaptive_tp.py` 모듈 자체 | ✅ 작동확인 (`adaptive_tp_enabled='1'`, 09-02 17:47:44 UTC) |
| auto_bb(v219) 경로 | ✅ **12:35:25 첫 실행 확인** (감사 초기엔 「게이트보다 먼저 실행돼 결과가 전부 삭제됨」) |
| **기본 방식(`bb_mid_line`/`pump_split`/`_quick_`)** | ❌ **미구현** — 호출 0곳 |
| **OBV 자동(`OBV_REVERSE`)** | ❌ **미구현** — 호출 0곳, 76건 전부 15.00 |
| 임계값 3개 설정행 | 없음 (코드 기본 15/15/3 로 동작, fail-safe 정상) |
| UI/API 노출 | 0건 — 사장님이 제어 불가 |

---

## 사장님이 결정하실 것

판단이 필요한 항목만 모았다. **나는 아무것도 바꾸지 않았다.**

### 결정 1. 🩸 명목 60 USDT 미만 포지션의 손절을 어떻게 할 것인가 (1순위)

지금 `#1988 TQQQUSDT`(증거금 10, 레버 2, 명목 20, 단계 1개)는
**-5% 에서도 -400% 에서도 손절 주문이 나가지 않는다.**
그리고 **모든 부분 손절 잔량이 앞으로 같은 상태가 된다.**

| 안 | 내용 | 장단 |
|---|---|---|
| **A** | SKIP 이면 **전량 청산**으로 폴백 (Fix 326 이전 동작) | 손절은 확실히 나감. 단 사장님 「10 남기기」가 소액 구간에서 무효 |
| **B** | `stage_min_trim_ratio` 를 낮추거나 `stage_keep_notional_usdt` 를 소액 전략에서 축소 | 사양 유지. 단 MIN_NOTIONAL 5 때문에 명목 20 에서는 여전히 한계 |
| **C** | **단계계획이 1개인 전략은 부분손절 대상에서 제외**(= 전량 청산) | 「다음 단계가 있을 때만 남긴다」는 사양의 원래 뜻에 가장 맞다. **권장** |
| **D** | 잔량 전용 타임아웃 청산 워커 신설 | 근본 해결이나 신규 기능 |

> 참고: 최근 3일 생성 148건 중 **61%가 단계 1개짜리**다. C 안의 적용 범위가 넓다.

### 결정 2. Fix 331 배포 효과를 얼마나 지켜볼 것인가

**A안은 이미 실행됐다**(Fix 330 12:21 UTC / Fix 331 12:34 UTC 배포·실행 확인).
첫 성과로 v219 정점 SHORT 가 **7일 만에 진입**했다(`#2098 ZECUSDT`).
그러나 **표본 1건이라 수익 효과는 판단 불가**다.
→ **24~48시간 관찰 후 재측정**을 권한다. 그동안 결정 3·4는 보류.

되돌리기: `confluence_reversal_exempt = 0` (Fix 331) / `trend_4h_reversal_exempt = 0` (Fix 330) 한 줄.

### 결정 3. Fix 274(LONG 24h 급등 15%)를 완화할 것인가

통과율 **0.19%(5/2,700)** 로 **LONG 을 사실상 전면 차단** 중이다.
근거는 「24h ≥ 15%」인데 **채택 표본이 20건뿐**이라 A안보다 근거가 얇다.
→ **A안(Fix 331) 배포 효과 재측정 후에 판단**하시기를 권한다.

### 결정 4. Fix 327 지지선 게이트를 켤 것인가

**진입을 더 줄이는 게이트**다. 실측: 접촉 시 **LONG 83.6% / SHORT 62.9% 차단**,
전체 진입시도 대비 **LONG 7.52% / SHORT 5.66%**.
사장님 주력인 「급등 중 조정 LONG」이 지지선 접촉 자리에 몰리므로 **LONG 이 훨씬 세게 맞는다.**
→ **A안으로 진입을 회복한 뒤에** 켜시는 순서를 권한다.
그리고 켜려면 **DB 직접 쓰기밖에 없다**(UI·API·시드 0곳) — 그것부터 만들 것인지도 결정이 필요하다.

### 결정 5. 추가 진입 2% 를 `bb_mid_line` / `split_entry` 에도 열 것인가

Fix 282 의 제외 사유(「TP1 도 +5% 라 익절 지점에서 추가 매수가 나간다」)는 **트리거가 2% 가 된 지금 소멸**했다.
그런데 제외는 `_trig` 를 참조하지 않는 **`frozenset` 하드코딩**이다.
`bb_mid_line` 만으로 라이브의 **1/3(3/9)**. 사장님 「+2% 부터 진행」이 이 3분의 1에는 닿지 않는다.
→ 다만 「1회 진입 전략은 남이 손대지 않는다」는 **별개 근거는 아직 유효**하다. 사장님 판단.

### 결정 6. 「하락 50위 = -0.23%」를 그대로 둘 것인가

5M 통과 풀 248개 중 `|24h| >= 10%` 는 **31개뿐**인데, 슬롯이 18% 라 Fix 328 우선 조건이 걸리지 않아
**-0.2% 보합 종목에도 신규 진입이 허용된다**(248개 중 100개 = **40% 통과**).
저장소 실측 기록은 **±5% 에서 구분력이 사라지고 10% 가 진짜 경계**라고 말한다.
사장님 문자에는 맞지만 **사상 ⑧(급락 = 이전 급등의 반작용)의 뜻과는 어긋난다.**

### 결정 7. 「100개」의 거래대금 5M 하한을 알고 계셨는가

`market_movers.py` 는 **거래대금 5M 이상**만 후보로 올린다. 그 결과
**진짜 상승 50위 중 14개**(TAG +15.66%, AIO +12.86%, COAI +11.75%, NIGHT +11.38% 등),
**진짜 하락 50위 중 22개**(RECALL -15.74% 등)가 제외된다 = **상위 50위의 28%가 감시망 밖.**
이 하한은 **사장님께 보고된 적이 없다.**

### 결정 8. 사장님 지시값이 DB 행 하나에만 걸려 있는 것을 그대로 둘 것인가

`sajangnim_pyramid_trigger_roi`(=2.0) 를 포함해 **9개 설정에 UI 도 API 도 없다**(위 「⏸ (4)」 표).
행이 삭제/공백이 되면 **경고 없이 코드 기본값으로 조용히 복귀**한다.
그리고 `success_pyramiding_enabled='0'` 같은 **죽은 키가 설정 테이블에 남아 오독을 유발**한다.

---

## 부록: 감사 방법론 — 이번에 실제로 통한 것

이번 감사에서 **정적 검사만으로는 절대 못 잡았을 사실**들:

| 발견 | 잡은 방법 |
|---|---|
| Fix 326 이 배포 후 한 번도 안 돌았다 | **1.4GB 원본 json 로그 전수 grep** (`docker compose logs --since` 는 대용량에서 신뢰 불가) |
| `tp1_pct_override=3.00` 이 전 테이블 0행 | **DB 전건 분포** (최근 것만 보면 안 보임) |
| `_trigger_roi()` 가 2.0 을 반환 | **운영 컨테이너 + 운영 세션으로 그 함수 직접 호출** |
| 게이트가 11종 전부 BLOCK | **`inspect.getsource(클래스)` 로 런타임 로드 객체를 AST 파싱** (디스크 파일이 아니라) |
| Fix 322 절이 0/25 건에서만 도달 | **살아있는 전략 전수에 실제 판정 함수 실행** |
| Fix 331 이 진입을 열었다 | **배포 전후 같은 심볼의 30초 사이클 비교** (자연 실험) |
| 원 감사관 6명 중 3명의 근거가 틀림 | **반증관을 따로 붙여 「뒤집기」를 목표로 시킴** |

🔁 **다음에도 지킬 것**:
1. **로그 0줄은 「안 돌았다」와 「돌고 조용히 통과했다」를 구분하지 못한다.** 반드시 DB 나 직접 호출로 교차.
2. **`--since` 는 대용량 로그에서 거짓 0을 만든다.** 원본 파일을 grep 할 것.
3. **테스트가 문자열 검사면 「순서」를 못 본다** — Fix 318 이 그렇게 통과했다.
4. **배포 판정은 `프로세스 시작 시각 > 파일 mtime`** + **md5 대조**.
5. **커밋 해시를 브리핑에 박아두면 낡는다** — 감사 시작·종료 시각에 각각 확인할 것.
   (이번에 `63d7b08` → `c6eb905` → `ab2132d` → `32a7696` 로 **네 번** 바뀌었다.)
