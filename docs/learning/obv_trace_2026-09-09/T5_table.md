| # | 사장님 의도 (2026-09-09 verbatim) | 코드 동작 (main 0d174bf, 배포 9/9) | 일치/불일치 | 근거 (file:line) | 고치는 법 (설정/코드) |
|---|---|---|---|---|---|
| 0 | 대시보드 모달에서 「새전략 OBV 자동」 수동 생성, 사다리 10/300/600, 레버 2, 헤지 | 모달은 `trigger_mode=OBV_REVERSE` 템플릿을 만들고, 인스턴스 `capital_management_mode` 는 **항상 `'fixed'`** (`stage_ladder` 가 아님). `total_capital` = 템플릿 합 = **910** (미진입 단계 포함). `force_sl_roi_override` = 설정 `force_sl_roi_new_default` (DB 행 없음 → 코드 기본 **25**). `tp1_pct_override` = 코드 고정 15. 2·3단계 트리거 % 칸은 OBV 모드에서 **비활성만 되고 값(2단계 10 / 3단계 20)은 그대로 남아** 템플릿 `trigger_percents=[null,10,20]` 로 저장되고, 계산기가 **2·3단계 `trigger_price`** 를 만든다 (SHORT: 시작가×1.10, ×1.32 / LONG: ×0.90, ×0.72). | 자본·레버·방향은 일치. **모드 마커가 `fixed`** 라 사다리 전용 게이트(Fix 344 잔고검사·정점-주춤·`is_stage_ladder` 면제)는 전부 미적용. **트리거 단가가 남아 있는 것**이 ②·④ 불일치의 원인 | cm-open-modal.js:43, :102-112 / cm-submit.js:217, :229 / cm-capitals-grid.js:23-28, :64, :77-80 / cm-collectors.js:70-84 / admin/templates.py:147 / strategy_service.py:576, :583, :585, :590, :594 / strategy_calculator.py:300-318, :325-338 / risk_constants.py:86 | (코드) OBV 템플릿이면 `strategy_calculator` 가 2단계+ `trigger_price=None` 로 만들거나, 워커가 OBV 발동 시 `force_market=True` 를 넘길 것 (아래 ②). 수동 OBV 도 사다리로 취급하려면 cm-submit.js:229 에서 `stage_ladder` 를 보낼 수 있게 할 것 |
| 1 | 수동 진입 10 USDT | `/strategies/{id}/start` → `start_stage1`: 1단계 IMMEDIATE, 시작가 LIMIT, 수량 = 10×2/가격. Fix 310(당일 10%) 게이트는 `_quick_` 수동 제외 | **일치** | execution_service.py:188-260 / strategy_calculator.py:243-255 / cm-submit.js:283-286 | — |
| 2-A | 이익 경로: 「수익이 지속되고 차트·지표가 확실하면 300 씩 최대 2회 추가」 | `success_pyramiding_worker`(30초): 상태 ACTIVE_WITH_POSITION·fixed 모드·`_quick_` 은 대상. 게이트 = ROI ≥ `sajangnim_pyramid_trigger_roi`(**live 5**) AND 가격 유리이동 ≥ `pyramid_min_move_pct`(행 없음 → **3.0** = 레버2 에서 ROI 6%) AND 방향 ∈ `pyramid_sides`(행 없음 → **"SHORT" 만**) AND 정점 되돌림 ≤2.5% AND 시작가 대비 ≥0.5% AND 15m MACD hist 3봉 가속(4H 참고만) AND 카운터 < **2** AND 심볼 5분 쿨다운. 추가금 = `sajangnim_pyramid_capital`(**300**) MARKET, mode=preserve. 추가 직후 `pyramid_cap_loss_enabled`(행 없음 → **ON**) 이 `force_sl_roi_override` 를 낮춤: 910→1210 이면 25%→18.8%, 1510 이면 15.1% | **SHORT 은 대체로 일치**(조건이 더 빡빡: 실효 ROI 6%). **LONG 인스턴스는 기본값으로는 추가가 영원히 0건**. 추가마다 손절 ROI 가 자동 하향(요청 없던 동작) | success_pyramiding_worker.py:46, :61, :94, :320-321, :394-405, :408-455, :537-566, :644, :676-687, :698-713, :831, :891, :921, :961-966 / trend_4h_gate.py:396-435 / execution_service.py:1618, :1657-1671 / single_entry_guard.py:49-53 / live: sajangnim_pyramid_trigger_roi=5, sajangnim_pyramid_capital=300 | (설정) LONG 도 원하면 `pyramid_sides=LONG,SHORT`; 실효 5% 로 맞추려면 `pyramid_min_move_pct=2.5`; 손절 하향 원치 않으면 `pyramid_cap_loss_enabled=0` |
| 2-B | (이익 경로에서) 나머지는 인스턴스 옵션대로 | **stage_trigger_worker 는 손익을 보지 않는다**: 1단계가 이익 중이어도 OBV 신호(`check_stage_entry_signal`)가 뜨면 **2단계 300 을 발주**한다. 게다가 피라미딩으로 310+ 가 된 상태에서 2단계가 뜨면 `_trim_before_stage` 가 **이익 중인 포지션을 10 USDT 만 남기고 청산**(손익 무관) 하고 300 을 다시 넣는다 | **불일치** — 이익 경로에 「2단계 진입」과 「10 남기고 정리」가 끼어든다 (실측 #1731: 2단계 + 추가 4회, 자본 8610, −103.86) | stage_trigger_worker.py:464, :792-855(ROI 조건 없음), :1006-1015, :1459-1461 / execution_service.py:355, :1847-1905(손익 검사 없음) / stage_trim.py:235-358(손익 입력 없음) / live: stage_trim_before_next_enabled=1 | (코드) OBV 분기에 「현재 ROI < 0 일 때만 2·3단계」 전제조건, 그리고 `_trim_before_stage` 에 「ROI ≥ 0 이면 정리 안 함(또는 진입 안 함)」 추가 |
| 3-A | 손실 경로: 10 진입 → 손실 → 「계속 모니터링」(1단계는 손절 없음) | 강제손절 판정 = ROI ≤ −`force_sl_roi_override`(25) (ROI=가격%×레버). 단계 게이트는 Fix 322(손절 명시)·Fix 317(정리 ON) 로 면제. 1단계 10 USDT 에서 도달해도 `compute_trim` 이 SKIP(남길 목표 10×2=20 명목 ≥ 보유) → 다음 단계가 있으므로 **청산하지 않고 유지** + `FORCE_SL_RESIDUE_KEPT` 기록 | **일치** (실측 #4421 SOPH: TRIGGERED 6 / RESIDUE_KEPT 7, 청산 0) | risk_service.py:36-73, :192-264, :394-433, :464 / tp_sl_orchestrator.py:623, :681-716 / stage_trim.py:112-166, :311-325 | — |
| 3-B | 「수익이 가능한 자리」에서 300 추가 (2단계) | 워커(10초)가 `STAGE1_OPEN` 등에서 plan 2 를 보고 OBV 분기 → `check_stage_entry_signal`(obv_gate → 양방향 blocklist → regime(SHORT) → 진입창(SHORT, `entry_window_short_enabled=true`) → confirm_peak 15m 2회 + 지표 2/3). 통과 시 `trigger_next_stage(force_market=False)` → 정리 SKIP(10 USDT) → `_place_stage_entry_order`: **plan 2 에 trigger_price 가 있으므로 MARKET 이 아니라 그 단가 LIMIT GTC**. SHORT 이면 시작가+10% 에 매도 지정가(현재가가 그 위면 즉시, 아니면 대기), LONG 이면 −10%. 발주 즉시 `current_stage=2`, `STAGE2_OPEN_PENDING`. live `auto_obv_stage2_trigger=-5` 는 이 워커가 **읽지 않는다**(제안 API 전용) | **불일치** — 「신호 자리」가 아니라 「시작가 ±10% 단가」에서 진입. 신호가 떴는데 가격이 트리거 미달이면 지정가가 호가창에 걸린 채 단계만 소진 | stage_trigger_worker.py:303, :408, :605-610, :792-855, :918-923, :981, :1459-1461 / execution_service.py:305-366, :1166, :1233, :1257 / strategy_calculator.py:325-338 / strategy_suggestions.py:883-906(설정의 유일한 소비처) | (코드) stage_trigger_worker.py:1459 에서 `_is_obv_mode` 면 `force_market=True` (Fix 260 과 같은 이유), 또는 OBV 템플릿 plan 2+ 의 `trigger_price=None`. `auto_obv_stage*_trigger` 는 이 경로에 무관함을 문서화 |
| 3-C | 2단계 후 「다시 손실이면 10 USDT 만 남기고 부분손절」 | 합산 평단 기준 ROI ≤ −25% → `_execute_force_stop_loss`: trim ON(fixed 는 제외 아님) → `compute_trim` 잔량 = 10 USDT 증거금×레버 = 20 명목(Fix 324), MIN_NOTIONAL×1.1 하한 → TRIM → 미체결 취소 안 함, 부분 청산, **상태 유지**, 피라미딩 카운터 리셋. 다음 사이클은 SKIP + 다음 단계 있음 → 유지 | **일치** (임계값은 인스턴스 옵션 25; 옛 설명의 −5 가 아님. 평단이 합산이라 10 레그 손실은 희석됨 = 사장님 계산 −0.29% 와 같은 구조) | tp_sl_orchestrator.py:623, :681-697, :702-716, :732-737, :754-760 / stage_trim.py:77, :88, :235-358 / risk_service.py:259-264 | 손절 % 는 전략 화면 「강제손절」PATCH(control.py:1122-1133) 로 변경 |
| 3-D | 「다시 모니터링 → 수익 가능한 자리에 600 (3단계)」 | 3-B 와 같은 경로. `_trim_before_stage(3)`: 잔량 10 → SKIP → 진입. 단 plan 3 `trigger_price` = 시작가 ×1.10×1.20 (SHORT +32% / LONG −28%) 의 **LIMIT** | **불일치(3-B 와 동일 원인, 더 심함)** — 3단계 600 은 시작가 대비 32% 불리 지점 지정가 | strategy_calculator.py:300-318 / execution_service.py:1166, :1257 / stage_trigger_worker.py:1459 | 3-B 와 동일 |
| 3-E | 3단계 이후 손익은 옵션대로 | 3단계 손실 → TRIM 으로 10 남김 → 다음 사이클 SKIP + 다음 단계 없음 → **전량 청산**(Fix 332) → STOPPING. 이익 → TP/트레일링 | **일치** | tp_sl_orchestrator.py:707-722, :763 | — |
| 4 | TP/트레일링/SL 은 인스턴스 옵션 | TP 레벨 = 템플릿 tp{n}_percent (모달 입력). 트레일링 = 첫 TP 부분체결 후 최고 ROI 대비 5%p 회귀(값 무관, Fix 31/335). `adaptive_tp_enabled=1` 은 자동 워커 전용이라 수동 OBV 에 영향 없음. `tp1_pct_override` 는 모달 값과 무관하게 **코드 고정 15** | 대체로 일치; `tp1_pct_override=15` 가 TP1 레벨에 어떻게 얹히는지는 미확인(unknown) | risk_service.py:555-620, :669-680, :765-840 / strategy_service.py:585 / auto_bb_breakdown_worker.py:1870-1876 / bb_mid_line_worker.py:401 | — |