## confirmed
Verified by reading the worktree (HEAD 3e04034 = 0d174bf + docs) and the dump (1,518 rows, OBV_REVERSE 84, max id 4422).

C1 OBV stage 2/3 go out as LIMIT GTC at start_price±10%/±32%, not MARKET at the signal. stage_trigger_worker.py:464 (_is_obv_mode), :826-830 (should_fire = check_stage_entry_signal only, no price), :918-923 + :981 (_ps_force_market True only for split/stage_ladder peak-stall), :1459-1461 (trigger_next_stage(force_market=_ps_force_market)); execution_service.py:1166 (MARKET only if trigger_price is None or force_market), :1257 (LIMIT GTC @ stage_plan.trigger_price). trigger_price always exists for a modal OBV instance: cm-open-modal.js:99-112 disables but does not clear cm-trg-N; cm-collectors.js:68-76 still pushes the values; even if null, strategy_calculator.py:31-49 defaults 10% (stage 2) / 20% (last, PRICE_UP_PCT default at :39-40, :162-167), compounded at :317-334 → SHORT 1.10× and 1.32×. Stage consumed on order placement: execution_service.py:359-362; reconcile false recovery: reconcile_worker.py:540-548 (is_triggered=True whenever position≠0). Dump: all 3 OBV instances that reached stage 2 (#1728/#1731/#1843, all 龙虾USDT) carry RECONCILE_TRIGGER_RECOVERED + MARTINGALE_GATE_MISSING, 0/3 wins, −217.67 — consistent with, not proof of, unfilled LIMIT.

C2 No profit/loss precondition on OBV stage 2/3 (stage_trigger_worker.py:792-855) and _trim_before_stage closes down to 10 USDT margin without looking at PnL (execution_service.py:355, :1849-1918; compute_trim gets qty/mark/leverage only, :1890-1892). stage_trim_before_next_enabled=1 live. auto_obv_stage2_trigger/-5 is NOT read by stage_trigger_worker or risk_service: consumers are strategy_suggestions.py:883-906 and auto_bb_breakdown_worker.py:1732-1733 (f"auto_obv_{k}", auto template only).

C3 Fix 269 ratchet uses strategy.total_capital, which is the template plan sum (strategy_service.py:586 ← templates.py:147 = 910), and is applied on every pyramiding add (success_pyramiding_worker.py:891, :966 cap_loss; execution_service.py:1657-1671). For the real instance: 25 → 25×910/1210 = 18.80 → 18.80×1210/1510 = 15.07. TRIM/partial-SL only resets the pyramid counter, never restores override or total_capital (tp_sl_orchestrator.py:683-704). Dump ratchets match the formula (#2091: 3.64 = 5×1600/2200; 6 instances, all base 5 or 30). No instance with override 25 exists in the dump (Fix 362 not yet observed).

C4 pyramid_sides default "SHORT" (success_pyramiding_worker.py:309, :336, :394-405) and gate at :682-685; no DB row → LONG instances get 0 adds (dump: LONG OBV created ≥9/4: 0 adds). pyramid_min_move_pct default 3.0 price% (:337, :687-691) = ROI 6% at lev 2, stricter than sajangnim_pyramid_trigger_roi=5 (:676).

C5 After a TP1 partial the status becomes TP1_DONE_PARTIAL (tp_sl_orchestrator.py:441), which is not in STAGES_WITH_NEXT (app/core/strategy_status.py:116-120) that stage_trigger_worker filters on (:37-38, :303) → no worker evaluates stage 2/3, while force-SL keeps the 10 USDT residue because _has_next_stage only checks plan rows (tp_sl_orchestrator.py:580-620, :702-719). Residue then rides to exchange liquidation. Dump #4421 SOPHUSDT (stage 1, total_capital 1210, override 5): FORCE_STOP_LOSS_TRIGGERED 6, FORCE_SL_RESIDUE_KEPT 7, LIQUIDATION_RISK_ALERT 5, ended by RECONCILE_FLAT_POSITION_CLEANUP.

C6 Manual instance gets fixed TP1 15% (strategy_service.py:588 TP1_PCT_DEFAULT; risk_constants.py:112); adaptive_tp callers are only auto_bb_breakdown_worker.py:1875 and bb_mid_line_worker.py:400-401, so adaptive_tp_enabled=1 has no effect on it.

C7 Stage 1 is LIMIT at start_price, not MARKET (strategy_calculator.py:249-258 trigger_price=price; execution_service.py:273 no force_market; :1166/:1257), status/current_stage set on placement (:274-275).

C8 Fix 322 exemption: override 25 → v130 stage gate exempt (risk_service.py:260-265). Peak-stall (Fix 260/342) applies only to split/stage_ladder (stage_trigger_worker.py:918-923). reset_peak_pnl has a single caller at COMPLETED (tp_sl_orchestrator.py:449). Force-SL alert pnl_pct divides by total_capital=910 (tp_sl_orchestrator.py:762-768) → under-reported.

## refuted
R1 T1-#3 "25 → about 0.81% after the first +300" is wrong for the actual instance. It assumes total_capital=10, but strategy_service.py:586 stores template total (910); the ratchet is 25→18.80→15.07 (T2/T4/T5 have it right). The cited #4392 2.68 / #3966 3.12 are ratchets from base 5 (pre-Fix 362), not evidence of a 25-base collapse; no override=25 instance exists in the dump.

R2 T2-#4 "post-TP adds structurally cause a trailing full close" is overstated: Fix 358 added an explicit guard (success_pyramiding_worker.py:345-349, pyramid_after_tp_min_trail_room_pct default 2.0) that skips adds when trailing exit is within 2%p. Still plausible, not structural; unmeasured after Fix 358.

R3 T5-#6 "strategy_suggestions.py is the only consumer of auto_obv_stage2/3_trigger" is inaccurate: auto_bb_breakdown_worker.py:1732-1733 also reads it (f"auto_obv_{k}") for the auto _OBV_HOLD template. Conclusion (dead for manual instances) stands.

R4 T3-#1's "real instances #1728/#1731/#1843 match this pattern" is consistent but not proof: RECONCILE_TRIGGER_RECOVERED is also emitted on a plain user-stream miss (reconcile_worker.py:536-540), and those instances used 5310/8610/3600 capital before Fix 304/322 — not the 10/300/600 ladder. Treat as unproven.

R5 T3-#9 / T4-#2 second-order effects (trailing peak not reset at stage entry; residue inheriting the ratcheted SL) are code-true but their money impact is unmeasured: in path B after a TP the worker never fires stage 2 anyway (C5), so the peak-reset issue is mostly moot for this flow.

R6 T1-#5 (creation-time gates: concurrent-strategy cap, exchange (symbol,side) position, Fix 303) — not verified here; not flow-changing for the question asked. unknown.

## ranked
Money-impact ranking for ladder 10/300/600, lev 2, override 25 (per-step: 의도 → 코드 → 판정 → 근거 → 고치는 법):

1. [~900 of 910 USDT never enter at the intended spot, or enter as an ungated zombie at the worst spot] B-path "손실 → 수익 가능한 자리에 300 (2단계)" / "다시 좋은 자리에 600 (3단계)" → signal fires (no price check) but the order is LIMIT at start×1.10 (SHORT stage 2 = ROI −20% on stage 1) and start×1.32 (stage 3); if mark is on the favorable side it sits unfilled while current_stage→2 and reconcile marks is_triggered=True → 불일치 → stage_trigger_worker.py:826-830, :918-923, :1459-1461; execution_service.py:1166, :1257, :359-362; strategy_calculator.py:31-49, :317-334; reconcile_worker.py:540-548 → Code: at stage_trigger_worker.py:1459 pass force_market=(_ps_force_market or _is_obv_mode) (same rationale as Fix 260), or null trigger_price on plans ≥2 for OBV templates. Setting workaround: none.

2. [A-path can be cut: a winning 610-margin position is closed to 10 USDT, then #1 fires] A-path "이익 지속 → 300 ×2 → 인스턴스 옵션대로" → OBV stage 2 has no ROI<0 condition; when check_stage_entry_signal says yes during profit, _trim_before_stage realizes and reduces to 10 USDT margin, then places the stage-2 LIMIT (unfillable if in profit) → 불일치 → stage_trigger_worker.py:792-855; execution_service.py:355, :1849-1918; stage_trim.compute_trim has no PnL input → Code: OBV branch requires unrealized ROI < 0 (or below auto_obv_stage2_trigger, currently a dead key: only strategy_suggestions.py:883-906 / auto_bb_breakdown_worker.py:1732) and _trim_before_stage skips when ROI ≥ 0. Setting: stage_trim_before_next_enabled=0 removes the trim but not the premature stage entry, and Fix 322 keeps SL gates exempt (risk_service.py:260-265).

3. [SL tightens 25→18.8→15.07; on 610 margin stop at ≈ −92 instead of −152 USDT, and the 600-stage inherits 15.07] "그 외는 인스턴스 옵션대로(손절 25)" → Fix 269 ratchets force_sl_roi_override against planned total_capital 910 on every add; TRIM/partial-SL never restores it → 불일치 → execution_service.py:1657-1671; strategy_service.py:586; templates.py:147; tp_sl_orchestrator.py:683-704; dump #2091 3.64=5×1600/2200 → Setting: pyramid_cap_loss_enabled=0 (success_pyramiding_worker.py:273-284). Code: base the ratchet on real margin, and restore override/total_capital in the Fix 338 reset.

4. [LONG instances: A-path adds are 0; SHORT: trigger is effectively ROI 6 not 5] "이익 지속 시 300 추가 (방향 무관)" → side gate default SHORT; min favorable move 3% price (= ROI 6 at lev 2) → 불일치 → success_pyramiding_worker.py:309, :337, :394-405, :676-691; dump LONG OBV ≥9/4 adds 0 → Setting: pyramid_sides=LONG,SHORT; pyramid_min_move_pct=2.5.

5. [After any TP1 partial, B-path is dead and the 10 USDT residue rides to liquidation (≤10 USDT loss, but 300/600 never enter)] "부분손절 후 10 남기고 모니터링 → 600 진입" → TP1_DONE_PARTIAL is not in STAGES_WITH_NEXT so no worker evaluates stage 2/3, while force-SL keeps the residue because _has_next_stage sees plan rows → 불일치 → app/core/strategy_status.py:116-120; stage_trigger_worker.py:303; tp_sl_orchestrator.py:441, :580-620, :702-719; dump #4421 → Code: add TP{n}_DONE_PARTIAL to the stage worker's candidate set, or make _has_next_stage also require a status the worker scans (else close the residue).

6. [Fixed TP1 15% on a manual instance; adaptive_tp_enabled=1 ignored] "익절은 옵션대로" → TP1_PCT_DEFAULT hard-set at creation; pick_tp1 called only by 2 auto workers → 불일치(설정 무효) → strategy_service.py:588; auto_bb_breakdown_worker.py:1875; bb_mid_line_worker.py:400 → Code: call adaptive_tp.pick_tp1 in create_strategy_instance when adaptive_tp_enabled; or set TP1 per instance in the modal.

7. [Low/cosmetic] Stage 1 is LIMIT at start_price, not instant MARKET (execution_service.py:273, :1166; strategy_calculator.py:249-258) → use 「현재가」 or pass force_market. total_capital=910 makes dashboard ROI and force-SL alerts ~91× under-reported (tp_sl_orchestrator.py:762-768). auto_obv_stage2/3_trigger=-5 are dead for this instance. Trailing peak reset only at COMPLETED (tp_sl_orchestrator.py:449) — plausible early trailing exit after a stage entry, unmeasured.

Answer to "does it work exactly like that today": No. Path A works only for SHORT, is capped by a 3%-move gate, tightens the SL on each add, and can be cut short by an ungated stage-2 signal. Path B does not work at all as described: stage 2/3 are price-limit orders at ±10%/±32% from the start price, not signal-timed market entries, and after any TP the stage worker stops watching the residue.