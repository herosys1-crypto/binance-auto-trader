## T2 — 수익 경로(A) 단계별 대조표 (수동 생성 OBV_REVERSE 인스턴스, 10/300/600, 레버리지 2, hedge)

| # | 사장님 의도 | 코드 동작 (실값) | 일치 | 근거 (file:line) | 고치는 법 |
|---|---|---|---|---|---|
| A0 | 수동 10 USDT 진입 후 워커가 이 포지션을 감시한다 | 후보 = status ∈ {STAGE{n}_OPEN, TP{n}_DONE_PARTIAL, TRAILING_ARMED, CRISIS_TP1_DONE} (STOPPING/MANUAL_CLEANUP 제외) + qty≠0 + current_stage≥1 + capital_management_mode≠split_entry + strategy_type∉{bb_mid_line, surge_peak_ladder}. 수동 모달 인스턴스는 mode="fixed", type=DYNAMIC_*, 템플릿명 `_quick_*` 이므로 **포함**. strategy_type 화이트리스트는 Fix 185 로 제거됨 | 일치 | success_pyramiding_worker.py:94-96, 532-572, 617-634; strategy_status.py:62-69; single_entry_guard.py:49-53; cm-submit.js:229 | — |
| A1 | 스위치 | `sajangnim_pyramid_enabled` 행 **없음** → 켜짐(행이 있고 ≤0 일 때만 OFF) | 일치 | success_pyramiding_worker.py:484-494 | — |
| A2 | 「지속적인 손익」= 이익 중 | ROI = (mark−avg)/avg×100×leverage (SHORT 부호 반대), Redis `mark_price:{symbol}` 사용. 트리거 = `sajangnim_pyramid_trigger_roi`=**5** (live 행). 추가로 **가격 이동 ≥ pyramid_min_move_pct=3.0**(행 없음→기본) — 레버리지 2 에서 ROI 5% = 가격 2.5% 이므로 실제 구속 조건은 **가격 3% = ROI 6%** | 대체로 일치(문턱이 6%) | success_pyramiding_worker.py:654-690, 408-452, 324-346 | 낮추려면 SystemSetting `pyramid_min_move_pct` (예 2.5) |
| A3 | LONG·SHORT 모두 추가 | `pyramid_sides` 행 **없음 → 기본 "SHORT"** → **LONG 인스턴스는 `side_not_allowed` 로 영구 스킵** (Fix 348 실측 「저점 LONG 은 어떤 조건에서도 음수」에 근거) | **불일치** | success_pyramiding_worker.py:308-321, 394-405, 682-685 | 의도대로 하려면 SystemSetting `pyramid_sides` = `LONG,SHORT` (재시작 불필요) |
| A4 | 차트가 계속 가는 중 | peak(Redis `pyramid_peak:{sym}:{side}`, TTL 1h) 대비 되돌림 > 2.5% 면 스킵, start_price 대비 유리 이동 < 0.5% 면 스킵 | 일치 | success_pyramiding_worker.py:49-50, 216-227, 692-714 | — |
| A5 | 보조지표가 확실히 확인 | `pyramid_indicator_gate_enabled` 행 없음→ON. 판정 = **15m MACD hist 완성봉 3봉 연속 내 편 방향 가속**(`pyramid_hist_accel_bars` 기본 3), 4H 는 참고만(`pyramid_4h_veto_enabled` 없음→reference). 몸통 성장(Fix 360)은 기본 shadow=차단 안 함 | 일치 | success_pyramiding_worker.py:288-304, 911-953; trend_4h_gate.py:396-443, 336-371 | 4H 도 막게 하려면 `pyramid_4h_veto_enabled=1` |
| A6 | 익절 뒤에도 추가 가능 | status 가 TP*/TRAILING_ARMED 면 `pyramid_after_tp_enabled`(없음→ON) + 트레일링 여유 = retrace(5) − (max_profit − roi) ≥ `pyramid_after_tp_min_trail_room_pct`(없음→2.0) 필요 | 일치(조건부) | success_pyramiding_worker.py:311-392, 715-732 | — |
| A7 | 최대 2번 | `pyramid_count:sid:{id}` Redis, `MAX_PYRAMID_COUNT=2`, TTL 7일; 심볼:side 쿨다운 300초(실패·예외도 쿨다운); 사이클당 3건 | 일치 | success_pyramiding_worker.py:61, 51, 149-213, 636-647, 971, 1024-1027 | — |
| A8 | 300 USDT 씩 | `get_pyramid_capital` → SystemSetting `sajangnim_pyramid_capital`=**300**(live 행) | 일치 | success_pyramiding_worker.py:829-834; sajangnim_capital.py:94-126 | — |
| A9 | 같은 포지션에 얹는다 | `add_position_now(MARKET, mode="preserve", cap_loss=True)`: qty = 300×lev/mark, **stage_no=NULL**(ADHOC_M), positionSide=strategy.side(hedge OK). preserve = status·max_profit_pct 유지, current_stage **불변** | 일치 | success_pyramiding_worker.py:955-967; execution_service.py:1461-1483, 1569-1594, 1949-1970 | — |
| A10 | 자본 표기 | `total_capital += 300` (MARKET 즉시). 단 인스턴스의 total_capital 은 **투입 10 이 아니라 템플릿 합계**(sum(capitals), 예 910; live 신규 OBV #4421 = 1210) | 부분 일치(계획액+추가액 혼합) | execution_service.py:1615-1628; strategy_service.py:586; admin/templates.py:147 | 표기 분리는 별건(#2433 메모) |
| A11 | 손절은 「인스턴스 옵션」(−25%) 그대로 | **Fix 269 cap_loss(행 없음→ON)** 가 추가 직후 `force_sl_roi_override` 를 재작성: new = old×old_cap/new_cap. (i) 실제 DB(총자본=템플릿 합 910): 25 → **18.80** → **15.07** (1210 이면 25→20.03→16.71, live #4416/#4417 과 정확히 일치). (ii) 과제 전제(총자본=10): 25 → **0.8065** → **0.4098** = 가격 0.2% 되돌림에 손절. 어느 쪽이든 「인스턴스 옵션 그대로」가 **아님** | **불일치** | execution_service.py:1657-1683; success_pyramiding_worker.py:269-285, 891 | 옵션 유지: SystemSetting `pyramid_cap_loss_enabled=0`. 또는 코드에서 old_cap 을 실투입 증거금으로 계산 |
| A12 | 추가 후 TP1/사다리/트레일링이 합산 포지션에 적용 | risk_service 가 **새 평단** 기준 pnl_ratio 로 TP·트레일링 판정. TP1 = tp1_pct_override=**15**(수동 생성 기본; Fix 299 적응 TP 는 auto 워커 전용이라 수동 인스턴스엔 미적용). 트레일링(Fix 31) = TP 하나라도 발동 후 peak−5. force SL = A11 의 재작성된 override, v130 단계 게이트는 override>0 이라 면제(Fix 322) | 일치(TP 는 15 고정) | risk_service.py:572-589, 794-839, 776; strategy_service.py:588; risk_constants.py:112; adaptive_tp 호출처 auto_bb_breakdown_worker.py:1876, bb_mid_line_worker.py:396; risk_service.py:260-268, 462-471 | 적응 TP 원하면 인스턴스 tp1 수동 편집 또는 create 경로에 pick_tp1 배선 |
| A13 | (부작용) 평단 희석 | 10 에 ROI+6% 인 상태에서 300 을 얹으면 합산 ROI ≈ +0.2% 로 급락하는데 preserve 는 max_profit_pct(6) 를 유지 → TP 발동 이후였다면 트레일링 조건 `pnl ≤ peak−5` 가 **즉시 성립**(Fix 358 실측 「추가 18초 뒤 전량 청산」의 기전), 2번째 추가는 `trailing_imminent` 로 막힘 | 불일치(익절 후 추가는 구조적으로 거의 불가) | risk_service.py:826-839; success_pyramiding_worker.py:380-391, 722-732; execution_service.py:1569-1594 | 추가 시 peak 를 새 평단 기준으로 재계산하거나 익절 전 추가만 허용 |
| A14 | 추가는 사다리 단계를 소비하지 않는다 | 맞음: stage_no NULL, current_stage 는 `trigger_next_stage` 만 올림. 실측 #4392 추가 7회에도 current_stage=1 | 일치 | execution_service.py:1479, 358; history_enriched #4392 | — |
| A15 | 추가 후 2단계도 올 수 있나 | **있다, 그리고 위험하다.** OBV 모드 2단계는 손실 조건 없이 `check_stage_entry_signal`(obv_gate + confirm_peak) 만으로 발동(`auto_obv_stage2_trigger=-5` 는 auto 워커 생성용이라 stage_trigger_worker 는 읽지 않음). 발동 시 `_trim_before_stage` 가 **손익 무관하게** 10 USDT 증거금만 남기고 610 짜리 이익 포지션을 청산한 뒤 300 을 2단계로 넣고 status=STAGE2_OPEN_PENDING. 피라미딩 카운터는 여기서 리셋되지 않음(리셋은 force-SL 부분손절 경로만) | **불일치** | stage_trigger_worker.py:792-849, 1161-1166, 1459-1461; execution_service.py:355-362, 1846-1920; stage_trim.py:112-165, 294-363; auto_obv 설정 사용처 strategy_suggestions.py:883-884, auto_bb_breakdown_worker.py:341; tp_sl_orchestrator.py:692-697 | 2단계는 손실 구간에서만 열리게 ROI 게이트를 stage_trigger OBV 분기에 추가, 또는 이익 중이면 trim 을 건너뛰게 |