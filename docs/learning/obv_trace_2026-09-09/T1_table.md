## T1 — 생성 + 1단계 (신 전략 OBV 자동 모달, capitals [10,300,600], 레버 2, 헤지)

기준 코드: worktree HEAD 3e04034 (= 0d174bf + docs 커밋, 코드 동일). 라이브 설정은 `history_dump.json` settings_history 기준.

| # | 단계 / 의도 (사장님) | 코드 동작 (오늘) | 일치 | 근거 (file:line) | 고치는 법 |
|---|---|---|---|---|---|
| 1 | 모달 「📊 새 전략 (OBV 자동)」 = 직접 입력, 레버 2, OBV 모드 | `openCreateChartObvModal()` 이 `cmState._triggerMode='OBV_REVERSE'`, `mode='direct'` 강제, 템플릿 선택 무효. 레버 칸 비어 있으면 2 채움, TP qty 10/15/20/25 자동. 2~10단계 트리거 % 칸은 **비활성만** (값은 남김). 「청산 후 재진입」 자동 체크 없음(Fix 323) | 일치 | `app/static/js/cm-open-modal.js:37-57`, `:92-100`, `:107-115`, `:116-131` | — |
| 2 | 저장되는 템플릿 | 제출 시 `POST /admin/strategy-templates` 로 `_quick_<ts>` 템플릿 1개 생성: capitals [10,300,600], trigger_percents(비활성 칸 값 그대로 전송), `trigger_mode='OBV_REVERSE'`, `stop_loss_percent_of_capital=90`(기본), `reentry_policy='manual_ready'`. 서버는 **`total_capital = sum(capitals) = 910`** 저장 | 일치 (단, 910 = 「계획」) | `cm-submit.js:207-241`, `cm-collectors.js:115`, `app/api/v1/admin/templates.py:147`, `:172`, `:226` | — |
| 3 | 인스턴스에 저장되는 값 | `POST /strategies` → `create_strategy_instance`: `leverage=preview.leverage`(= leverage_override 2), **`total_capital=template.total_capital=910`**(투입 10 아님), `status='WAITING'`, `tp1_pct_override=15`(TP1_PCT_DEFAULT), `force_sl_enabled_override=True`, `force_sl_roi_override=get_new_strategy_force_sl_roi()` → 설정키 `force_sl_roi_new_default` **행 없음 → 25**, `retry_after_liquidation_enabled=False`, `retry_trigger_pct=10`, `capital_management_mode='fixed'`(모달이 항상 'fixed', 예약이면 'scheduled') | 일치 (사장님 옵션대로) / 표기 불일치 (910) | `cm-submit.js:246-262`, `app/api/v1/strategies/crud.py:35-56`, `app/services/strategy_service.py:89`, `:577-596`, `app/services/system_settings_service.py:98-109`, `app/core/risk_constants.py:85-86`, `:112` | total_capital 을 「투입」으로 쓰려면 생성 시 `capitals[0]` 로 두고 단계 진입마다 가산(현재는 add_position 만 가산: `execution_service.py:1615-1620`) |
| 4 | 단계 계획(stage_plans) | preview 가 stage1 = `trigger_mode="IMMEDIATE"`, **`trigger_price = start_price`**, `planned_qty = 10×2/start_price`; 2·3단계는 비활성 칸의 트리거 % 로 가격을 미리 계산(OBV 워커는 안 씀) | 일치 | `app/services/strategy_calculator.py:240-261`, `:385-395`(qty=capital×lev/price), `strategy_service.py:597-610` | — |
| 5 | 생성 시점 차단 게이트 (수동에도 적용) | ① 같은 계정·심볼·방향 활성 전략 있으면 거부 ② Kill-Switch ③ `max_leverage`(env, 템플릿 빈값) ④ 화이트리스트 — env `ALLOWED_SYMBOLS_CSV` 비면 스킵, DB `whitelist_enabled=false` ⑤ **계정당 동시 활성 전략 수 `max_concurrent_strategies_per_account`** — 자동 워커 전략까지 전부 셈(코드 기본 10, .env 실값 unknown) ⑥ Binance 계정 조회 실패 시 거부 ⑦ **거래소에 같은 (symbol, positionSide) 포지션 있으면 거부** ⑧ OBV 모드는 130% 예약 검사 **면제**, 잔액 검사는 **1단계 10 USDT 만** ⑨ 청산거리(env 빈값) | 대체로 일치 — 단 ⑤·⑦ 은 수동을 막을 수 있음 | `strategy_service.py:137-146`, `:221-226`, `:234-243`, `:248-263`, `:267-277`, `:286-303`, `:309-327`, `:440-441`, `:467-486`, `:505-518` | ⑤ 가 걸리면 .env `MAX_CONCURRENT_STRATEGIES_PER_ACCOUNT` 상향 (config.py:38) |
| 6 | 1단계 = 수동 진입 10 USDT 즉시 | `POST /strategies/{id}/start` → `start_stage1`: KS 확인 → Fix 310 chg24 게이트(라이브 `entry_chg24_gate_enabled=1`) 는 **`_quick_` 템플릿명이면 통과** → Fix 327 지지선 게이트는 `support_score_gate_enabled` **행 없음 = OFF**, fail-open → ISOLATED·레버 적용 → 발주. **슬롯 상한(position_limit) 은 start_stage1/컨트롤에 없음.** 실패(ValueError) 시 전략은 `STOPPED` 로 마킹 | 일치 (게이트에 안 막힘) | `app/api/v1/strategies/control.py:26-56`, `app/services/execution_service.py:188-196`, `:207-228`, `:260-279`, `app/services/chg24_entry_gate.py:171-172`, `:184-187`, `app/services/support_score.py:128-131`, `:359`; position_limit 호출처 목록에 execution_service/control 없음 | — |
| 7 | 1단계 주문 형태 | `_place_stage_entry_order`: 먼저 **Fix 303 심볼 제외 목록**(BTC/ETH 계열, 수동 포함) 검사 → `trigger_price` 가 있으므로 **LIMIT GTC @ start_price** (MARKET 은 trigger_price None 이거나 force_market 일 때만). `positionSide=strategy.side` = 헤지 모드 전제. 상태 `STAGE1_OPEN_PENDING`, `current_stage=1` 은 **발주만으로** 올라감 | 부분 불일치 — 「즉시 진입」이 아니라 시작가 지정가. 모달 필수값(`start_price gt=0`) 이라 MARKET 경로 불가 | `execution_service.py:1154-1166`, `:1246-1250`, `:280-282`, `:1923-1947`, `app/schemas/strategy.py:40` | 즉시 체결을 원하면 「💲 현재가」 버튼(현재가 LIMIT = 사실상 taker) 을 쓰거나, 모달 경로에 `trigger_price=None`(MARKET) 옵션 추가 |
| 8 | ROI (손절/TP/피라미딩 판정) | 엔진 ROI 는 전부 **`(mark−avg)/avg × 100 × leverage`** (SHORT 부호 반대). 강제손절 `force_sl_should_trigger`, 트레일링/TP `pnl_ratio = raw_pnl_pct × leverage`, 피라미딩 `roi_pct = price_pct × lev`. **`unrealized_pnl / total_capital` 은 엔진 판정에 안 쓰임** → 10/910 투입이라도 판정은 정확 | 일치 | `app/services/risk_service.py:48-72`, `:342`, `:518`, `:587-589`, `app/workers/success_pyramiding_worker.py:668-674` | — |
| 9 | 강제손절 −25% 가 1단계부터 적용 | `resolve_force_sl` 로 override(True, 25) 가 전역 80 을 이김. v130 「모든 단계 진입 후」 게이트는 Fix 322 `_stage_gate_exempt` 가 `force_sl_enabled_override=True` 면 면제 → 1단계(10 USDT) 에서 ROI −25%(2x = 가격 −12.5%, 손실 ≈2.5 USDT) 도달 시 발동. 템플릿 SL 90% 는 ROI −90 이라 사실상 무의미 | 일치 (옵션대로) | `risk_service.py:406-416`, `:255-264`, `:470-505`, `:334-344` | — |
| 10 | total_capital=910 이 쓰이는 곳 (표기) | 대시보드 「전략 ROI」 = `pnl×lev/total_capital` → 10 투입인데 910 으로 나눠 **91배 과소**(「포지션 ROI」는 정확). 강제손절 Telegram 손실률, SL 진행률 80% 알림도 total_capital 기준. 예약 계산은 Fix 344 로 사다리 미진입 단계 제외(`sajangnim_ladder_stages_enabled=1`) | 불일치 (화면·알림만) | `app/static/js/strategies-list.js:572-588`, `app/services/tp_sl_orchestrator.py:766-773`, `:893-901`, `risk_service.py:1064-1080`, `strategy_service.py:395-409` | Fix 341 의 `invested_capital_computed`(helpers.py:65-83) 로 화면 ROI 분모 교체 |
| 11 | TP = 인스턴스 옵션 | TP1 = `tp1_pct_override` 15 = **ROI 15%** (2x → 가격 7.5%). 라이브 `adaptive_tp_enabled=1` 이지만 `pick_tp1` 호출처는 auto_bb_breakdown/bb_mid_line 워커뿐 → **모달 인스턴스엔 적응 TP 미적용** | 일치 (고정 15) | `risk_service.py:669-670`, `:781-800`; `grep pick_tp1(` 결과 워커 2곳 | — |
| 12 | 이익 경로 A 의 「+300」 이 1단계에 걸리는 조건 (T2 예고) | 피라미딩 트리거 ROI 5(라이브), **허용 방향 기본 `{"SHORT"}`**(`pyramid_sides` 행 없음) → LONG 수동 인스턴스는 추가 0건, 최소 유리 이동 3%(가격), MAX 2. 그리고 `pyramid_cap_loss_enabled` 행 없음 = **기본 ON** → 첫 +300 직후 `force_sl_roi_override` 를 25×10/310 = **0.81% ROI**(2x = 가격 0.4%) 로 낮춤 | 불일치 (Fix 362 −25% × Fix 269 상호작용) | `success_pyramiding_worker.py:395-405`, `:327-330`, `:61`, `:276-282`, `:890-891`, `:966`, `execution_service.py:1657-1671` | `pyramid_sides=LONG,SHORT` 설정 / `pyramid_cap_loss_enabled=0` 또는 ratchet 을 「단계 자본」이 아니라 「사다리 합」 기준으로 |