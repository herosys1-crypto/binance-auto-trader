## T3 — 손실 경로(B) 2·3단계 추적: 「새전략 OBV 자동」(모달 수동 생성, trigger_mode=OBV_REVERSE, 사다리 10/300/600 직접 입력, 레버 2, 헤지)

전제(코드로 확인): 모달 OBV 생성은 `capital_management_mode="fixed"`(cm-submit.js:238, strategy_service.py:594), 템플릿은 `_quick_*`/`DYNAMIC_SHORT|LONG`(cm-submit.js:203-218), 단계 자본은 **모달에 입력한 capitals** 가 stage_plan.planned_capital 로 저장(strategy_service.py:599-611) — `sajangnim_capital_ladder` 는 이 인스턴스의 단계 자본에 쓰이지 않는다(그 키는 자동 워커·피라미딩 300 에만: sajangnim_capital.py:56-57, 94-126). 신규 인스턴스 `force_sl_roi_override=25`(strategy_service.py:588-590, risk_constants.py:86), `total_capital=template.total_capital`(=입력 자본 합, strategy_service.py:580).

| # | 사장님 의도 | 코드 동작(오늘 배포 0d174bf 기준) | 일치 | 근거(file:line) | 고치는 법 |
|---|---|---|---|---|---|
| B-0 | 10 USDT 수동 1단계 진입 | 1단계는 stage_plan.trigger_mode=IMMEDIATE·trigger_price=시작가로 LIMIT(시작가) 발주. 수동 `_quick_` 는 Fix 310(당일 10%) 게이트 제외 | ✅ | strategy_calculator.py:248-262, execution_service.py:188-286, 1252-1263 | — |
| B-1 | 손실 → 계속 모니터링 | 상태 STAGE1_OPEN 이면 stage_trigger_worker 가 15초마다 다음 단계(2) 검사. 단, 모달에서 「청산 후 재진입」을 켰다면 trim ON(1) 또는 손절 명시(25>0)라 정상 감시 유지 | ✅ | stage_trigger_worker.py:375-397, 408-427 | — |
| B-2a | 「다시 이익이 가능한 자리」에서 2단계 300 | **ROI/손익 조건이 전혀 없다.** OBV 모드 2·3단계는 오직 `check_stage_entry_signal` = ①obv_gate ②양방향 blocklist ③SHORT regime ③-b SHORT 진입창(설정 `entry_window_short_enabled=true` 라 **차단 활성**) ④confirm_peak(15m 반복 정점/저점 ≥2회 + RSI≥65/CCI≥80(SHORT)·RSI≤35/CCI≤-80(LONG) 중 2/3 꺾임). 이익 중이든 손실 중이든 신호만 맞으면 발동 | ⚠️ 부분일치 | stage_trigger_worker.py:792-855(830 호출), stage_entry_signal.py:76-193, peak_confirmation.py:46-51,152-192 | 「손실 중일 때만」이 의도면 OBV 분기에 ROI<0 조건(코드) 추가. 「이익이 가능한 자리」=confirm_peak 정의가 사장님 정의와 같은지는 별도 검증 필요 |
| B-2b | `auto_obv_stage2_trigger=-5 / stage3=-5` 가 「-5% 손실 트리거」? | **아니다.** 이 키는 stage_trigger_worker·risk_service 어디에서도 읽지 않는다. 읽는 곳은 설정 API(strategy_suggestions.py:878-906)와 **자동 `_OBV_HOLD` 워커의 템플릿 trigger_percents(가격 %)**뿐. 수동 모달 인스턴스에는 **무관(죽은 설정)**. ROI 가 아니라 가격 % 이며 그마저 OBV 발동 판정에는 안 쓰인다 | ❌ 불일치(설정이 의도와 다른 것) | auto_bb_breakdown_worker.py:1721-1738; grep 결과 stage_trigger_worker/risk_service 0건 | 화면 문구·설정을 「가격 %, 자동 OBV_HOLD 전용」으로 바로잡거나 제거. -5% ROI 를 2단계 조건으로 쓰려면 B-2a 코드 |
| B-2c | 정점-주춤(Fix 260) 적용? | **적용 안 됨.** `_is_ladder`(capital_management_mode=="stage_ladder")·`_is_split` 만 대상. 모달 OBV 는 "fixed" → 가격 경로도 아니고(OBV 분기가 먼저) 정점-주춤도 아니다. Fix 312 꺾임 대기도 `auto_bb_break_SAJANGNIM*` 접두사만 | ✅(설계상 제외) | stage_trigger_worker.py:896-934, 918-921; stage_entry_timing.py:74,112-126 | 정점-주춤을 OBV 에도 쓰려면 `_is_ladder` 판정에 OBV 모드 포함(코드) |
| B-2d | 2단계 300 = 시장가 | **LIMIT 이다.** 모달이 2·3단계 trigger% 를 수집(빈칸이면 캘큘레이터 기본 2단계 10%·마지막 20%)해 stage_plan.trigger_price 를 만든다(SHORT=PRICE_UP_PCT, LONG=PRICE_DOWN_PCT). OBV 분기는 `force_market` 을 주지 않으므로(`_ps_force_market=False`) `_place_stage_entry_order` 가 **trigger_price 로 LIMIT** 발주. 발주만으로 `current_stage=2`·`STAGE2_OPEN_PENDING` 이 되고, 2분 뒤 reconcile 이 「미체결 plan + 실포지션≠0(1단계 잔량)」을 보고 `is_triggered=True` 로 **거짓 회복** → 자본 없이 단계 소진 + 호가창 좀비 LIMIT(Fix 260 docstring 이 경고한 그 구조) | ❌ 불일치(중대) | cm-collectors.js:60-102; strategy_calculator.py:272-334(기본 10%/20%); stage_trigger_worker.py:1444-1461; execution_service.py:1166(MARKET 조건), 1252-1263(LIMIT), 305-364(313-323 경고, 358-362 상태 승격); reconcile_worker.py:540-559 | 코드: OBV 분기 발동 시 `force_market=True` 로 호출(또는 OBV 템플릿 2~N단계 trigger_price=None 저장). 실인스턴스 3건(#1728/#1731/#1843)이 생성 10~12분 뒤 RECONCILE_TRIGGER_RECOVERED+MARTINGALE_GATE_MISSING = 이 패턴과 일치(주문 타입은 집계 덤프라 unknown) |
| B-2e | 2단계 전 「10 남기고」 정리 | `trigger_next_stage` → `_trim_before_stage`: trim ON(설정 1)·stage>1 → compute_trim(레버 반영 증거금 10) → 1단계 10 USDT(명목 20)는 SKIP=정리 없이 진입(사장님 「첫진입 10 이라 손절없이」). **손익을 보지 않으므로** 이익 중 피라미딩으로 310 이 된 포지션도 OBV 2단계가 뜨면 10 만 남기고 잘린 뒤 300 재진입 | ⚠️ 손실경로 ✅ / 이익경로 충돌 | execution_service.py:1784-1920(1849,1890-1897,1916); stage_trim.py:235-363(308-325) | 이익 중에는 trim 을 건너뛰거나 OBV 단계 발동을 ROI<0 로 한정(코드) |
| B-2f | 2단계 진입 후 평단·total_capital·TP | 체결 시 stream_service 가 STAGE2_OPEN·is_triggered·qty 갱신, 평단은 Binance ep(=혼합 평단)로 갱신. **total_capital 은 안 바뀐다**(생성 시 입력 합 910 고정; 부분손절·단계진입 어디서도 안 줄고 안 늘음 — 늘리는 건 add_position_now 뿐). TP1~n·트레일링·강제손절 ROI 는 모두 새 혼합 평단 기준으로 재계산. Redis peak(트레일링 기준)는 리셋되지 않음(reset_peak_pnl 호출처 = COMPLETED 뿐) | ⚠️ | stream_service.py:78,97-104,466-473; execution_service.py:1615-1628(add 만 갱신); tp_sl_orchestrator.py:449; risk_service.py:585-589,805-847 | 표시용이면 total_capital 을 실증거금으로 동기화(코드); 트레일링 peak 를 단계 진입 시 리셋할지는 사장님 결정 |
| B-2g | 2단계 후 손절 % | 인스턴스 override 25 그대로(단계 진입은 Fix 269 cap_loss 를 안 탐) → 310 증거금 × -25% = **-77.5 USDT** 에서 부분손절. 손절 게이트(v130 「전 단계 진입 후」)는 override>0 로 면제 | ✅ 옵션대로 | risk_service.py:394-434(431-434 ROI=가격변동×레버), 192-268(261-264 면제); execution_service.py:1657-1688(add 전용) | — |
| B-3 | 다시 손실 → **10 USDT 만 남기고 부분손절** | evaluate_force_stop_loss(ROI=(평단 vs mark)×레버 ≤ -25) → `_execute_force_stop_loss`: trim ON → compute_trim → TRIM(300+10 → 잔량 증거금 10)·Fix 338 피라미딩 카운터 리셋·미체결 취소 안 함·상태 유지(STAGE2_OPEN)·전략 종료 안 함. cumulative_realized_loss 누적(상한 설정 `stage_max_cumulative_loss_usdt` 없음=무제한) | ✅ | tp_sl_orchestrator.py:623-758(683-700 TRIM, 730-747, 751-758); stream_service.py:372-399; stage_trim.py:366-412 | — |
| B-4a | 잔량 10 으로 「다시 모니터링」 | 부분청산이라 status=STAGE2_OPEN·current_stage=2 유지, plan3.is_triggered=False → 워커가 3단계를 계속 검사. **평단은 그대로**(부분청산은 ep 불변)라 잔량 ROI 는 여전히 ≤-25 → 매 사이클 손절 판정은 뜨지만 compute_trim=SKIP + `_has_next_stage`=True 라 **잔량 유지**(FORCE_SL_RESIDUE_KEPT, 60분 dedup). 잔량은 3단계가 올 때까지 손절 없음 | ✅(사양) / ⚠️ 잔량은 거래소 청산까지 방치될 수 있음 | stage_trigger_worker.py:426; tp_sl_orchestrator.py:580-621, 702-719, 546-578; stream_service.py:466-473 | 잔량 최대 손실을 10 으로 보는 사양이면 그대로. 실측 #4421 참고(아래) |
| B-4b | 3단계 트리거 = ROI 기준? 잔량이 무엇을 리셋? | 3단계도 **동일 OBV 신호**(check_stage_entry_signal) 하나뿐, ROI 무관. 잔량이 리셋하는 것: 피라미딩 카운터(Fix 338)만. `auto_obv_stage3_trigger=-5` 미사용. 정점-주춤의 「재갱신」 조건도 OBV 엔 미적용 | ⚠️ (B-2a 와 동일) | tp_sl_orchestrator.py:689-696; success_pyramiding_worker.py:165-191; stage_trigger_worker.py:792-855 | B-2a 와 동일 |
| B-4c | 3단계 600 진입 | 신호 → `_trim_before_stage`(잔량 10 = SKIP, 정리 없음) → **LIMIT(trigger_price = 시작가 기준 +10%+20% 등)** 발주 → B-2d 와 같은 미체결/거짓회복 위험. 3단계가 마지막(capitals 3개)이라 이후 `next_stage>total_stages` 로 감시 종료 | ❌ (B-2d) | execution_service.py:1897; stage_trigger_worker.py:41-50, 410-413; strategy_calculator.py:272-311 | B-2d 와 동일 |
| B-5 | 이후 손익은 인스턴스 옵션 | 610(잔량10+600) 혼합 평단 기준 TP1 15%(tp1_pct_override 기본)·트레일링(TP 발동 후 peak-5%p)·강제손절 -25%. 3단계가 마지막이라 다음 손절 판정은 `_has_next_stage`=False → **전량 손절**로 종료 | ✅ | risk_service.py:805-847; tp_sl_orchestrator.py:580-621(다음 단계 없음→전량) | — |
| A-1 | (이익경로) 지속 이익 + 차트·지표 확인 → 300 ×2 | success_pyramiding_worker: 상태 ACTIVE_WITH_POSITION, ROI ≥ `sajangnim_pyramid_trigger_roi=5`, **가격 유리이동 ≥3%(pyramid_min_move_pct 기본)**, peak 되돌림 ≤2.5%, 시작가 대비 ≥0.5%, **허용 방향 기본 SHORT 만(pyramid_sides 미설정)**, 15m hist 가속 게이트(Fix 273/345 기본 ON), MAX 2회, 5분 쿨다운, 금액 `sajangnim_pyramid_capital=300`, MARKET·preserve·cap_loss(override 25 를 total_capital 비율로 축소 — total_capital 이 미진입 단계 포함 910 이라 「손실 금액 고정」 산수가 실증거금과 어긋남). DYNAMIC_* 은 제외 목록 아님 | ⚠️ LONG 인스턴스는 추가 0회 / cap_loss 산수 어긋남 | success_pyramiding_worker.py:94,61,321,394-406,408-452,536-567,643-646,660-690,692-708,911-953,961-966; execution_service.py:1657-1688; single_entry_guard.py:49-53 | LONG 도 원하면 설정 `pyramid_sides=LONG,SHORT`; cap_loss 는 실증거금 기준으로(코드) |
| A-2 | 이익경로와 OBV 단계가 서로 간섭 안 함 | 피라미딩 추가는 current_stage 를 안 올리므로 OBV 2단계 신호가 별도로 살아 있다 → 310 을 10 으로 잘라내고 300 재진입 가능(B-2e) | ❌ | 위 B-2e | 위 B-2a/B-2e |