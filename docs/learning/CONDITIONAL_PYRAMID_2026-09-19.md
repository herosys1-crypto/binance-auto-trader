# 🔼 조건 붙인 피라미딩 — 가상매매 검증 (Fix 382, 2026-09-19)

## 사장님 말씀 (verbatim)
> 피라미딩 꺼줘 왜 꺼야 하는지 알려줘
> 조건 붙인 피라미딩 가상매매로 검증해줘

## 0. 지금 상태
- 운영 `sajangnim_pyramid_enabled = 0` (9/19 사장님 지시로 끔). 자동매매 전면 중단 중에는 원래도 자동 추가가 막혀 있다.
- 피라미딩 스위치는 **하나**라서 켜면 사장님 수동 전략에도 수익 중이면 300 USDT 씩 추가한다.

## 1. 조건 없는 피라미딩 = 장세에 따라 부호가 뒤집힌다
가상매매가 모든 포지션 위에 병렬로 기록한 「300 USDT 추가」(live_both = 운영 추가 조건 · 양방향) 22,965건:

| 기간 | LONG 추가 합 | SHORT 추가 합 |
|---|---:|---:|
| 9/9~9/14 (하락장) | **−17,134** | +28,546 |
| 9/15~ (상승장) | **+66,446** | −20,739 |

→ 실력이 아니라 **방향에 거는 레버리지**다. 장세가 바뀌는 순간 늘린 만큼 잃는다.

## 2. 조건을 붙이면 (진입 시점 시장폭 = 24h 오른 종목 비율)

| 조건 | 기간 | 통과 (건 · 1건 평균) | 제외 (건 · 1건 평균) |
|---|---|---:|---:|
| **A1 LONG 추가는 시장폭 ≥ 0.55 일 때만** | 하락장 | 1,153 · **+0.94** | 4,440 · −4.10 |
| | 상승장 | 2,301 · **+15.91** | 3,079 · +9.69 |
| A2 SHORT 추가는 시장폭 ≤ 0.45 일 때만 | 하락장 | 3,525 · −1.92 | 5,324 · +6.63 |
| | 상승장 | 2,174 · −9.18 | 969 · −0.80 |

- **A1 은 두 장세 모두 통과 쪽이 나았다** (날짜별 6일 중 4일). 하락장에선 손실을 거의 다 피했다.
- A2(SHORT)는 오히려 나빴다 (6일 중 1일) — 시장이 이미 빠진 뒤의 SHORT 추가는 반등에 물린다. → SHORT 추가는 하지 않는다 (운영 `pyramid_sides=LONG` 과 같은 방향).
- 태그 기준(MKT_UP/DOWN)으로 재도 결과가 같았다.

## 3. 새 데이터로 검증 (사전등록)
- `paper_report_v3` 보고서 **4-1. 조건부 피라미딩** 표 — A1(검증 대상) · A2(대조군).
- **9/20 00:00 UTC 이후 진입분만** 센다 (위 숫자를 고를 때 본 표본은 제외).
- 숫자 0.55 / 0.45 는 「Claude가 정함」.
- 판정(9/27경, 7일): A1 통과 쪽 1건 평균이 제외 쪽보다 크고 · 통과 합이 + · 날짜별 우세가 70% 이상이면 → 사장님께 「시장폭 조건부 LONG 피라미딩」 실매매 적용을 여쭌다.
- 한계: 진입 시점 시장폭이다 (실매매로 옮기면 **추가하는 순간의 시장폭**을 쓴다 — 더 최신 정보). 9일 표본, 두 장세.

연구 스크립트: 운영 DB 읽기 전용 (`pyramid_regime.py` · `pyramid_regime_daily.py`, 결과는 이 문서).
