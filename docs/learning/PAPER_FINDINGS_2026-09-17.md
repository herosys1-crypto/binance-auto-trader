# 🧪 가상매매 → 실로직 수정 후보 (2026-09-17)

## 사장님 말씀 (verbatim)
> 가상매매를 분석해서 현 로직에 수정부분을 찾아줘

## 표본과 한계
- 운영 서버 읽기 전용 실행: 가상 보고서 v3(`_build_report_v3`, 10일) + 차트 상태 보고서(3일). 2026-09-16 23:38 UTC 기준.
- 실시간 가상 진입 22,933건. **사전등록(9/13 22:16 UTC) 뒤 표본은 6,544건·4일**이라 v3 공식 채택 문턱(7일·클러스터 300·t≥2·전후 반쪽 양수)을 **아직 넘은 것이 없다**.
  아래 「전체 8일」 수치는 사전등록 이전 데이터가 섞여 있어 **후보 판단용**이다.
- ⚠️ 기간 대부분이 하락장이라 SHORT 평균은 +, LONG 평균은 − 였다. 9/16 마지막 국면 태그는 **MKT_UP(0.65)** 으로 바뀌었다 → 방향 결론은 뒤집힐 수 있다. 그래서 아래는 **같은 창 무작위 대비 Δ**와 **짝 비교 diff** 위주로 본다.

## 확인한 운영 설정 (읽기만)
`pyramid_sides=LONG` · `sajangnim_pyramid_enabled` 행 없음(=켜짐) · `sajangnim_pyramid_capital=300` · `auto_trading_halt` 행 없음(=중단) ·
`pump_split_enabled=1` · `unified_entry_enabled=0` · `sajangnim_top_short_daily_limit=1` · `auto_bb_break_daily_limit=0` · `rf_*_mode` 행 없음(=그림자) ·
강제손절 새 전략 기본 = 방향 무관 한 키(`force_sl_roi_new_default`, 기본 25) · 시간 청산 워커 없음(`time_reverse_exit` 주석).

## A. 설정만으로 바로 줄일 수 있는 위험 (사장님 결정)

| # | 무엇 | 근거 | 제안 |
|---|---|---|---|
| A1 | **자동 LONG 추가(피라미딩)** | 가상 LONG 추가 lot 6,367건 **합 −13,060 USDT**(평균 −0.68, 전/후 반쪽 모두 −). 실설정 `pyramid_sides=LONG` = **지는 쪽에만 추가**. 9/13 KOMA·哈基米 손실도 LONG 추가. 켜짐이 기본값 | 재개 전 관제실에서 「수익 추가」 **끔**. SHORT 로 바꾸는 것은 하락장 편향이라 권하지 않음 |
| A2 | 규칙 가족 켜기 | 무작위 대비 Δ(전체 8일): `surge_start_346` −1.25(t −2.6, 반쪽 둘 다 −) · `wick_rev_long` −1.33(t −3.1) · `off8_267` **−2.06(t −4.0)** · `s1_breakdown` −0.79(t −2.0). 9/12 채택했던 3A `s2_hist` +0.13 · 3B `bottom_331` −0.34 = **효과 사라짐** | `rf_surge_long`·`rf_wick_long`·`rf_off8`·`rf_s1_breakdown` 은 **켜지 않는다**. 유일하게 통과한 규칙 = `pullback_331` LONG(Δ +1.32, t 2.8, 반쪽 둘 다 +) |
| A3 | 「본전 보호」 도입 제안 | `live_be10`(+10 도달 뒤 손절을 본전으로) = LONG −0.66(t −5.9) · SHORT **−0.96(t −9.2)** | **넣지 않는다** (실코드에 없음 — 현상 유지) |

⚠️ 9/15 `AUTO_ENTRY_READY` 분석은 `off8` 을 최고(A5/10 +1.32)로 꼽았다. 청산 방식(분할 TP1 5 · 손절 10)을 바꾸면 순위가 뒤집힌다는 뜻이다 — 규칙 가족을 켤 때는 **실제로 쓸 청산 방식으로 다시 재야** 한다.

## B. 코드 수정 후보 (사전등록 7일 차는 9/20~21에 판정)

| # | 무엇 | 근거 | 실코드 현황 | 수정안 (가족·방향별) |
|---|---|---|---|---|
| B1 | **LONG 무진전 24시간 청산** | `live_stale24`(24h 째 TP1 없고 ROI ≤ 0 → 청산) LONG **+0.96, t 10.4**, 반쪽 +0.87/+1.06 · 사전등록 +1.15(t 9.5, 후반 −0.04). SHORT 는 −0.36 = 해로움. 타이밍 채점도 LONG 37% 가 「움직임 없음」 | 시간 청산이 **아예 없다** (가상 live 엔진은 48h 만료가 있어 실코드보다 낙관적) | LONG 자동 가족에만 `stale_exit_long_hours`(기본 24, 기본 끔) |
| B2 | **LONG 손절 −25 → −15** | `live_sl15` LONG +0.23(t 2.46, 반쪽 둘 다 +, 8일 ✅) · 사전등록 +0.65(t 3.65). SHORT 는 −0.22(t −2.4) = 해로움 | 새 전략 손절이 **방향 무관 한 키**(25) | `force_sl_roi_new_default_long` / `_short` 로 분리 (LONG 15 · SHORT 25) |
| B3 | **LONG 은 깊은 조정에서만** | P3(5일 고점 대비 −10% 이하만): 유지군 −0.89 vs 제외군 **−3.11** · 사전등록 −0.29 vs −3.69 (긍정일 5/8 = 문턱 0.7 미달). 차트 상태(3일)도 같은 방향 — 일봉 `UPPER_BREAKOUT` LONG −2.47, `DEEP_PULLBACK` +7.8, 5분 바닥권 Δ +7.3(4/4) | 자동 LONG 진입에 조정 깊이 조건 없음 | LONG 자동 가족 진입에 `dist_high5d_pct ≤ −10` 게이트 (그림자부터) |
| B4 | **이미 떨어지는 시장에 SHORT 추격 금지** | P2(MKT_DOWN 이면 SHORT 건너뜀): 유지군 +2.43 vs 제외군 **+0.77** (5/6일) · 사전등록 +4.87 vs +2.69 | 국면 게이트는 LONG 추가(2②)에만 있고 **SHORT 진입엔 없다** | SHORT 자동 가족 진입에 `market_breadth_last` MKT_DOWN 제외 (그림자부터) |
| B5 | 지지 자리 SHORT 금지 (참고) | 차트 상태 3일: 일봉 「상단 돌파 뒤 큰 조정(중단선까지)」 SHORT −2.45, Δ −2.2, **0/4** · 중단선 저항 SHORT Δ −8.4(n 30) | 일봉 볼밴은 판정에 없다 (Fix 372 는 기록만) | 1주 표본 뒤 재확인 (지금은 표본 부족) |

## C. 타이밍 채점에서 본 것 (3일)
- LONG: 움직임 없음 37% · **틀린 방향 14.7%(평균 −13.2)** · 늦은 진입 3.9%(오히려 +11.2).
- SHORT: 좋은 진입 45% · 틀린 방향 10%(평균 −14.7).
- → 「늦은 진입」은 문제가 아니다. 손실은 **방향 오판**과 **안 움직이는 LONG 을 오래 드는 것**에서 난다 = B1·B3 와 같은 결론.

## 참고: 그 밖의 수치
- 추가 lot 지표 게이트: 게이트 없음(noind)이 LONG −11,538 / SHORT +59,048 로 현행(live) 보다 조금 낫다 → 지표 게이트는 효과가 없다(작은 차이, 기간 의존).
- `live_lock5`(+5 뒤 손절 −10): 양방향 ≈0 → 넣을 이유 없음.
- 실거래 타이밍 채점은 9건뿐 (자동매매 중단 중).

## 다음 판단 시점
- **9/20~21**: 사전등록 7일 차 — B1·B2·B3·B4 가 공식 문턱(클러스터 300·t≥2·전후 반쪽 +)을 넘는지 같은 명령으로 재측정.
- 넘으면 가족·방향별로 **그림자 먼저** 구현 → 사장님 승인 → 켜기.
