All paths are under `C:/Users/user/바이낸스/binance-auto-trader/.claude/worktrees/infallible-euler-6dc297/backend/`.

| # | v2.20 bullet | Status | Where (file:line · function) | Setting key(s) → default |
|---|---|---|---|---|
| 1 | 10%+ daily movers universe | EXISTS (two layers) | `app/services/market_movers.py:67 top_movers()` (24h-change ranked, up 50 / down 50, `DEFAULT_TOP_N=50` L31, `MIN_QUOTE_VOLUME=5_000_000` L48); consumers: pump_top_detector / long_bottom_detector / bb_mid_line / surge_peak_ladder / chart_learning_worker. Entry-side hard gate: `app/services/chg24_entry_gate.py:175 passes()` called from `app/services/execution_service.py:188 start_stage1()` (Fix 310, stage-1 only, `_quick_` manual exempt). | `entry_chg24_gate_enabled`; `entry_chg24_gate_mode` → "rank" (alt "abs"); `entry_rank_top_n` → 50; `entry_min_abs_chg24` → 10.0 (L67, abs mode only); `entry_priority_abs_pct` → 10.0; `entry_slot_tight_pct` |
| 2 | 15m entry timing + 4H trend filter | EXISTS (15m = judge, 4H = reference by default) | 15m: `app/services/peak_confirmation.py:152 confirm_peak(bc, symbol, side, *, min_swings=None)` — `PEAK_TF="15m"` L41, 80 bars, swing count ≥2 (L43-46) + RSI/MACD/CCI turns ≥2/3 (RSI_HIGH 65 / RSI_LOW 35 / CCI ±80 L48-51). 4H: `app/services/trend_4h_gate.py:241 check_trend_4h()` wired in `app/workers/auto_bb_breakdown_worker.py:1580-1600` (`_create_auto_bb_strategy`), `trend_4h_gate_enabled()` L211 **default OFF** (memory: gate was disproved 9/3). 1h late-entry veto: `auto_short_at_top_worker.py:327 check_hist_rising(...,"1h",min_bars=2)`. | `trend_4h_gate_enabled` → OFF; `trend_4h_reversal_exempt` → ON; `trend_4h_reentry_exempt` → ON; `top_short_skip_if_1h_down` → True; `confluence_gate_enabled` → OFF (`confluence_gate.py:74`) |
| 3 | Stage 1 small entry (10 USDT) | EXISTS | `app/services/sajangnim_capital.py:57 DEFAULT_CAPITAL_LADDER=[10,300,600]`, `get_capital_ladder()` L70, `compute_stage1_capital(bc, db)` L143 (called at `auto_short_at_top_worker.py:~190` and LONG worker `_create_long_strategy` L1108). Order placed by `execution_service.py:188 start_stage1()` → `_place_stage_entry_order` L1154. | `sajangnim_capital_ladder` → "10,300,600"; `sajangnim_pyramid_capital` → 300 (L62-63) |
| 4 | 99% partial stop leaving 10 USDT residue, stage-2 monitoring continues | EXISTS (default OFF) | `app/services/stage_trim.py:235 compute_trim()` (TRIM/SKIP/BLOCK L107-109; keep = margin 10 × leverage, `MIN_NOTIONAL_SAFETY=1.1` L80, `MIN_TRIM_RATIO_DEFAULT=2` L88); `trim_enabled(db, strategy)` L112 per-strategy. Callers: SL path `app/services/tp_sl_orchestrator.py:623 _execute_force_stop_loss` → trim block L678-700 (Fix 326: SKIP = leave residue, do not full-close); pre-stage path `execution_service.py:1784 _trim_before_stage` (L1846-1900, Fix 313/316); `risk_service.py:237`. Residue keeps `STAGE_N_OPEN` so `app/workers/stage_trigger_worker.py:287 run_stage_trigger_once` keeps watching (Fix 306 exclusivity L356-396: trim mode beats retry mode). | `stage_trim_before_next_enabled` → **OFF** (L65); `stage_keep_notional_usdt` → 10 (margin, L66/77); `stage_max_cumulative_loss_usdt` → unlimited; `stage_min_trim_ratio` → 2; `stage_trim_exclude_modes` (comma list) |
| 5 | Stage 2 at set trigger price (residue + new capital) | EXISTS | `stage_trigger_worker.py:450-465` reads `StrategyTemplate.trigger_mode` (default `PRICE_DOWN_PCT`, `app/models/strategy_template.py:101`); price compare L862-868 (`should_fire = mark>=trigger` SHORT / `mark<=trigger` LONG); ladder/split use peak-stall judge L910-925 (Fix 260/ladder_peak_stall). Trigger % from `app/services/strategy_calculator.py:171/325` (`trigger_percents` array, `_default_middle_trigger_pct`), execution `execution_service.py:305 trigger_next_stage()`. Capital = ladder[stage-1] (`sajangnim_capital.py:172 compute_reentry_capital`). | `ladder_peak_stall_enabled` (default ON per L916 comment); template JSON `trigger_percents`; `DEFAULT_LAST_LONG/SHORT_TRIGGER_PCT=20` (`strategy_calculator.py:35-36`) |
| 6 | Stage 3 final trigger | EXISTS | Same worker; `MAX_REENTRY_STAGE=3` (`sajangnim_capital.py:169`); last-stage trigger dual storage resolved Fix 234 (`last_stage_trigger_percent` vs `trigger_percents[-1]`, `strategy_template.py:19`). | as above |
| 7 | Basic (fixed trigger) vs OBV auto strategy | EXISTS | Branch key `StrategyTemplate.trigger_mode` ∈ {`PRICE_DOWN_PCT`, `OBV_REVERSE`} (`strategy_template.py:97-103`). OBV path `stage_trigger_worker.py:792-855` → `app/services/stage_entry_signal.py:58 check_stage_entry_signal()` (obv_gate + confirm_peak, label L55); retry+OBV L711-741; price path L862. `_is_price_mode` L1153. | `trigger_mode` column (server_default `PRICE_DOWN_PCT`); no SystemSetting |
| 8 | Pyramiding max 2 in profit + "body keeps growing" | PARTIAL — count/ROI/indicator gates exist; **candle-body condition = gap** | `app/workers/success_pyramiding_worker.py:455 run_success_pyramiding`: gate order L643 `MAX_PYRAMID_COUNT=2` (L61) → L655-680 ROI ≥ `_trigger_roi` (L408, default `MIN_UNREALIZED_ROI_PCT=5.0` L46) → L681 `_allowed_sides` → L686 `_min_move_pct` → L694-701 peak retrace ≤ `PEAK_HOLD_TOLERANCE_PCT=2.5` → L704-711 `MIN_SUSTAIN_PCT=0.5` vs start_price → L713-731 after-TP room → L735 24h filter (log only) → **L906-928 indicator gate** `trend_4h_gate.py:396 check_pyramid_trend()` = 15m MACD hist rising N completed bars (`pyramid_hist_accel_bars`→3), 4H reference only unless veto. No candle body/wick logic anywhere in this worker. `add_position_now` L1387 (mode="preserve", cap_loss). | `sajangnim_pyramid_enabled` → ON (L484); `sajangnim_pyramid_trigger_roi` → 5.0; `pyramid_sides` → "SHORT" (L322); `pyramid_min_move_pct` → 3.0; `pyramid_indicator_gate_enabled` → ON (L295); `pyramid_4h_veto_enabled` → OFF (`trend_4h_gate.py:377`); `pyramid_hist_accel_bars` → 3 (1..10); `pyramid_cap_loss_enabled` → ON; `pyramid_after_tp_enabled` → ON; `pyramid_after_tp_min_trail_room_pct` → 2.0; `COOLDOWN_SECONDS=300` |
| 9 | Don't exit early on wick noise (trailing/TP engine) | GAP — engine is tick-based on mark price, no completed-bar / wick filter | `app/services/risk_service.py:555 evaluate_take_profit_level()`: mark from Redis `get_mark_price` L575-580 every tick; peak via `_update_peak_pnl` L899; trailing branch L780-836 (Fix 31/335: any TP fired + `pnl <= peak - retrace`), legacy branch L842-848; SL `evaluate_stop_loss` L270 / `evaluate_force_stop_loss` L394 (also mark-tick). Constants `app/core/risk_constants.py:127 TRAILING_RETRACE_PCT=5`, `TRAILING_PEAK_THRESHOLD_PCT=20` L119, `TRAILING_MIN_TP_INDEX=3`, `TRAILING_MIN_STAGE=1`, `FORCE_SL_ROI_DEFAULT=5` L81. TP1 sizing `app/services/adaptive_tp.py:120 pick_tp1()`. A wick can set a new peak and a wick can trip `peak - retrace` inside the same 15m bar. | per-strategy `trailing_retrace_pct` (NULL→5), `tp1_pct_override`, `force_sl_*_override`; `trailing_require_peak_ge_tp1` → OFF; `adaptive_tp_enabled`, `adaptive_tp_surge_chg24`, `adaptive_tp_surge_tp1`, `adaptive_tp_calm_tp1` |
| 10 | Per-strategy parameters synced to core | PARTIAL | Per-strategy overrides live as columns on StrategyInstance/Template (`force_sl_enabled_override`, `force_sl_roi_override`, `trailing_retrace_pct`, `tp1_pct_override`, `trigger_mode`, `capital_management_mode`, template JSON `capitals`/`trigger_percents`) and are read directly by risk_service/stage_trigger/pyramiding. Global knobs are SystemSetting rows read ad-hoc per module. `app/workers/settings_sync_worker.py:26 run_settings_sync_once` only validates `WALLET_LIMIT_PCT` hourly — there is **no** generic per-strategy↔core sync module. UI card at `app/api/v1/terminal.py:1237` (trim.enabled). | n/a |
| P1 | Candle battle (body/upper/lower wick, completed 15m, LONG lower-wick+bull body / SHORT upper-wick+bear body) | GAP as a live gate; primitives exist (see existing_candle_code) | — | — |

## wiring
Exact plug-in points for a new `app/services/candle_battle.py` (all paths relative to the worktree backend/):

(a) SHORT pipeline — `app/workers/auto_short_at_top_worker.py`, function `run_auto_short_at_top()` (L111). Per-alert gate order inside the `for key in alert_keys` loop (L197+):
   1. L200-206 `symbol in active_syms` / `confidence < 0.85` skip
   2. L209-217 `obv_gate.check_obv_gate(bc, symbol, "SHORT")` (Fix 65, fail-open)
   3. L220-227 `bidirectional_blocklist.is_bidirectional_blocked` (Fix 66)
   4. L230-237 `pump_dump_regime.is_regime_blocked_for_short` (Fix 66)
   5. **L262-269 `peak_confirmation.confirm_peak(bc, symbol, "SHORT")` → `continue` on fail (Fix 111)**
   6. L281-321 Fix 346 surge-start veto / LONG hand-off (`momentum_phase.is_surge_start`, setting `surge_start_short_veto_enabled`)
   7. L325-340 Fix 350 1h hist already-down veto (`top_short_skip_if_1h_down`, default True)
   8. L343-348 `_create_auto_bb_strategy(db, symbol, side, cfg, strategy_type_suffix="_SAJANGNIM_TOP")` → common gates in `auto_bb_breakdown_worker.py:1404` (slot L1433, confluence L1458/L1619, Fix 274 long-surge L1496, trend_4h L1580) → `execution_service.start_stage1` L2083 (chg24 gate inside start_stage1 L188+).
   → Insert the candle-battle SHORT check **immediately after step 5 (after L269 `logger.info("[auto_short_top+Fix111] ...")`) and before Fix 346 (L281)**, pattern: `mode = candle_battle.mode(db)`; if "shadow" log+Redis only, if "gate" `skipped += 1; continue`. Use `bc.get_klines(symbol=..., interval="15m", limit=...)` and drop the in-progress bar with `chart_events.trim_open_bar` (L473).

(b) LONG bottom worker — `app/workers/auto_long_at_bottom_worker.py`:
   - Candidate judgment `_check_long_entry_conditions(bc, symbol, ticker_24h, *, surge_pullback_on, stats)` L566 (called from scan loop L1776). Order: L598 `_surge_pullback_probe` (Fix 244, blocked→return / ok→return SURGE_PULLBACK) → L627 `_check_trend_strength_long` extreme_bull skip → L640 pattern-A skip (+5..+15%) → L672+ pattern B (chg24 ≤ PATTERN_B_MAX_CHG) with Fix 256 support-breakdown veto → OBV/MACD/RSI reversal AND-gates (B0..B3, see L1795-1800 bucket comments). Result dict keys: detected/passed/confidence/signals/entry_snapshot/reason/pattern/priority.
   - Execution `_create_long_strategy(...)` L1108: **L1147-1157 `confirm_peak(bc, symbol, "LONG")` (Fix 111b) skipped for patterns SURGE_PULLBACK/SURGE_START/MULTIDAY_PULLBACK (L1144)** → L1159 `_create_auto_bb_strategy(db, symbol, "LONG", cfg, strategy_type_suffix="_SAJANGNIM_BOTTOM")` with `cfg["entry_pattern"]=pattern`.
   → Insert LONG candle-battle check right after the Fix 111b block (after L1157, before `from app.workers.auto_bb_breakdown_worker import _create_auto_bb_strategy` L1159) so it applies to every pattern; `return None` in gate mode (that is how this function signals skip). Alternatively add as a bucket inside `_check_long_entry_conditions` (append key to result so Fix 151 `_bump(f"nd:{pattern}...")` L1795 shows it).
   - `chart_events.is_bottom_reversal(kl_15m, kl_4h, *, market_breadth=None, db=None, cfg=None, trim=True)` L1157 → `_bottom_base` L1067 + `_bottom_rules` L1090 (rules m15_smallbody / m15_atr_mid / m15_pctb_low / m15_below_bb_lower, `bottom_min_points`=3) → `_finish` L523. **It has 0 live callers** (only `chart_learning.py:310` via `_r_bottom_331`); the LONG worker never calls it. A candle-battle rule can be added as one more `rules[...]` entry in `_bottom_rules` (L1138-1144) with a new `Thresholds` field (dataclass L118, loader L177-180) — but that only affects the journal until someone wires `is_bottom_reversal` into (b).

(c) Pyramiding indicator gate — `app/workers/success_pyramiding_worker.py:906-928` inside `run_success_pyramiding()`: `if _indicator_gate_enabled(db): ... _ok273, _why273, _det273 = check_pyramid_trend(_bc273, si.symbol, si.side, db=db); if not _ok273: skipped += 1; _bump("indicator_not_rising"); continue`. Two options: (1) add the body-growth check as a new block right after L928 (`logger.info("[Fix273] ✅ ...")`) reusing `_bc273` and `_bump("body_not_growing")`; (2) extend `app/services/trend_4h_gate.py:396 check_pyramid_trend()` after the 15m hist test (L438-441) with `det["body"]=...` so the shadow detail lands in the existing `_det273` log. Fix 358 `_trailing_room_pct` L380 shows how per-strategy trailing is read if the "ignore small opposite wick" rule needs the trailing state.

(d) chart_learning RULES registry — `app/services/chart_learning.py`:
   - `RuleCtx` dataclass L262-274: fields `j, c, h, l, v, hist, rsi14, obv, kl15 (tail 260 bars incl. j), kl1h (tail 60), kl4h (tail 60)`. Note **no `o` (open) field** — opens must be read from `ctx.kl15[-1][1]` (bar = [open_time, o, h, l, c, v]) or a field added.
   - `Rule(key, side, label, origin, fn)` frozen dataclass L278-283; origin ∈ {"system","candidate"}.
   - `RULES: tuple[Rule, ...]` L398-409 (10 rules: toprev_331, s2_hist_turn_down, s1_breakdown, confirm_peak_111, off8_267 | pullback_331, bottom_331, surge_start_346, multiday_rebound_352, l1_hist_turn_up). Add e.g. `Rule("candle_battle_short", "SHORT", "...", "candidate", _r_candle_battle_short)` and the LONG twin.
   - `LABEL_VERSION = 2` L70 — bump to 3 when adding rules; `label_row()` L424 stamps `outcome["version"]`; `build_report()` L604 / `render_markdown()` L677.
   - Relabel CLI: `app/workers/chart_learning_worker.py:406 relabel(days=60, *, limit=None, only_old_version=True)` (skips rows whose `outcome.version >= LABEL_VERSION` L420), argparse sub-command `relabel --days N --limit N --all` L491-494, dispatch L517; `python -m app.workers.chart_learning_worker relabel --days 60`. Rules that call live code use `_BarsClient` L325 (fake bc with stub in-progress bar).

Other candle-related live code to stay consistent with (not wired to chart_events): `app/workers/resistance_reversal_worker.py:173-174` (`wick >= body * UPPER_WICK_RATIO`, bearish body) and `app/workers/peak_break_reversal_worker.py:209-210` (`wick >= body*1.5`) — both wick-vs-body, not wick-vs-range.

## gaps
1. Candle battle quantification (spec Part 1) — no module computes upper wick vs range for entry; `_lwick_ratio` exists but no `_uwick_ratio`; `bb_top_analyzer` wick is display/learning only; the two live wick users (resistance_reversal / peak_break_reversal) use wick-vs-body and are alert producers only. No LONG "long lower wick + bullish body" sequence rule and no SHORT "long upper wick + long bearish body" sequence rule exists anywhere; no absolute-size (ATR) normaliser is applied to candles in any live path.
2. Pyramiding "body keeps growing in trade direction, ignore small opposite wicks" (Part 2 §④) — gap. Live gate is 15m MACD-hist acceleration (`check_pyramid_trend`, `pyramid_hist_accel_bars`=3) plus price-based peak-retrace/sustain checks on **mark ticks**, not candle bodies. No place reads open/close bodies in `success_pyramiding_worker.py`.
3. "Do not exit early on wick noise" — gap. `risk_service.evaluate_take_profit_level` / trailing / force-SL evaluate every mark-price tick (Redis), peak is updated by ticks, so a single wick both raises the peak and can trigger `peak - retrace`. No completed-bar confirmation, no wick filter, no debounce setting exists (`trailing_require_peak_ge_tp1` is the only trailing toggle). Any change here touches the SL path too and needs its own OFF-default switch.
4. `chart_events.is_bottom_reversal` / `is_top_reversal` (and the `m15_smallbody` rule) have zero live callers — they are journal-only; the LONG worker's real gate is `confirm_peak(bc, symbol, "LONG")` in `_create_long_strategy` L1150. Wiring a candle rule into `_bottom_rules` alone does nothing in production.
5. `RuleCtx` has no `o` (opens) sequence — journal rules must read opens from `ctx.kl15` (bar[1]) or the dataclass must gain an `o` field (touches `label_row` construction L424+).
6. Bullet 10 "per-strategy parameters synced to the core" — no generic sync layer; `settings_sync_worker` only checks WALLET_LIMIT_PCT. Per-strategy overrides are scattered columns read directly by each engine.
7. `trend_4h_gate_enabled` and `confluence_gate_enabled` and `stage_trim_before_next_enabled` are all default OFF in code — spec bullets 2 and 4 are "implemented" only if the operator has set those SystemSetting rows on the VPS (not verifiable read-only here).
8. Measurement note: the task's numeric evaluation (journal replay, CV cells, baselines) was not part of this read-only mapping deliverable and no numbers were produced; scratchpad dir `.../scratchpad/v220/` was not created.