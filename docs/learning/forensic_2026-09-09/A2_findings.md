1) Deployed commit: 6618e46 (07-29). No commits 07-30..08-05; first behaviour change 390af4c on 08-07. The 07-29..08-04 profit (+3,120, 61% win) AND the 08-05/06 losses (−4,686, −4,112) ran the identical code — git cannot point to a logic regression for the initial collapse; the rows show capital median 23–1,282 → 2,010 and 34/39 trades with adds on 08-05.
2) THEN engine = no auto entry/add/reentry workers at all (27 jobs: guards + price-only stage_trigger + tp_sl + auto_reentry that only serves reentry_policy='auto' templates; `_quick_` templates are manual_ready). Good-window trades: all lev 2, all `_quick_`, stage 1 only, adds by hand (ADD_POSITION_TP_RESET 13 / PRESERVE 11), MANUAL_TP 15/21, TP1 override 50/25 via PATCH, force_sl override None (inherits LONG ON −10% / SHORT OFF from code; DB had one settings row), 0 FORCE_STOP_LOSS events.
3) v130 (08-07..09) is the first real change: force-SL stage gate (then/risk_service.py:213 'no stage gate' → now/risk_service.py:434-511 gate), lev 5x default (c5c0153) → lev5 in 137/223 trades that week and first FORCE_STOP_LOSS events (31 on 08-07), trailing peak 5→20 (v173 08-18), retrace 10→5.
4) NOW differences that are code-level, not settings: every new instance gets tp1_pct_override=15 and force_sl_enabled_override=True/force_sl_roi_override=25 (strategy_service.py:588-590; force_sl_roi_new_default=0 refused at system_settings_service.py:110); trailing constants risk_constants.py:124/132; normal SL default 90→50 (risk_constants.py:59); TP1 override semantics v105 (all TPs) → v131 (TP1 only, constitution C10, deliberate).
5) NOW differences restorable by settings only (no restart): auto workers (sajangnim_top_short_daily_limit=0, auto_bb_break_daily_limit=0, sajangnim_reentry_daily_limit=0, pump_split_enabled=0, bb_mid_line_mode=off, surge_ladder_mode=off, unified_entry_enabled=0, scheduled_entry_enabled=0, auto_obv_enabled=0, bottom_long_dip_alerts_enabled=0); pyramiding on manual strategies (sajangnim_pyramid_enabled=0, success_pyramiding_worker.py:484; DAY_02 shows 47 manual DYNAMIC adds in 7 d); partial stop (stage_trim_before_next_enabled=0, stage_trim.py:65,112, tp_sl_orchestrator.py:623-720); adaptive TP (adaptive_tp_enabled=0, adaptive_tp.py:80); chg24 gate (entry_chg24_gate_enabled=0; manual `_quick_` already exempt at execution_service.py:205); symbol exclusion (excluded_symbols, symbol_exclusion.py:52); global force-SL rows force_sl_long_roi=80/force_sl_short_enabled=true/force_sl_short_roi=80 (08-22) → set 10/false.
6) v130 stage gate is effectively bypassed for new instances because Fix 322 exempts any strategy with force_sl_roi_override (risk_service.py:192-248) and Fix 362 sets it on all new instances; old instances without override still hit the gate (Fix 235 unlock setting ON).
7) Per-stage additional_margin_usdt path still exists (stage_trigger_worker.py:1467-1472); only the 08-22 auto_add_margin worker was retired (Fix 340). Leverage default 2 is unchanged (index.html:2153). Loop intervals tp_sl/stage_trigger 10→15 s, auto_reentry 30→60 s (ban mitigation).
8) Measured verdicts recorded in repo: re-entry structurally dead until Fix 103-105 and again until Fix 262; stop switch broken (Fix 107); 6 silent failures (Fix 133-145); TP1 option dead (Fix 183, then 3.7%→50%); 4H gate disproved 09-03 and demoted Fix 330; confluence gate Fix 247 effect −2.06 (20 blocks/6 h); pyramiding −1,729 (09-05) / −635 on 188 adds (09-07); BB split pyramiding damage Fix 213; auto workers never a positive week (dataset: 717 inst −2,118).
9) Screens: THEN index.html + 35 JS; NOW +9 HTML pages, +12 JS modules, +16 API modules (inventory in report §1.6).