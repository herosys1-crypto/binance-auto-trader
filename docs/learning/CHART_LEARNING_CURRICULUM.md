# 차트 학습 커리큘럼 — 상승 50위·하락 50위를 매일 나눠서 배운다 (Fix 353, 2026-09-05 시작)

> 사장님: "상승 50위 하락 50위 심볼을 차트를 우리가 필요한 시스템로직을 위해서 분석학습 할 수 있을까?
> 상세하게 분석해서 적용할수 있게 한번에 어려우면 할수 있는 만큼씩 매일 매일 나눠서 학습을 해줘"
>
> 사장님 (시스템 로직): "시스템로직은 첫번째 진입이 실패했을때 좋은 자리에서 포지션 진입부터 승부를 하고
> 처음부터 승리하면 포지션 추가로 수익을 만들어 가는거야 실패했을 손실을 최소화하고 다시 포지션 진입하는 로직이야"

## 0. 학습이 답해야 하는 세 결정점 (사장님 로직 그대로)

| # | 결정점 | 질문 | 지금 코드 |
|---|---|---|---|
| ① | 첫 진입 자리 | 어느 규칙이 기준선(같은 자리 무작위 봉)보다 낫나 | 정점(pump_top→auto_short_at_top) · 저점/급등 계열(long_bottom→auto_long_at_bottom) · 볼밴 분할 |
| ② | 이겼을 때 추가 | 첫 진입이 이기고 있을 때 어디서 얹으면 이어지나 | 피라미딩(Fix 345/348: 15m 가속 3봉 · 이동 ≥3% · SHORT 만) |
| ③ | 졌을 때 최소화 → 다시 진입 | 손절 뒤 「진짜 자리」는 어디였고 재진입 규칙이 그걸 잡나 | 부분손절(10 USDT 잔량) → 정점-주춤 2·3단계(Fix 260/342) · 재진입 워커 |

## 1. 장치 (매일 자동으로 쌓인다)

- **스냅샷** (UTC 00:05 = KST 09:05, 일봉 마감 직후): 당일 상승 50·하락 50 ∪ 3일·5일 상승 50·하락 50 → 심볼별 15m 200봉 + 4h 61봉 + 지표를 **그 시각 기준 완성봉만** 저장. 한 심볼에 여러 태그(예: `UP5D`+`DOWN` = 며칠 상승 뒤 당일 조정).
- **라벨링** (매시간): 36h 지난 행에 결과 15m 144봉을 붙이고 「자리의 값」(정점·저점까지 시간, 정점 뒤 12h 낙폭, 저점 뒤 반등)과 **규칙별 첫 충족 봉의 결과**(레버 2 · SL −5% · TP +15% ROI · 12h)를 계산.
- **보고서**: `python -m app.workers.chart_learning_worker report --days 60` 또는 `GET /api/v1/chart-learning/report.md`.
- 되돌리기: `chart_learning_enabled=0` (재시작 불필요). 표: `chart_learning_days` (라벨은 영구, 원시 봉은 45일).
- 미래참조가 **구조적으로 불가능**한 이유: 감시 대상이 스냅샷 시각에 확정되고, 결과는 그 뒤에만 붙는다. 9/3 에 4번 잡은 「과거 봉에 오늘 순위를 붙이는」 함정이 생길 자리가 없다.

## 2. 채택 규칙 (사람이 한다 — 자동 튜닝 없음)

1. 규칙은 먼저 레지스트리(`chart_learning.RULES`)에 **후보(candidate)** 로 넣고 라벨이 쌓이길 기다린다.
2. 교차검증 네 조각(심볼 짝/홀 · 날짜 전/후반)이 **전부 기준선보다 양수**이고 n ≥ 100 이면 후보 → 사장님 보고.
3. 배선은 설정키와 함께, 전략 가족별로(A 사다리 / B 볼밴 / C 기타), 공용 경로에 넣지 않는다.
4. 배선 뒤에도 매일 재측정 — 뒤집히면 설정으로 끈다.

## 3. 일정 (하루에 하나씩 — 표본이 되는 대로)

| Day | 날짜 | 주제 | 결정점 | 필요한 표본 |
|---|---|---|---|---|
| 1 | 09-05 | 장치 설치 + 백필 20일 + **자리별 기준선 · 시스템 규칙 넷의 첫 성적** | ① | 백필 |
| 2 | 09-06 | 당일 상승 50 의 **정점 SHORT**: 정점까지 시간 분포, `toprev_331` vs 후보 2종, 「24h 몇 %」 구간별 | ① SHORT | 백필+1일 |
| 3 | 09-07 | 3·5일 상승 + 당일 하락(조정)의 **롱**: `multiday_rebound_352` · `pullback_331` · 조정 깊이별 | ① LONG | 백필+2일 |
| 4 | 09-08 | 당일 하락 50 · 며칠 하락: **추가 하락 vs 반등** — SHORT 가 되는 자리가 있나 (지금은 저점 롱 OFF) | ① | 백필+3일 |
| 5 | 09-09 | **② 이겼을 때 추가**: 규칙 발동 뒤 +3% 이동한 건에서 그 다음 6~24봉의 지속률, 15m 가속 3봉 조건 검증 | ② | 라벨 1,500+ |
| 6 | 09-10 | **③ 졌을 때 다시 진입**: 첫 진입이 SL 난 심볼-일에서 「진짜 정점/저점」이 몇 시간 뒤였나, 정점-주춤 조건이 거길 잡나 | ③ | 라벨 1,500+ |
| 7 | 09-11 | 스냅샷 지표(RSI·%B·4H EMA·5일 고점 거리)로 **어떤 상태의 종목이 이기나** — 종목 선별 축 | ①②③ | 라벨 2,000+ |
| 8+ | 매일 | 새 후보 규칙 1개씩 넣고 재측정. 실매매 결과(risk_events)와 대조. | | |

## 4. 일지

- [Day 1 — 2026-09-05](DAY_01_2026-09-05.md)
